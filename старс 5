import telebot
from telebot import types
import sqlite3
import datetime
import re

TOKEN = "7626087655:AAH0fFlDD7GH1CVJP7zFaGtgCIx9j0zdDqw"
ADMIN_ID = 6392028972
bot = telebot.TeleBot(TOKEN)
CHAT_ID_1 = -1002294344190  # ID первого чата
CHAT_ID_2 = -1002396806491  # ID второго чата

conn = sqlite3.connect('stars_bot.db', check_same_thread=False)
cursor = conn.cursor()

# таблицы
cursor.execute('''CREATE TABLE IF NOT EXISTS users (
    user_id INTEGER PRIMARY KEY,
    username TEXT,
    stars REAL DEFAULT 0.00
)''')
conn.commit()

try:
    cursor.execute("ALTER TABLE users ADD COLUMN gram REAL DEFAULT 0.00")
    conn.commit()
except sqlite3.OperationalError:
    pass

cursor.execute('''
CREATE TABLE IF NOT EXISTS republics (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    creator_id INTEGER NOT NULL,
    chat_id INTEGER NOT NULL,
    date_created TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
''')
conn.commit()

# Добавление таблицы для хранения скинов пользователя
cursor.execute('''CREATE TABLE IF NOT EXISTS skins (
    user_id INTEGER PRIMARY KEY,
    selected_skin TEXT DEFAULT NULL
)''')
conn.commit()

# Завершение работы с базой данных (например, при остановке бота)
def close_connection():
    conn.commit()  # Сохранение всех изменений
    conn.close()   # Закрытие соединения

# Вызовите эту функцию, когда бот будет выключаться
# Например, перед завершением работы программы:
# close_connection()

# Создание таблицы для хранения рейтинга
cursor.execute('''CREATE TABLE IF NOT EXISTS game_stats (
    user_id INTEGER PRIMARY KEY,
    ribbons INTEGER DEFAULT 0
)''')
conn.commit()

# Создание таблицы для хранения заданий
cursor.execute('''
CREATE TABLE IF NOT EXISTS tasks (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    channel_link TEXT NOT NULL
)
''')
conn.commit()

# Таблица для хранения кланов
cursor.execute('''
CREATE TABLE IF NOT EXISTS clans (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL UNIQUE,
    creator_id INTEGER NOT NULL,
    date_created TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
''')
conn.commit()

# Таблица для хранения участников кланов
cursor.execute('''
CREATE TABLE IF NOT EXISTS clan_members (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    clan_id INTEGER NOT NULL,
    FOREIGN KEY (clan_id) REFERENCES clans (id) ON DELETE CASCADE
)
''')
conn.commit()

ADMIN_ID = 6392028972
PROVIDER_TOKEN = "PAYMENT_PROVIDER_TOKEN"  # Токен платежного провайдера
CURRENCY = "XTR"

bot = telebot.TeleBot(TOKEN)

# Подключение к базе данных
conn = sqlite3.connect('stars_bot.db', check_same_thread=False)
cursor = conn.cursor()

# Создание таблицы пользователей
cursor.execute('''
CREATE TABLE IF NOT EXISTS users (
    user_id INTEGER PRIMARY KEY,
    username TEXT,
    stars REAL DEFAULT 0.00,
    gram REAL DEFAULT 0.00
)
''')
conn.commit()

from telebot import types
import sqlite3

# Создаем таблицу для хранения стартового фото (если её нет)
cursor.execute("""
    CREATE TABLE IF NOT EXISTS start_photo (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        file_id TEXT
    )
""")
conn.commit()

class SafeCursor:
    def __init__(self, connection):
        self.connection = connection
        self.cursor = connection.cursor()

    def execute(self, query, params=None):
        try:
            if params:
                return self.cursor.execute(query, params)
            return self.cursor.execute(query)
        except Exception as e:
            print(f"Ошибка выполнения запроса: {e}")
            return None

    def fetchall(self):
        try:
            return self.cursor.fetchall()
        except Exception as e:
            print(f"Ошибка при fetchall: {e}")
            return []

    def fetchone(self):
        try:
            return self.cursor.fetchone()
        except Exception as e:
            print(f"Ошибка при fetchone: {e}")
            return None

    def close(self):
        try:
            self.cursor.close()
        except Exception:
            pass  # Игнорируем ошибку закрытия

# Функция получения сохраненного стартового фото
def get_start_photo():
    cursor.execute("SELECT file_id FROM start_photo ORDER BY id DESC LIMIT 1")
    result = cursor.fetchone()
    return result[0] if result else None

@bot.message_handler(commands=['start'])
def start_command(message):
    user_id = message.from_user.id
    username = message.from_user.username or "Без имени"

    # Проверяем реферальный ID
    args = message.text.split()
    referrer_id = int(args[1]) if len(args) > 1 and args[1].isdigit() else None

    # Проверяем, зарегистрирован ли уже этот пользователь
    cursor.execute("SELECT user_id FROM users WHERE user_id = ?", (user_id,))
    existing_user = cursor.fetchone()

    welcome_text = (
        "🎮 Играйте в Kraft Skins 💱 _игровой бот 1.0_\n\n"
        "👤 С другом в ЛС\n"
        "🥊 Игры в своей группе\n"
        "🎭 Купить скин\n\n"
        "💬 Игровой чат: @GVB_klan\n"
        "🖥 Баги и идеи: @laborotory_2\n"
        "🐶 Питомец: /pt\n"
        "👤 Профиль: Чч"
    )

    start_photo_id = get_start_photo()

    # Кнопка "Добавить бота в чат"
    add_bot_button = types.InlineKeyboardMarkup()
    add_button = types.InlineKeyboardButton(
        text="➕ Добавить бота в чат",
        url=f"https://t.me/{bot.get_me().username}?startgroup=true"
    )
    add_bot_button.add(add_button)

    # Если пользователь уже есть
    if existing_user:
        if start_photo_id:
            bot.send_photo(message.chat.id, start_photo_id, caption=welcome_text, reply_markup=main_menu())
        else:
            bot.send_message(message.chat.id, welcome_text, reply_markup=main_menu())

        # Отдельно отправляем кнопку добавления бота
        bot.send_message(message.chat.id, "🚀 Добавьте бота в группу!", reply_markup=add_bot_button)
        return

    # Добавляем нового пользователя
    cursor.execute("INSERT INTO users (user_id, username, stars, gram) VALUES (?, ?, 0, 0)", (user_id, username))
    conn.commit()

    # Начисляем бонус рефереру
    if referrer_id:
        cursor.execute("UPDATE users SET gram = gram + 1500 WHERE user_id = ?", (referrer_id,))
        conn.commit()
        try:
            bot.send_message(referrer_id, f"🎉 По вашей реферальной ссылке зарегистрировался новый пользователь {username}!\n\n💰 Вам начислено 1500 GRAM.")
        except Exception as e:
            print(f"Ошибка при отправке сообщения рефереру: {e}")

    # Отправляем приветственное сообщение
    if start_photo_id:
        bot.send_photo(message.chat.id, start_photo_id, caption=welcome_text, reply_markup=main_menu())
    else:
        bot.send_message(message.chat.id, welcome_text, reply_markup=main_menu())

    # Отправляем кнопку "Добавить бота в чат"
    bot.send_message(message.chat.id, "🚀 Добавьте бота в группу!", reply_markup=add_bot_button)

# Функция создания основного меню
def main_menu():
    keyboard = types.ReplyKeyboardMarkup(resize_keyboard=True, one_time_keyboard=False)
    keyboard.add("💱 Обменять", "Магазин", "🎰 Казино")
    keyboard.add("💳 Донат", "📊 Статистика", "💰 Заработать")
    keyboard.add("🎁 Бонус", "🎰 Мега Казино", "🏪 Магазин скинов")
    keyboard.add("📢 Пригласить бота", "⚙️ Крафт", "🏆 купить VIP", "📢 Объявление")
    return keyboard
    
      # Отправляем сообщение с фото (если оно есть)
    if start_photo_id:
        bot.send_photo(message.chat.id, start_photo_id, caption=welcome_text, reply_markup=start_menu_buttons())
    else:
        bot.send_message(message.chat.id, welcome_text, reply_markup=start_menu_buttons())

# Функция для загрузки нового стартового фото администратором
@bot.message_handler(commands=['ll'])
def request_start_photo(message):
    admin_id = 6392028972  # Замените на свой Telegram ID
    if message.from_user.id == admin_id:
        bot.send_message(message.chat.id, "📸 Отправьте фото, которое будет использоваться при старте бота.")
        bot.register_next_step_handler(message, save_start_photo)
    else:
        bot.send_message(message.chat.id, "⛔ У вас нет прав для выполнения этой команды.")

def save_start_photo(message):
    if message.content_type == 'photo':
        file_id = message.photo[-1].file_id  # Берем фото лучшего качества

        # Удаляем старое стартовое фото (если было) и записываем новое
        cursor.execute("DELETE FROM start_photo")
        cursor.execute("INSERT INTO start_photo (file_id) VALUES (?)", (file_id,))
        conn.commit()

        bot.send_message(message.chat.id, "✅ Фото успешно сохранено! Теперь оно будет отправляться при старте бота.")
    else:
        bot.send_message(message.chat.id, "⛔ Пожалуйста, отправьте именно фото!")

import random

games = {}

@bot.message_handler(commands=["games"])
def start_game(message):
    keyboard = types.InlineKeyboardMarkup(row_width=3)
    keyboard.add(
        types.InlineKeyboardButton("😄 3x3", callback_data="select_size|3"),
        types.InlineKeyboardButton("🤨 5x5", callback_data="select_size|5"),
        types.InlineKeyboardButton("😬 8x8", callback_data="select_size|8"),
     types.InlineKeyboardButton("🤖 против бота", callback_data="play_vs_bot"),
     types.InlineKeyboardButton("✌️✊🤚", callback_data="kamen_l"),
    
        )
    bot.send_message(
        message.chat.id, "Выберите режим игры для старта:\n\n🅧 Классические Крестики-Нолики\n🤖Против бота игру или 🔎Найдите соперника:", reply_markup=keyboard
    )

@bot.callback_query_handler(func=lambda call: call.data.startswith("select_size"))
def select_size(call):
    size = int(call.data.split("|")[1])
    
    keyboard = types.InlineKeyboardMarkup()
    keyboard.add(types.InlineKeyboardButton("Опубликовать игру", callback_data=f"publish_game|{size}"))

    bot.edit_message_text(
        f"⚙️Вы выбрали поле {size}x{size}. Нажмите 'Опубликовать игру' , чтобы любой мог присоединиться.⤵️",
        call.message.chat.id, call.message.message_id, reply_markup=keyboard
    )

@bot.callback_query_handler(func=lambda call: call.data.startswith("publish_game"))
def publish_game(call):
    size = int(call.data.split("|")[1])
    
    keyboard = types.InlineKeyboardMarkup()
    button_accept = types.InlineKeyboardButton("Принять вызов", callback_data=f"accept_game|{call.from_user.id}|{size}")
    keyboard.add(button_accept)

    bot.edit_message_text(
        f"🎮 Игрок {call.from_user.first_name} создал игру {size}x{size}! Нажмите 'Принять вызов' , чтобы сыграть.",
        call.message.chat.id, call.message.message_id, reply_markup=keyboard
    )

@bot.callback_query_handler(func=lambda call: call.data.startswith("accept_game"))
def accept_game(call):
    _, creator_id, size = call.data.split("|")
    creator_id, size = int(creator_id), int(size)
    opponent_id = call.from_user.id
    opponent_name = call.from_user.first_name

    if opponent_id == creator_id:
        bot.answer_callback_query(call.id, "Вы не можете играть сами с собой!")
        return

    # Получаем скины игроков
    cursor.execute("SELECT selected_skin FROM users WHERE user_id = ?", (creator_id,))
    creator_skin = cursor.fetchone()
    creator_skin = creator_skin[0] if creator_skin and creator_skin[0] else "❌"

    cursor.execute("SELECT selected_skin FROM users WHERE user_id = ?", (opponent_id,))
    opponent_skin = cursor.fetchone()
    opponent_skin = opponent_skin[0] if opponent_skin and opponent_skin[0] else "⭕"

    # Создаем поле
    board = generate_board(size)

    # Сохраняем данные игры
    games[call.message.chat.id] = {
        "board": board,
        "size": size,
        "players": {creator_id: creator_skin, opponent_id: opponent_skin},
        "turn": creator_id,
        "moves": 0,
        "message_id": None,
        "draw_offered_by": None
    }

    message = display_board(call.message.chat.id, board, size)
    games[call.message.chat.id]["message_id"] = message.message_id

def generate_board(size):
    board = [["ᅠᅠ" for _ in range(size)] for _ in range(size)]
    
    # Добавляем деревья и камни
    tree_count = random.randint(1, 2 if size == 5 else 3 if size == 8 else 1)
    stone_chance = {3: 15, 5: 20, 8: 25}[size]

    for _ in range(tree_count):
        x, y = random.randint(0, size - 1), random.randint(0, size - 1)
        board[x][y] = "🌳"

    for _ in range(size * size):
        if random.randint(1, 100) <= stone_chance:
            x, y = random.randint(0, size - 1), random.randint(0, size - 1)
            if board[x][y] == "ᅠᅠ":
                board[x][y] = "🪨"
                break

    return board

def display_board(chat_id, board, size):
    keyboard = types.InlineKeyboardMarkup(row_width=size)
    for i, row in enumerate(board):
        buttons = []
        for j, cell in enumerate(row):
            buttons.append(types.InlineKeyboardButton(cell, callback_data=f"move|{i}|{j}"))
        keyboard.add(*buttons)

    keyboard.add(types.InlineKeyboardButton("Предложить ничью", callback_data="offer_draw"))
    return bot.send_message(chat_id, "Игровое поле:", reply_markup=keyboard)

@bot.callback_query_handler(func=lambda call: call.data.startswith("move"))
def handle_moves(call):
    game = games.get(call.message.chat.id)
    if not game:
        bot.answer_callback_query(call.id, "Игра не найдена!")
        return

    user_id = call.from_user.id
    if user_id != game["turn"]:
        bot.answer_callback_query(call.id, "Сейчас не ваш ход.")
        return

    _, x, y = call.data.split("|")
    x, y = int(x), int(y)

    if game["board"][x][y] == "🌳":
        cursor.execute("SELECT quantity FROM user_skins WHERE user_id = ? AND skin = '🪓'", (user_id,))
        result = cursor.fetchone()

        if result and result[0] > 0:    
            game["board"][x][y] = "🪵"    
            cursor.execute(
                '''INSERT INTO user_skins (user_id, skin, quantity)     
                   VALUES (?, '🪵', 1)     
                   ON CONFLICT(user_id, skin) DO UPDATE SET quantity = quantity + 1''',
                (user_id,)
            )    
            conn.commit()    
            bot.answer_callback_query(call.id, "Вы срубили дерево и получили бревно! 🪵")    
        else:    
            bot.answer_callback_query(call.id, "У вас нет топора! 🪓")    
        return

    elif game["board"][x][y] == "🪨":
        bot.answer_callback_query(call.id, "На этом месте камень! Пропустите ход.")
        return

    elif game["board"][x][y] != "ᅠᅠ":
        bot.answer_callback_query(call.id, "Эта клетка уже занята!")
        return
    else:
        symbol = game["players"][user_id]
        game["board"][x][y] = symbol
        game["turn"] = list(game["players"].keys())[1 - list(game["players"].keys()).index(user_id)]
        game["moves"] += 1

        winner = check_winner(game["board"], symbol, game["size"])
        if winner:
            end_game(call.message.chat.id, user_id, symbol, game["moves"], game["message_id"])
            return

        update_board(call.message.chat.id, game["board"], game["message_id"], game["size"])

def check_winner(board, symbol, size):
    win_length = {3: 3, 5: 4, 8: 5}[size]  # Количество символов в ряд для победы

    for row in board:
        for i in range(size - win_length + 1):
            if row[i:i + win_length] == [symbol] * win_length:
                return True

    for col in range(size):
        for row in range(size - win_length + 1):
            if [board[row + i][col] for i in range(win_length)] == [symbol] * win_length:
                return True

    for row in range(size - win_length + 1):
        for col in range(size - win_length + 1):
            if [board[row + i][col + i] for i in range(win_length)] == [symbol] * win_length:
                return True
            if [board[row + i][col + win_length - 1 - i] for i in range(win_length)] == [symbol] * win_length:
                return True

    return False

def update_board(chat_id, board, message_id, size):
    keyboard = types.InlineKeyboardMarkup(row_width=size)
    for i, row in enumerate(board):
        buttons = []
        for j, cell in enumerate(row):
            buttons.append(types.InlineKeyboardButton(cell, callback_data=f"move|{i}|{j}"))
        keyboard.add(*buttons)

    keyboard.add(types.InlineKeyboardButton("Предложить ничью", callback_data="offer_draw"))
    bot.edit_message_reply_markup(chat_id, message_id, reply_markup=keyboard)

def end_game(chat_id, winner_id, symbol, moves, message_id, draw=False):
    if draw:
        bot.edit_message_text(
            f"Игра завершена ничьей! 🎉\nКоличество ходов: {moves}",
            chat_id,
            message_id
        )
    else:
        bot.edit_message_text(
            f"Игрок {symbol} победил! 🎉\nКоличество ходов: {moves}",
            chat_id,
            message_id
        )

    if winner_id:
        cursor.execute("SELECT games_played FROM game_progress WHERE user_id = ?", (winner_id,))
        result = cursor.fetchone()
        if result:
            games_played = result[0] + 1
            cursor.execute("UPDATE game_progress SET games_played = ? WHERE user_id = ?", (games_played, winner_id))
        else:
            cursor.execute("INSERT INTO game_progress (user_id, games_played) VALUES (?, ?)", (winner_id, 1))
        conn.commit()

    if chat_id in games:
        del games[chat_id]

@bot.callback_query_handler(func=lambda call: call.data == "offer_draw")
def offer_draw(call):
    game = games.get(call.message.chat.id)
    if not game:
        bot.answer_callback_query(call.id, "Игра не найдена!")
        return

    user_id = call.from_user.id

    # Проверка, может ли игрок предложить ничью
    if user_id != game["turn"]:
        bot.answer_callback_query(call.id, "Вы не можете предложить ничью, так как это не ваш ход.")
        return

    # Предложение ничьей
    keyboard = types.InlineKeyboardMarkup()
    button_accept = types.InlineKeyboardButton("Принять ничью", callback_data="accept_draw")
    button_reject = types.InlineKeyboardButton("Отказать в ничьей", callback_data="reject_draw")
    keyboard.add(button_accept, button_reject)

    bot.send_message(
        call.message.chat.id,
        "Игрок предложил ничью. Примете ли вы?",
        reply_markup=keyboard
    )

@bot.callback_query_handler(func=lambda call: call.data == "accept_draw")
def accept_draw(call):
    game = games.get(call.message.chat.id)
    if not game:
        bot.answer_callback_query(call.id, "Игра не найдена!")
        return

    user_id = call.from_user.id
    if user_id == game["turn"]:
        bot.answer_callback_query(call.id, "Вы не можете принимать ничью, пока не завершился ваш ход.")
        return

    # Завершаем игру ничьей
    end_game(call.message.chat.id, None, None, game["moves"], game["message_id"], draw=True)

@bot.callback_query_handler(func=lambda call: call.data == "reject_draw")
def reject_draw(call):
    bot.answer_callback_query(call.id, "Предложение ничьей отклонено.")

import random
from telebot import types

# Игровое состояние
games = {}

# Функция начала игры
@bot.message_handler(commands=["Играть"])
def start_game(message):
    size = 6  # Установим поле 6x6 для игры

    # Случайный выбор крестика или нолика для игроков
    player_symbol = random.choice(["❌", "⭕"])
    ghost_position = (random.randint(0, size - 1), random.randint(0, size - 1))

    # Инициализация игрового поля
    board = [["ᅠᅠ" for _ in range(size)] for _ in range(size)]
    board[ghost_position[0]][ghost_position[1]] = "👻"  # Привидение

    # Сохраняем данные игры
    games[message.chat.id] = {
        "board": board,
        "size": size,
        "players": [player_symbol, "❌" if player_symbol == "⭕" else "⭕"],  # Два игрока
        "turn": 0,  # Начинает первый игрок
        "ghost_position": ghost_position,
        "moves": 0,
        "message_id": None
    }

    message = display_board(message.chat.id, board, size)
    games[message.chat.id]["message_id"] = message.message_id

# Функция отображения игрового поля
def display_board(chat_id, board, size):
    keyboard = types.InlineKeyboardMarkup(row_width=size)
    for i, row in enumerate(board):
        buttons = []
        for j, cell in enumerate(row):
            buttons.append(types.InlineKeyboardButton(cell, callback_data=f"move|{i}|{j}"))
        keyboard.add(*buttons)

    return bot.send_message(chat_id, "Игровое поле:", reply_markup=keyboard)

# Функция обработки ходов
@bot.callback_query_handler(func=lambda call: call.data.startswith("move"))
def handle_move(call):
    game = games.get(call.message.chat.id)
    if not game:
        bot.answer_callback_query(call.id, "Игра не найдена!")
        return

    user_id = call.from_user.id
    player_symbol = game["players"][game["turn"]]
    ghost_position = game["ghost_position"]

    # Получаем координаты клетки
    _, x, y = call.data.split("|")
    x, y = int(x), int(y)

    # Проверка, что клетка пуста
    if game["board"][x][y] != "ᅠᅠ":
        bot.answer_callback_query(call.id, "Эта клетка уже занята!")
        return

    # Очищаем клетку
    game["board"][x][y] = player_symbol

    # Проверка на победу
    if check_victory(game["board"], x, y, ghost_position):
        end_game(call.message.chat.id, player_symbol, game["moves"], game["message_id"])
        return

    # Перемещение привидения
    new_ghost_position = move_ghost(ghost_position, game["size"])
    game["ghost_position"] = new_ghost_position
    game["board"][new_ghost_position[0]][new_ghost_position[1]] = "👻"

    # Обновляем ход
    game["turn"] = (game["turn"] + 1) % 2  # Меняем игрока
    game["moves"] += 1

    update_board(call.message.chat.id, game["board"], game["message_id"], game["size"])

# Функция перемещения привидения на соседнюю клетку
def move_ghost(ghost_position, size):
    directions = [(-1, 0), (1, 0), (0, -1), (0, 1)]  # Вверх, вниз, влево, вправо
    possible_moves = []

    for dx, dy in directions:
        new_x, new_y = ghost_position[0] + dx, ghost_position[1] + dy
        if 0 <= new_x < size and 0 <= new_y < size:
            possible_moves.append((new_x, new_y))

    return random.choice(possible_moves)  # Случайный выбор из возможных направлений

# Функция проверки победы
def check_victory(board, x, y, ghost_position):
    # Проверяем, окружено ли привидение
    gx, gy = ghost_position
    if (
        (gx == x and abs(gy - y) == 1) or
        (gy == y and abs(gx - x) == 1)
    ):
        return True
    return False

# Функция обновления игрового поля
def update_board(chat_id, board, message_id, size):
    keyboard = types.InlineKeyboardMarkup(row_width=size)
    for i, row in enumerate(board):
        buttons = []
        for j, cell in enumerate(row):
            buttons.append(types.InlineKeyboardButton(cell, callback_data=f"move|{i}|{j}"))
        keyboard.add(*buttons)

    bot.edit_message_reply_markup(chat_id, message_id, reply_markup=keyboard)

# Функция окончания игры
def end_game(chat_id, winner_symbol, moves, message_id):
    bot.edit_message_text(
        f"Игрок с символом {winner_symbol} победил! 🎉\nКоличество ходов: {moves}",
        chat_id,
        message_id
    )

    if chat_id in games:
        del games[chat_id]  # Удаляем игру после завершения

import random

bot_games = {}

@bot.callback_query_handler(func=lambda call: call.data == "play_vs_bot")
def play_vs_bot_callback(call):
    """Запуск игры против бота при нажатии кнопки."""
    chat_id = call.message.chat.id

    if chat_id in bot_games:
        bot.send_message(chat_id, "Вы уже играете! Завершите текущую игру перед началом новой.")
        return

    board = [["ᅠᅠ" for _ in range(5)] for _ in range(5)]
    player_symbol, bot_symbol = "⭕", "❌"

    bot_games[chat_id] = {
        "board": board,
        "player_symbol": player_symbol,
        "bot_symbol": bot_symbol,
        "turn": "player",
        "moves": 0,
        "message_id": None
    }

    message = send_bot_board(chat_id, board)
    bot_games[chat_id]["message_id"] = message.message_id

def send_bot_board(chat_id, board):
    keyboard = types.InlineKeyboardMarkup(row_width=5)
    for i, row in enumerate(board):
        buttons = []
        for j, cell in enumerate(row):
            buttons.append(types.InlineKeyboardButton(cell, callback_data=f"bot_move|{i}|{j}"))
        keyboard.add(*buttons)

    return bot.send_message(chat_id, "🎮 Игровое поле 5x5:\nВы ходите первым!", reply_markup=keyboard)

@bot.callback_query_handler(func=lambda call: call.data.startswith("bot_move"))
def handle_bot_move(call):
    game = bot_games.get(call.message.chat.id)
    if not game:
        bot.answer_callback_query(call.id, "Игра не найдена!")
        return

    if game["turn"] != "player":
        bot.answer_callback_query(call.id, "Сейчас ход бота!")
        return

    _, x, y = call.data.split("|")
    x, y = int(x), int(y)

    if game["board"][x][y] != "ᅠᅠ":
        bot.answer_callback_query(call.id, "Эта клетка уже занята!")
        return

    game["board"][x][y] = game["player_symbol"]
    game["moves"] += 1

    if check_bot_winner(game["board"], game["player_symbol"]):
        end_bot_game(call.message.chat.id, "Вы победили! 🎉", game["message_id"])
        return

    game["turn"] = "bot"
    update_bot_board(call.message.chat.id, game["board"], game["message_id"])

    bot_move(call.message.chat.id)

def bot_move(chat_id):
    game = bot_games.get(chat_id)
    if not game:
        return

    board = game["board"]
    player = game["player_symbol"]
    bot_symbol = game["bot_symbol"]

    best_move = find_best_move(board, bot_symbol, player)

    if best_move:
        x, y = best_move
        board[x][y] = bot_symbol
        game["moves"] += 1

    if check_bot_winner(board, bot_symbol):
        end_bot_game(chat_id, "Бот победил! 🤖", game["message_id"])
        return

    game["turn"] = "player"
    update_bot_board(chat_id, board, game["message_id"])

def find_best_move(board, bot_symbol, player_symbol):
    """
    Анализирует поле и выбирает лучший ход для бота.
    1. Если есть возможность выиграть — выигрывает.
    2. Если игрок угрожает победой — блокирует его.
    3. Если нет угроз — строит свою комбинацию.
    4. Если ничего не найдено — делает случайный ход.
    """
    empty_cells = [(i, j) for i in range(5) for j in range(5) if board[i][j] == "ᅠᅠ"]

    # 1. Проверка на победный ход
    for x, y in empty_cells:
        board[x][y] = bot_symbol
        if check_bot_winner(board, bot_symbol):
            board[x][y] = "ᅠᅠ"
            return x, y
        board[x][y] = "ᅠᅠ"

    # 2. Проверка на блокировку игрока
    for x, y in empty_cells:
        board[x][y] = player_symbol
        if check_bot_winner(board, player_symbol):
            board[x][y] = "ᅠᅠ"
            return x, y
        board[x][y] = "ᅠᅠ"

    # 3. Попытка построить свою комбинацию (поиск ряды из 2-3 своих символов)
    for x, y in empty_cells:
        if is_good_move(board, x, y, bot_symbol):
            return x, y

    # 4. Если нет хороших ходов, ходит случайно
    return random.choice(empty_cells) if empty_cells else None

def is_good_move(board, x, y, symbol):
    """ Проверяет, является ли ход хорошим (строит комбинацию, приближая к победе). """
    board[x][y] = symbol
    win = check_bot_winner(board, symbol)
    board[x][y] = "ᅠᅠ"
    return win

def update_bot_board(chat_id, board, message_id):
    keyboard = types.InlineKeyboardMarkup(row_width=5)
    for i, row in enumerate(board):
        buttons = []
        for j, cell in enumerate(row):
            buttons.append(types.InlineKeyboardButton(cell, callback_data=f"bot_move|{i}|{j}"))
        keyboard.add(*buttons)

    bot.edit_message_reply_markup(chat_id, message_id, reply_markup=keyboard)

def check_bot_winner(board, symbol):
    """ Проверяет победные комбинации по горизонтали, вертикали и диагоналям. """
    win_length = 4

    # Горизонтали
    for row in board:
        for i in range(2):
            if row[i:i + win_length] == [symbol] * win_length:
                return True

    # Вертикали
    for col in range(5):
        for row in range(2):
            if [board[row + i][col] for i in range(win_length)] == [symbol] * win_length:
                return True

    # Диагонали
    for row in range(2):
        for col in range(2):
            if [board[row + i][col + i] for i in range(win_length)] == [symbol] * win_length:
                return True
            if [board[row + i][col + win_length - 1 - i] for i in range(win_length)] == [symbol] * win_length:
                return True

    return False

def end_bot_game(chat_id, result_text, message_id):
    bot.edit_message_text(result_text, chat_id, message_id)
    bot_games.pop(chat_id, None)

from telebot import types

duels = {}

# Обработчик кнопки для вызова дуэли
@bot.callback_query_handler(func=lambda call: call.data == "kamen_l")
def duel_request(call):
    user_id = call.from_user.id

    # Проверяем, был ли вызов уже создан (проверка на активную дуэль)
    for opponent_id, duel in duels.items():
        if duel['challenger'] == user_id:
            bot.send_message(call.message.chat.id, "❌ Вы уже вызвали другого игрока на дуэль!")
            return

    # Сохраняем запрос на дуэль с текущим пользователем в качестве инициатора
    duels[user_id] = {'challenger': user_id}

    # Кнопка для принятия вызова
    keyboard = types.InlineKeyboardMarkup()
    accept_button = types.InlineKeyboardButton("⚔️ Принять вызов", callback_data=f"accept_duel_{user_id}")
    keyboard.add(accept_button)

    bot.send_message(
        call.message.chat.id,
        f"🎯 <b>{call.from_user.first_name}</b> вызывает кого-то на дуэль! Принять вызов?",
        parse_mode="HTML",
        reply_markup=keyboard
    )

# Обработчик принятия дуэли
@bot.callback_query_handler(func=lambda call: call.data.startswith("accept_duel_"))
def accept_duel(call):
    opponent_id = call.from_user.id
    duel_data = call.data.split("_")
    challenger_id = int(duel_data[2])

    # Проверяем, существует ли этот вызов
    if challenger_id not in duels or duels[challenger_id]['challenger'] != challenger_id:
        bot.answer_callback_query(call.id, "❌ Вы не можете принять вызов", show_alert=True)
        return

    # Удаляем из списка активных дуэлей
    del duels[challenger_id]

    # Кнопки для игры (Камень, ножницы, бумага)
    keyboard = types.InlineKeyboardMarkup()
    options = ["✊", "✌️", "🤚"]
    for option in options:
        keyboard.add(types.InlineKeyboardButton(option, callback_data=f"rps_{challenger_id}_{opponent_id}_{option}"))

    bot.send_message(call.message.chat.id, f"🛡️ {call.from_user.first_name} принял дуэль!\nВыберите свой ход:", reply_markup=keyboard)

rps_choices = {}  # Словарь для хранения ходов участников

@bot.callback_query_handler(func=lambda call: call.data.startswith("rps_"))
def play_rps(call):
    data = call.data.split("_")
    challenger_id = int(data[1])
    opponent_id = int(data[2])
    player_choice = data[3]

    player_id = call.from_user.id

    # Проверяем, сделал ли ход первый игрок
    if (challenger_id, opponent_id) not in rps_choices:
        rps_choices[(challenger_id, opponent_id)] = {player_id: player_choice}
        bot.answer_callback_query(call.id, "Вы сделали выбор! Ожидаем соперника...")
        return

    # Получаем выбор обоих игроков
    rps_choices[(challenger_id, opponent_id)][player_id] = player_choice
    choices = rps_choices.pop((challenger_id, opponent_id))

    # Определяем победителя
    challenger_choice = choices[challenger_id]
    opponent_choice = choices[opponent_id]

    results = {
        ("✊", "✌️"): challenger_id, ("✌️", "🤚"): challenger_id, ("🤚", "✊"): challenger_id,  # Победа вызвавшего
        ("✌️", "✊"): opponent_id, ("🤚", "✌️"): opponent_id, ("✊", "🤚"): opponent_id  # Победа второго игрока
    }

    if challenger_choice == opponent_choice:
        winner_id = None
    else:
        winner_id = results.get((challenger_choice, opponent_choice))

    if winner_id:
        loser_id = challenger_id if winner_id == opponent_id else opponent_id

        # Победитель получит похвалу (или GRAM, если хотите)
        bot.send_message(call.message.chat.id,
            f"⚔️ Дуэль завершена!\n\n"
            f"🎭 {call.from_user.first_name} выбрал: {player_choice}\n"
            f"🏆 Победитель: {bot.get_chat_member(call.message.chat.id, winner_id).user.first_name}\n\n"
        )
    else:
        bot.send_message(call.message.chat.id, "⚔️ Дуэль завершилась ничьей!")

@bot.message_handler(commands=["рынок"])
def upload_item(message):
    user_id = message.from_user.id
    try:
        _, skin, price = message.text.split(" ")
        price = int(price)

        if price <= 0:
            bot.reply_to(message, "Цена должна быть больше 0.")
            return

        # Проверяем, есть ли этот скин у пользователя
        cursor.execute(
            "SELECT quantity FROM user_skins WHERE user_id = ? AND skin = ?",
            (user_id, skin),
        )
        result = cursor.fetchone()

        if not result or result[0] <= 0:
            bot.reply_to(message, "У вас нет этого товара для продажи!")
            return

        # Отнимаем 1 штуку скина из инвентаря пользователя
        cursor.execute(
            "UPDATE user_skins SET quantity = quantity - 1 WHERE user_id = ? AND skin = ?",
            (user_id, skin),
        )
        cursor.execute("DELETE FROM user_skins WHERE quantity <= 0")

        # Добавляем товар на рынок
        cursor.execute(
            "INSERT INTO market (seller_id, skin, price) VALUES (?, ?, ?)",
            (user_id, skin, price),
        )
        conn.commit()

        bot.reply_to(message, f"✅ Вы успешно выставили {skin} за {price} GRAM на рынок!")

    except ValueError:
        bot.reply_to(message, "Используйте формат: `/рынок скин 100`", parse_mode="Markdown")


market_messages = {}  # Хранение сообщений для удаления
user_clicks = {}  # Отслеживание двойных кликов

def send_market(message, user_id, page=1, edit_msg_id=None):
    offset = (page - 1) * 10
    cursor.execute("SELECT skin, COUNT(skin) FROM market GROUP BY skin")
    skin_counts = cursor.fetchall()  # Получаем список скинов и их количества

    if not skin_counts:
        bot.send_message(message.chat.id, "Рынок пуст! Вы можете загрузить товар с помощью /рынок.")
        return

    # Формируем строку с доступными скинами и их количеством
    skin_display = "На продаже: " + " ".join([f"{skin}x{count}" for skin, count in skin_counts])

    cursor.execute("SELECT id, skin, price, seller_id FROM market LIMIT 20 OFFSET ?", (offset,))
    items = cursor.fetchall()

    keyboard = types.InlineKeyboardMarkup(row_width=4)
    buttons = []

    for item_id, skin, price, seller_id in items:
        buttons.append(
            types.InlineKeyboardButton(
                f"{skin} {price}",
                callback_data=f"buy_market_{item_id}_{seller_id}"
            )
        )

    # Добавление кнопок в 4 столбца
    for i in range(0, len(buttons), 4):
        keyboard.row(*buttons[i:i+4])

    # Навигация
    nav_buttons = []
    if page > 1:
        nav_buttons.append(types.InlineKeyboardButton("⬅️ Назад", callback_data=f"market_page_{page-1}"))
    
    nav_buttons.append(types.InlineKeyboardButton(f"Стр. {page}", callback_data="none"))

    cursor.execute("SELECT COUNT(*) FROM market")
    total_items = cursor.fetchone()[0]

    if total_items > offset + 20:
        nav_buttons.append(types.InlineKeyboardButton("➡️ Вперёд", callback_data=f"market_page_{page+1}"))

    if nav_buttons:
        keyboard.row(*nav_buttons)

    # Финальный текст рынка
    market_text = f"🛒 #Рынок товаров:\n{skin_display}"

    if edit_msg_id:
        bot.edit_message_text(market_text, message.chat.id, edit_msg_id, reply_markup=keyboard)
        market_messages[user_id] = edit_msg_id
    else:
        msg = bot.send_message(message.chat.id, market_text, reply_markup=keyboard)
        market_messages[user_id] = msg.message_id

@bot.message_handler(func=lambda message: message.text == "Рынок")
def market(message):
    send_market(message, message.from_user.id, page=1)


@bot.callback_query_handler(func=lambda call: call.data.startswith("market_page_"))
def paginate_market(call):
    page = int(call.data.split("_")[-1])
    send_market(call.message, call.from_user.id, page, edit_msg_id=market_messages.get(call.from_user.id))


@bot.callback_query_handler(func=lambda call: call.data.startswith("buy_market_"))
def buy_market_item(call):
    user_id = call.from_user.id
    item_data = call.data.split("_")
    item_id, seller_id = int(item_data[2]), int(item_data[3])

    if user_id == seller_id:
        # Проверяем, нажал ли продавец два раза подряд
        if user_clicks.get(user_id) == item_id:
            cursor.execute("SELECT skin, price FROM market WHERE id = ?", (item_id,))
            item = cursor.fetchone()
            
            if item:
                skin, price = item
                
                # Возвращаем товар владельцу
                cursor.execute("DELETE FROM market WHERE id = ?", (item_id,))
                cursor.execute(
                    '''INSERT INTO user_skins (user_id, skin, quantity)
                       VALUES (?, ?, 1) ON CONFLICT(user_id, skin) DO UPDATE SET quantity = quantity + 1''',
                    (user_id, skin)
                )
                conn.commit()

                bot.answer_callback_query(call.id, f"❌ Вы сняли {skin} с продажи.")
                send_market(call.message, user_id, page=1, edit_msg_id=market_messages.get(user_id))
            return
        
        # Первый клик по своему товару
        user_clicks[user_id] = item_id
        bot.answer_callback_query(call.id, "Нажмите еще раз, чтобы снять товар с продажи.")
        return

    # Покупка товара
    cursor.execute("SELECT skin, price FROM market WHERE id = ?", (item_id,))
    item = cursor.fetchone()

    if not item:
        bot.answer_callback_query(call.id, "Этот товар уже продан.")
        return

    skin, price = item

    # Проверяем баланс покупателя
    cursor.execute("SELECT gram FROM users WHERE user_id = ?", (user_id,))
    buyer_data = cursor.fetchone()

    if not buyer_data or buyer_data[0] < price:
        bot.answer_callback_query(call.id, "У вас недостаточно GRAM для покупки.")
        return

    # Списание денег и передача скина
    cursor.execute("UPDATE users SET gram = gram - ? WHERE user_id = ?", (price, user_id))
    cursor.execute("UPDATE users SET gram = gram + ? WHERE user_id = ?", (price, seller_id))
    cursor.execute(
        '''INSERT INTO user_skins (user_id, skin, quantity)
           VALUES (?, ?, 1) ON CONFLICT(user_id, skin) DO UPDATE SET quantity = quantity + 1''',
        (user_id, skin)
    )

    # Удаление товара с рынка
    cursor.execute("DELETE FROM market WHERE id = ?", (item_id,))
    conn.commit()

    bot.answer_callback_query(call.id, f"Вы купили {skin} за {price} GRAM!")
    bot.send_message(call.message.chat.id, f"✅ Вы успешно приобрели {skin}. GRAM переведены продавцу.")
    
    # Обновляем рынок после покупки
    send_market(call.message, user_id, page=1, edit_msg_id=market_messages.get(user_id))

import time
import threading

def track_gram_changes():
    while True:
        cursor.execute('''
        SELECT gc.user_id, u.username, gc.old_gram, gc.new_gram, gc.change_time
        FROM gram_changes gc
        JOIN users u ON gc.user_id = u.user_id
        ORDER BY gc.change_time ASC
        ''')
        changes = cursor.fetchall()

        for change in changes:
            user_id, username, old_gram, new_gram, change_time = change
            difference = new_gram - old_gram
            message = (f"📢 Изменение баланса граммов!\n"
                       f"👤 Пользователь: @{username} (ID: {user_id})\n"
                       f"💰 Было: {old_gram} GRAM\n"
                       f"💵 Стало: {new_gram} GRAM\n"
                       f"📈 Изменение: {'+' if difference > 0 else ''}{difference} GRAM\n"
                       f"🕒 Время: {change_time}")
            
            try:
                bot.send_message(ADMIN_ID, message)
            except Exception as e:
                print(f"Ошибка отправки уведомления админу: {e}")

            # Удаляем запись после отправки уведомления
            cursor.execute("DELETE FROM gram_changes WHERE user_id = ?", (user_id,))
            conn.commit()

        time.sleep(5)  # Проверка каждые 5 секунд

# Запускаем отслеживание в фоновом потоке
threading.Thread(target=track_gram_changes, daemon=True).start()

@bot.message_handler(commands=["me"])
def user_market(message):
    user_id = message.from_user.id
    send_user_market(message, user_id)

def send_user_market(message, user_id, page=1, edit_msg_id=None):
    offset = (page - 1) * 10
    
    # Получаем все товары, выставленные этим пользователем
    cursor.execute(
        "SELECT id, skin, price FROM market WHERE seller_id = ? LIMIT 20 OFFSET ?",
        (user_id, offset)
    )
    items = cursor.fetchall()

    if not items:
        bot.send_message(message.chat.id, "У вас нет товаров на продаже!")
        return

    # Получаем имя пользователя
    username = message.from_user.username or f"ID: {user_id}"

    # Формируем список скинов
    skin_display = f"🛒 Товары от @{username}:\n"
    for item_id, skin, price in items:
        skin_display += f"🔹 {skin} — {price} GRAM\n"

    keyboard = types.InlineKeyboardMarkup(row_width=4)
    buttons = []

    for item_id, skin, price in items:
        buttons.append(
            types.InlineKeyboardButton(
                f"{skin} {price}💰",
                callback_data=f"buy_market_{item_id}_{user_id}"
            )
        )

    # Добавление кнопок в 4 столбца
    for i in range(0, len(buttons), 4):
        keyboard.row(*buttons[i:i+4])

    # Навигация
    nav_buttons = []
    if page > 1:
        nav_buttons.append(types.InlineKeyboardButton("⬅️ Назад", callback_data=f"user_market_{user_id}_{page-1}"))
    
    nav_buttons.append(types.InlineKeyboardButton(f"Стр. {page}", callback_data="none"))

    cursor.execute("SELECT COUNT(*) FROM market WHERE seller_id = ?", (user_id,))
    total_items = cursor.fetchone()[0]

    if total_items > offset + 20:
        nav_buttons.append(types.InlineKeyboardButton("➡️ Вперёд", callback_data=f"user_market_{user_id}_{page+1}"))

    if nav_buttons:
        keyboard.row(*nav_buttons)

    # Отправляем сообщение
    if edit_msg_id:
        bot.edit_message_text(skin_display, message.chat.id, edit_msg_id, reply_markup=keyboard)
    else:
        bot.send_message(message.chat.id, skin_display, reply_markup=keyboard)


@bot.callback_query_handler(func=lambda call: call.data.startswith("user_market_"))
def paginate_user_market(call):
    _, user_id, page = call.data.split("_")
    send_user_market(call.message, int(user_id), int(page), edit_msg_id=call.message.message_id)

@bot.message_handler(commands=["сс"])
def clear_market(message):
    user_id = message.from_user.id

    # Проверка, является ли пользователь админом (можно заменить на свой ID)
    admin_id = 6392028972  # Замените на свой Telegram ID
    if user_id != admin_id:
        bot.reply_to(message, "❌ У вас нет прав для очистки рынка!")
        return

    # Очищаем таблицу рынка
    cursor.execute("DELETE FROM market")
    conn.commit()

    bot.reply_to(message, "✅ Все товары на рынке удалены!")

@bot.message_handler(commands=["пж"])
def request_donation(message):
    recipient_id = message.from_user.id
    recipient_name = message.from_user.first_name

    command_parts = message.text.split()
    if len(command_parts) < 2:
        bot.send_message(message.chat.id, "Введите сумму или скин после команды! Например: /пж 500 или /пж 🌟")
        return

    request_item = command_parts[1]

    if request_item.isdigit():
        amount = int(request_item)
        keyboard = types.InlineKeyboardMarkup()
        button_donate = types.InlineKeyboardButton(
            f"💎 Пожертвовать {amount} GRAM", callback_data=f"donate|{recipient_id}|{amount}"
        )
        keyboard.add(button_donate)
        bot.send_message(
            message.chat.id,
            f"🎁 {recipient_name} запросил {amount} GRAM! Кто хочет помочь? Нажмите кнопку ниже.",
            reply_markup=keyboard
        )
    else:
        # Проверяем, существует ли запрошенный скин в базе
        cursor.execute("SELECT COUNT(*) FROM user_skins WHERE skin = ?", (request_item,))
        if cursor.fetchone()[0] == 0:
            bot.send_message(message.chat.id, "❌ Такого скина нет в магазине!")
            return

        keyboard = types.InlineKeyboardMarkup()
        button_donate_skin = types.InlineKeyboardButton(
            f"🎁 Подарить {request_item}", callback_data=f"donate_skin|{recipient_id}|{request_item}"
        )
        keyboard.add(button_donate_skin)
        bot.send_message(
            message.chat.id,
            f"🎁 {recipient_name} хочет получить скин {request_item}! Кто готов подарить?",
            reply_markup=keyboard
        )

@bot.callback_query_handler(func=lambda call: call.data.startswith("donate_skin"))
def process_skin_donation(call):
    _, recipient_id, skin = call.data.split("|")
    donor_id = call.from_user.id
    recipient_id = int(recipient_id)

    # Проверяем, есть ли у донора такой скин
    cursor.execute("SELECT quantity FROM user_skins WHERE user_id = ? AND skin = ?", (donor_id, skin))
    donor_skins = cursor.fetchone()

    if not donor_skins or donor_skins[0] < 1:
        bot.answer_callback_query(call.id, "❌ У вас нет этого скина для передачи!")
        return

    # Убираем скин у донора
    cursor.execute("UPDATE user_skins SET quantity = quantity - 1 WHERE user_id = ? AND skin = ?", (donor_id, skin))
    # Добавляем скин получателю
    cursor.execute('''
        INSERT INTO user_skins (user_id, skin, quantity)
        VALUES (?, ?, 1)
        ON CONFLICT(user_id, skin) DO UPDATE SET quantity = quantity + 1
    ''', (recipient_id, skin))
    conn.commit()

    bot.send_message(recipient_id, f"🎉 Вам подарили скин {skin} от {call.from_user.first_name}! 🎁")
    bot.send_message(donor_id, f"🙏 Вы передали скин {skin} пользователю {recipient_id}. Спасибо за щедрость!")

    bot.answer_callback_query(call.id, "✅ Скин успешно передан!")

from telebot.types import LabeledPrice, InlineKeyboardMarkup, InlineKeyboardButton

@bot.message_handler(func=lambda message: message.text == "💳 Донат")
def donate_message(message):
    markup = InlineKeyboardMarkup()
    markup.add(InlineKeyboardButton("1⭐ = 6,000 GRAM", callback_data="donate_1"))
    markup.add(InlineKeyboardButton("2⭐ = 12,000 GRAM", callback_data="donate_2"))
    markup.add(InlineKeyboardButton("4⭐ = 24,000 GRAM", callback_data="donate_4"))
    markup.add(InlineKeyboardButton("8⭐ = 50,000 GRAM", callback_data="donate_8"))
    markup.add(InlineKeyboardButton("3⭐ = 1🅾️ зерокоин", callback_data="buy_zerocoin_1"))
    markup.add(InlineKeyboardButton("9⭐ = 3🅾️ зерокоина", callback_data="buy_zerocoin_3"))

    bot.send_message(message.chat.id, "Выберите сумму доната:", reply_markup=markup)


@bot.callback_query_handler(func=lambda call: call.data.startswith("donate_") or call.data.startswith("buy_zerocoin_"))
def process_donation(call):
    if call.data.startswith("donate_"):
        stars_map = {
            "donate_1": (1, 6000),
            "donate_2": (2, 12000),
            "donate_4": (4, 24000),
            "donate_8": (8, 50000),
        }
        stars, amount = stars_map[call.data]
        description = f"{stars}⭐ = {amount} GRAM"
        payload = f"donation_{stars}_{call.from_user.id}"

    elif call.data.startswith("buy_zerocoin_"):
        zerocoin_map = {
            "buy_zerocoin_1": (3, 1),
            "buy_zerocoin_3": (9, 3),
        }
        stars, zerocoins = zerocoin_map[call.data]
        description = f"{stars}⭐ = {zerocoins}🅾️ зерокоин(ов)"
        payload = f"zerocoin_{zerocoins}_{call.from_user.id}"

    prices = [LabeledPrice(label=f"Покупка за {stars}⭐", amount=stars * 1)]  # 100 = 1 XTR (пример)

    bot.send_invoice(
        chat_id=call.message.chat.id,
        title=f"Покупка за {stars}⭐",
        description=description,
        provider_token=PROVIDER_TOKEN,
        currency=CURRENCY,
        prices=prices,
        start_parameter=payload,
        invoice_payload=payload,
    )


@bot.message_handler(content_types=["successful_payment"])
def got_payment(message):
    user_id = message.from_user.id
    payload = message.successful_payment.invoice_payload

    if payload.startswith("donation_"):
        stars = int(payload.split("_")[1])
        gram_amount = {1: 6000, 2: 12000, 4: 24000, 8: 50000}[stars]

        cursor.execute("UPDATE users SET gram = gram + ? WHERE user_id = ?", (gram_amount, user_id))
        conn.commit()
        bot.send_message(message.chat.id, f"✅ Вам начислено {gram_amount} GRAM 💎")

    elif payload.startswith("zerocoin_"):
        zerocoins = int(payload.split("_")[1])

        cursor.execute("UPDATE users SET zerocoin = zerocoin + ? WHERE user_id = ?", (zerocoins, user_id))
        conn.commit()
        bot.send_message(message.chat.id, f"✅ Вам начислено {zerocoins}🅾️ зерокоина!")
            
from telebot import types

# Скины и их категории
skins = {
    "Транспорт": [("🚔", 12), ("🚙", 7), ("✈️", 7), ("🛸", 6)],
    "Животные": [("🦖", 6), ("🐉", 6), ("🐖", 5), ("🐑", 5), ("🐍", 8), ("🐊", 10), ("🦕", 7), ("🐲", 7)],
    "Техника": [("⛽", 17), ("🧭", 14), ("🧰", 8), ("📺", 7)],
    "Черепа и мистические скины": [("💀", 8), ("👽", 7), ("🙀", 7), ("❄️", 7), ("🌷", 7), ("👀", 3), ("🌌", 3)],
}

CATEGORIES_PER_PAGE = 2  # Количество категорий на странице

# Обработчик кнопки "Магазин скинов"
@bot.message_handler(func=lambda message: message.text == "🏪 Магазин скинов")
def skin_shop(message, page=0):
    total_pages = (len(skins) - 1) // CATEGORIES_PER_PAGE + 1
    categories = list(skins.keys())[page * CATEGORIES_PER_PAGE : (page + 1) * CATEGORIES_PER_PAGE]

    keyboard = types.InlineKeyboardMarkup()

    # Добавляем категории и скины
    for category in categories:
        keyboard.add(types.InlineKeyboardButton(f"🔹 {category} 🔹", callback_data="none"))
        for skin, price in skins[category]:
            button = types.InlineKeyboardButton(f"{skin} {price} 🌟", callback_data=f"pay_skin_{skin}_{price}")
            keyboard.add(button)

    nav_buttons = []
    if page > 0:
        nav_buttons.append(types.InlineKeyboardButton("⬅️", callback_data=f"shop_page_{page-1}"))
    if page < total_pages - 1:
        nav_buttons.append(types.InlineKeyboardButton("➡️", callback_data=f"shop_page_{page+1}"))

    if nav_buttons:
        keyboard.add(*nav_buttons)

    bot.send_message(message.chat.id, f"Выберите товар для покупки (Страница {page+1}/{total_pages}):", reply_markup=keyboard)

# Обработчик кнопок для переключения страниц
@bot.callback_query_handler(func=lambda call: call.data.startswith("shop_page_"))
def change_page(call):
    page = int(call.data.split("_")[2])
    bot.delete_message(call.message.chat.id, call.message.message_id)
    skin_shop(call.message, page)

# Обработчик покупки товаров
@bot.callback_query_handler(func=lambda call: call.data.startswith("pay_skin_"))
def pay_skin(call):
    user_id = call.from_user.id
    _, _, skin, price = call.data.split("_")
    price = int(price)

    prices = [types.LabeledPrice(label=f"Покупка {skin}", amount=price * 1)]  # Цена в минимальных единицах валюты

    bot.send_invoice(
        chat_id=call.message.chat.id,
        title="Покупка",
        description=f"После оплаты вам начислится {skin}.",
        provider_token=PROVIDER_TOKEN,
        currency=CURRENCY,
        prices=prices,
        start_parameter="buy_skin",
        invoice_payload=f"buy_skin_{skin}_{user_id}",
    )

# Подтверждение платежа
@bot.pre_checkout_query_handler(func=lambda query: True)
def checkout(pre_checkout_query):
    bot.answer_pre_checkout_query(pre_checkout_query.id, ok=True)

# Обработчик успешного платежа
@bot.message_handler(content_types=["successful_payment"])
def got_payment(message):
    user_id = message.from_user.id
    payload = message.successful_payment.invoice_payload

    if payload.startswith("buy_skin_"):
        item = payload.split("_")[2]

        cursor.execute('''    
            INSERT INTO user_skins (user_id, skin, quantity)    
            VALUES (?, ?, 1)    
            ON CONFLICT(user_id, skin) DO UPDATE SET quantity = quantity + 1    
        ''', (user_id, item))    

        conn.commit()    
        bot.send_message(message.chat.id, f"✅ Вам начислено: {item}!")

@bot.message_handler(commands=["zerocoin"])
def transfer_zerocoin(message):
    user_id = message.from_user.id
    args = message.text.split()

    # Проверяем, указал ли пользователь количество зерокоинов
    if len(args) < 2 or not args[1].isdigit():
        bot.reply_to(message, "❌ Использование: /zerocoin [количество]\nПример: `/zerocoin 2`", parse_mode="Markdown")
        return

    amount = int(args[1])

    # Проверяем, есть ли ответ на сообщение
    if not message.reply_to_message:
        bot.reply_to(message, "❌ Вы должны ответить на сообщение пользователя, которому хотите передать зерокоины!")
        return

    target_id = message.reply_to_message.from_user.id

    # Проверяем баланс отправителя
    cursor.execute("SELECT zerocoin FROM users WHERE user_id = ?", (user_id,))
    sender_balance = cursor.fetchone()

    if not sender_balance or sender_balance[0] < amount:
        bot.reply_to(message, "❌ У вас недостаточно 🅾️ зерокоинов для передачи!")
        return

    # Списываем зерокоины у отправителя и зачисляем получателю
    cursor.execute("UPDATE users SET zerocoin = zerocoin - ? WHERE user_id = ?", (amount, user_id))
    cursor.execute("UPDATE users SET zerocoin = zerocoin + ? WHERE user_id = ?", (amount, target_id))
    conn.commit()

    bot.reply_to(message.reply_to_message, f"🎉 Вам передано {amount} 🅾️ зерокоина(ов) от {message.from_user.first_name}!")
    bot.send_message(message.chat.id, f"✅ {message.from_user.first_name} передал {amount} 🅾️ зерокоина {message.reply_to_message.from_user.first_name}!")

from telebot.types import LabeledPrice, InlineKeyboardMarkup, InlineKeyboardButton

# Функция для обработки
@bot.message_handler(func=lambda message: message.text == "🏆 купить VIP")
def donate_message(message):
    markup = InlineKeyboardMarkup()
    markup.row_width = 2
    markup.add(InlineKeyboardButton("VIP на 1 неделю за 2 🌟", callback_data="vip_1_week"),
               InlineKeyboardButton("VIP на 1 месяц за 5 🌟", callback_data="vip_1_month"),
               InlineKeyboardButton("VIP на 3 месяца за 14 🌟", callback_data="vip_3_months"),
               InlineKeyboardButton("VIP навсегда за 25 🌟", callback_data="vip_forever"))
    bot.send_message(
        message.chat.id,
        "Выберите VIP 🏆 статус после оплаты ждите подтверждения админа @basir_l2 \n"
        "все плюсы VIP https://telegra.ph/FUNKCIYA-VIP--polzovatelej-02-26",
        reply_markup=markup
    )

# Обработка callback запросов от кнопок
@bot.callback_query_handler(func=lambda call: call.data.startswith("vip_"))
def callback_query(call):
    user_id = call.from_user.id
    if call.data == "vip_1_week":
        prices = [LabeledPrice(label="VIP на 1 неделю", amount=2)]  # 200 = 2 🌟
        bot.send_invoice(call.message.chat.id, title="VIP на 1 неделю",
                         description="VIP статус на 1 неделю", provider_token=PROVIDER_TOKEN,
                         currency=CURRENCY, prices=prices, start_parameter="vip_1_week",
                         invoice_payload=f"vip_1_week_{user_id}")
    elif call.data == "vip_1_month":
        prices = [LabeledPrice(label="VIP на 1 месяц", amount=5)]  # 500 = 5 🌟
        bot.send_invoice(call.message.chat.id, title="VIP на 1 месяц",
                         description="VIP статус на 1 месяц", provider_token=PROVIDER_TOKEN,
                         currency=CURRENCY, prices=prices, start_parameter="vip_1_month",
                         invoice_payload=f"vip_1_month_{user_id}")
    elif call.data == "vip_3_months":
        prices = [LabeledPrice(label="VIP на 3 месяца", amount=14)]  # 1400 = 14 🌟
        bot.send_invoice(call.message.chat.id, title="VIP на 3 месяца",
                         description="VIP статус на 3 месяца", provider_token=PROVIDER_TOKEN,
                         currency=CURRENCY, prices=prices, start_parameter="vip_3_months",
                         invoice_payload=f"vip_3_months_{user_id}")
    elif call.data == "vip_forever":
        prices = [LabeledPrice(label="VIP навсегда", amount=25)]  # 2500 = 25 🌟
        bot.send_invoice(call.message.chat.id, title="VIP навсегда",
                         description="VIP статус навсегда", provider_token=PROVIDER_TOKEN,
                         currency=CURRENCY, prices=prices, start_parameter="vip_forever",
                         invoice_payload=f"vip_forever_{user_id}")

# Обработка успешного платежа
@bot.message_handler(content_types=["successful_payment"])
def got_payment(message):
    user_id = message.from_user.id
    payload = message.successful_payment.invoice_payload

    if payload.startswith("vip_1_week_"):
        cursor.execute("UPDATE users SET vip_level = 0, vip_expiry = DATE('now', '+7 days') WHERE user_id = ?", (user_id,))
        conn.commit()
        bot.send_message(message.chat.id, "✅ Вам выдан VIP статус на 1 неделю!")
    elif payload.startswith("vip_1_month_"):
        cursor.execute("UPDATE users SET vip_level = 1, vip_expiry = DATE('now', '+1 month') WHERE user_id = ?", (user_id,))
        conn.commit()
        bot.send_message(message.chat.id, "✅ Вам выдан VIP статус на 1 месяц!")
    elif payload.startswith("vip_3_months_"):
        cursor.execute("UPDATE users SET vip_level = 2, vip_expiry = DATE('now', '+3 months') WHERE user_id = ?", (user_id,))
        conn.commit()
        bot.send_message(message.chat.id, "✅ Вам выдан VIP статус на 3 месяца!")
    elif payload.startswith("vip_forever_"):
        cursor.execute("UPDATE users SET vip_level = 3, vip_expiry = NULL WHERE user_id = ?", (user_id,))
        conn.commit()
        bot.send_message(message.chat.id, "✅ Вам выдан VIP статус навсегда!")

# Команда для сброса питомцев
@bot.message_handler(commands=['pt_z'])
def reset_pets(message):
    user_id = message.from_user.id

    # Сброс данных питомцев пользователя
    cursor.execute("UPDATE pets SET pet_count = 0, pet_photo = NULL, last_collect = NULL WHERE user_id = ?", (user_id,))
    conn.commit()

    bot.send_message(message.chat.id, "⚠️ Ваши питомцы были сброшены. Все данные о питомцах удалены.")

import os
import datetime

# Получаем список всех колонок таблицы 'pets'
cursor.execute("PRAGMA table_info(pets);")
columns = [column[1] for column in cursor.fetchall()]

# Проверяем, есть ли колонка 'pet_photo'
if "pet_photo" not in columns:
    cursor.execute("ALTER TABLE pets ADD COLUMN pet_photo TEXT DEFAULT NULL")

conn.commit()

# Команда /пт - Покупка питомца и информация о питомцах
@bot.message_handler(commands=['pt'])
def pets_info(message):
    user_id = message.from_user.id

    cursor.execute("SELECT pet_count, pet_photo, pet_pats FROM pets WHERE user_id = ?", (user_id,))
    pet_data = cursor.fetchone()
    
    pet_count = pet_data[0] if pet_data else 0
    daily_income = pet_count * 200
    pet_pats = pet_data[2] if pet_data else 0  # Количество поглаживаний
    pet_photo = pet_data[1] if pet_data and pet_data[1] else None

    # Увеличиваем доход с учетом поглаживаний
    daily_income += pet_pats * 10  # Каждый пат увеличивает доход на 10 GRAM

    text = f"🐶 У вас {pet_count} питомцев.\n💰 Они дают {daily_income} GRAM в день.\n👋 Питомец был поглажен {pet_pats} раз."

    keyboard = types.InlineKeyboardMarkup()
    buy_button = types.InlineKeyboardButton("Купить питомца (6000 GRAM)", callback_data="buy_pet")
    upload_photo_button = types.InlineKeyboardButton("📷 Загрузить фото", callback_data="upload_pet_photo")
    pat_button = types.InlineKeyboardButton(f"👋 Погладить питомца ({pet_pats})", callback_data="pat_pet")  # Кнопка для поглаживания
    keyboard.add(buy_button, upload_photo_button, pat_button)

    if pet_photo:
        bot.send_photo(message.chat.id, pet_photo, caption=text, reply_markup=keyboard)
    else:
        bot.send_message(message.chat.id, text, reply_markup=keyboard)

# Обработка нажатия кнопки "Погладить питомца"
@bot.callback_query_handler(func=lambda call: call.data == "pat_pet")
def pat_pet(call):
    user_id = call.from_user.id

    cursor.execute("SELECT pet_pats, last_pat FROM pets WHERE user_id = ?", (user_id,))
    pet_data = cursor.fetchone()

    if not pet_data:
        bot.answer_callback_query(call.id, "У вас нет питомцев!")
        return

    pet_pats, last_pat = pet_data

    # Получаем текущую дату
    today = datetime.date.today().isoformat()

    # Проверяем, не гладил ли пользователь питомца сегодня
    if last_pat == today:
        bot.answer_callback_query(call.id, "Вы уже погладили питомца сегодня! Попробуйте завтра.")
        return

    # Увеличиваем количество поглаживаний на 1
    cursor.execute("UPDATE pets SET pet_pats = pet_pats + 1, last_pat = ? WHERE user_id = ?", (today, user_id))
    conn.commit()

    # Увеличиваем доход с учетом нового количества поглаживаний
    daily_income = (pet_pats + 1) * 10
    cursor.execute("UPDATE users SET gram = gram + ? WHERE user_id = ?", (daily_income, user_id))
    conn.commit()

    bot.answer_callback_query(call.id, "Вы погладили питомца! 🐾")
    bot.send_message(call.message.chat.id, f"Теперь ваш питомец принесет на {pet_pats + 1} GRAM больше в день!")

# Обработка нажатия кнопки "Купить питомца"
@bot.callback_query_handler(func=lambda call: call.data == "buy_pet")
def buy_pet(call):
    user_id = call.from_user.id

    cursor.execute("SELECT gram FROM users WHERE user_id = ?", (user_id,))
    user_data = cursor.fetchone()

    if not user_data or user_data[0] < 6000:
        bot.answer_callback_query(call.id, "У вас недостаточно GRAM!")
        return

    cursor.execute("UPDATE users SET gram = gram - 6000 WHERE user_id = ?", (user_id,))
    cursor.execute("INSERT INTO pets (user_id, pet_count, last_collect) VALUES (?, 1, ?) ON CONFLICT(user_id) DO UPDATE SET pet_count = pet_count + 1",
                   (user_id, datetime.date.today().isoformat()))
    conn.commit()

    bot.answer_callback_query(call.id, "Вы купили питомца! 🐶")
    bot.send_message(call.message.chat.id, "🎉 Новый питомец добавлен! за 6,000 Теперь он приносит 200 GRAM в день.")

# Обработка нажатия кнопки "Загрузить фото питомца"
@bot.callback_query_handler(func=lambda call: call.data == "upload_pet_photo")
def request_pet_photo(call):
    bot.answer_callback_query(call.id)
    bot.send_message(call.message.chat.id, "📸 Отправьте фото вашего питомца.")

    bot.register_next_step_handler(call.message, save_pet_photo)

# Сохранение фото питомца
def save_pet_photo(message):
    if not message.photo:
        bot.send_message(message.chat.id, "❌ Это не фото! Отправьте изображение.")
        return

    user_id = message.from_user.id
    file_id = message.photo[-1].file_id  # Берем самое большое фото

    cursor.execute("UPDATE pets SET pet_photo = ? WHERE user_id = ?", (file_id, user_id))
    conn.commit()

    bot.send_message(message.chat.id, "✅ Фото питомца сохранено!")
    bot.send_photo(message.chat.id, file_id, caption="Вот ваш питомец!")

# Команда /сбор - Сбор заработанных GRAM с питомцев
@bot.message_handler(commands=['сбор'])
def collect_grams(message):
    user_id = message.from_user.id
    today = datetime.date.today().isoformat()

    # Проверяем количество питомцев
    cursor.execute("SELECT pet_count, last_collect FROM pets WHERE user_id = ?", (user_id,))
    pet_data = cursor.fetchone()

    if not pet_data or pet_data[0] == 0:
        bot.send_message(message.chat.id, "У вас нет питомцев! Купите хотя бы одного с помощью команды /пт.")
        return

    pet_count, last_collect = pet_data

    # Проверяем, собирал ли пользователь GRAM сегодня
    if last_collect == today:
        bot.send_message(message.chat.id, "Вы уже собирали GRAM сегодня! Попробуйте завтра.")
        return

    # Вычисляем доход и добавляем его пользователю
    daily_income = pet_count * 200
    cursor.execute("UPDATE users SET gram = gram + ? WHERE user_id = ?", (daily_income, user_id))
    cursor.execute("UPDATE pets SET last_collect = ? WHERE user_id = ?", (today, user_id))
    conn.commit()

    bot.send_message(message.chat.id, f"💰 Вы собрали {daily_income} GRAM с питомцев!")

# Создание таблицы для объявлений (если не существует)
cursor.execute('''
CREATE TABLE IF NOT EXISTS announcements (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    username TEXT,
    text TEXT,
    time_added INTEGER
)
''')
conn.commit()

@bot.message_handler(content_types=["photo", "video", "document", "audio", "video_note", "voice"])
def send_media_to_admin(message):
    user_id = message.from_user.id
    chat_type = message.chat.type  # Проверяем, где отправлено сообщение

    if chat_type != "private":  # Функция работает только в ЛС бота
        return

    username = message.from_user.username or f"ID: {user_id}"

    # Указываем admin_id
    admin_id = 6392028972

    # Если сообщение содержит фото
    if message.photo:
        media = message.photo[-1].file_id  # Получаем ID фото
        caption = f"📸 Новый скриншот от @{username}"
        bot.send_photo(admin_id, media, caption=caption)

    # Если сообщение содержит видео
    elif message.video:
        media = message.video.file_id  # Получаем ID видео
        caption = f"🎥 Новое видео от @{username}"
        bot.send_video(admin_id, media, caption=caption)

    # Если сообщение содержит документ
    elif message.document:
        media = message.document.file_id  # Получаем ID документа
        caption = f"📄 Новый документ от @{username}"
        bot.send_document(admin_id, media, caption=caption)

    # Если сообщение содержит аудио
    elif message.audio:
        media = message.audio.file_id  # Получаем ID аудио
        caption = f"🎶 Новое аудио от @{username}"
        bot.send_audio(admin_id, media, caption=caption)

    # Если сообщение содержит голосовое сообщение
    elif message.voice:
        media = message.voice.file_id  # Получаем ID голосового сообщения
        caption = f"🎤 Новое голосовое сообщение от @{username}"
        bot.send_voice(admin_id, media, caption=caption)

    # Если сообщение содержит видеозапись
    elif message.video_note:
        media = message.video_note.file_id  # Получаем ID видеозаписи
        caption = f"🎬 Новая видеозапись от @{username}"
        bot.send_video_note(admin_id, media, caption=caption)

    # Сообщаем пользователю, что файл отправлен
    bot.send_message(message.chat.id, "✅")

# Обработчик для кнопки "Добавить объявление"
@bot.message_handler(func=lambda message: message.text == "📢 Объявление")
def ask_for_announcement_text(message):
    # Отправляем пользователю сообщение с просьбой ввести текст объявления
    bot.send_message(message.chat.id, "Пожалуйста, введите текст вашего объявления.")

    # Ожидаем ответ от пользователя
    bot.register_next_step_handler(message, save_announcement)

# Функция для сохранения объявления в базе данных
def save_announcement(message):
    text = message.text.strip()
    user_id = message.from_user.id
    username = message.from_user.username
    time_added = int(datetime.datetime.now().timestamp())  # Получаем текущий timestamp

    # Сохраняем объявление в базу данных
    cursor.execute('''
        INSERT INTO announcements (user_id, username, text, time_added)
        VALUES (?, ?, ?, ?)
    ''', (user_id, username, text, time_added))
    conn.commit()

    # Отправляем пользователю сообщение о том, что объявление добавлено
    bot.send_message(message.chat.id, "✅ Ваше объявление добавлено!")

# Обработчик для команды /доска (выводит объявления)
@bot.message_handler(commands=["doska"])
def show_announcements(message):
    # Получаем все объявления из базы данных
    cursor.execute("SELECT username, time_added, text FROM announcements ORDER BY time_added DESC")
    announcements = cursor.fetchall()

    # Проверяем, есть ли объявления
    if not announcements:
        bot.send_message(message.chat.id, "🚫 Пока нет объявлений.")
        return

    # Начинаем формировать сообщение
    response = "📋 Доска объявлений:\n\n"

    for announcement in announcements:
        username, time_added, text = announcement
        formatted_time = datetime.datetime.fromtimestamp(time_added).strftime('%Y-%m-%d %H:%M:%S')
        response += f"👤 @{username}:\n{text}\n📅 {formatted_time}\n\n"

    # Отправляем сообщение с объявлениями
    bot.send_message(message.chat.id, response)                                                    

# Функция для удаления всех объявлений из базы данных
def delete_all_announcements():
    cursor.execute("DELETE FROM announcements")
    conn.commit()

# Обработчик команды /doska_z (удаление всех объявлений)
@bot.message_handler(commands=["doska_z"])
def clear_announcements(message):
    delete_all_announcements()
    bot.send_message(message.chat.id, "🚫 Все объявления были удалены!")    

@bot.message_handler(commands=['админи'])
def show_admins(message):
    chat_id = message.chat.id
    try:
        # Получаем список администраторов чата
        admins = bot.get_chat_administrators(chat_id)
        if not admins:
            bot.send_message(chat_id, "В этом чате нет администраторов.")
            return

        # Создаем отдельные категории для администраторов
        creator = None
        deputies = []
        helpers = []
        others = []

        # Разделяем администраторов по ролям
        for admin in admins:
            if admin.status == 'creator':  # Создатель чата
                creator = admin.user
            elif admin.status == 'administrator':  # Заместитель
                deputies.append(admin.user)
            elif admin.status == 'member':  # Помощник
                helpers.append(admin.user)
            else:  # Прочие администраторы
                others.append(admin.user)

        # Формируем текст со списком администраторов
        admin_list = "👑 <b>Администраторы чата:</b>\n\n"

        # Добавляем создателя в начало списка
        if creator:
            admin_list += f"👑 <b>Создатель:</b> @{creator.username or f'Пользователь {creator.id}'}\n\n"

        # Добавляем заместителей
        if deputies:
            admin_list += "💼 <b>Заместители:</b>\n"
            for deputy in deputies:
                admin_list += f"— @{deputy.username or f'Пользователь {deputy.id}'}\n"
            admin_list += "\n"

        # Добавляем помощников
        if helpers:
            admin_list += "🤝 <b>Помощники:</b>\n"
            for helper in helpers:
                admin_list += f"— @{helper.username or f'Пользователь {helper.id}'}\n"
            admin_list += "\n"

        # Добавляем прочих администраторов (если есть)
        if others:
            admin_list += "⭐ <b>Другие администраторы:</b>\n"
            for other in others:
                admin_list += f"— @{other.username or f'Пользователь {other.id}'}\n"

        # Отправляем сообщение со списком администраторов
        bot.send_message(
            chat_id,
            admin_list,
            parse_mode="HTML"
        )
    except Exception as e:
        bot.send_message(chat_id, f"Произошла ошибка: {e}")

import datetime
from datetime import datetime

@bot.message_handler(regexp="📊 Статистика")
def update_handler(message):
    # Получение данных из базы
    cursor.execute("SELECT COUNT(*) FROM users")  # Количество участников
    total_users = cursor.fetchone()[0]
    
    cursor.execute("SELECT SUM(gram) FROM users")  # Общее количество граммов
    total_grams = cursor.fetchone()[0] or 0
    
    cursor.execute("SELECT SUM(stars) FROM users")  # Общее количество старсов
    total_stars = cursor.fetchone()[0] or 0

    # Формируем текст для отправки
    update_text = (
        "📊 **Статистика проекта:**\n\n"
        f"👥 Всего участников: {total_users}\n"
        f"🪙 Всего GRAM: {total_grams:.2f}\n"
        f"🌟 Всего Stars: {total_stars:.2f}\n\n"
        "🔗 **Полезные ссылки:**\n"
        "📢 Канал по обновлениям: [valyta_Gram_stras](https://t.me/valyta_Gram_stras)\n"
        "💬 Игровой чат: [GVB_klan](https://t.me/GVB_klan)\n"
        "👑 Администратор: [@basir_l2](https://t.me/basir_l2)\n"
         "⚙️ Баги и идеи: [Лаборатория](https://t.me/laborotory_2)\n"
               )

    bot.send_message(
        message.chat.id, 
        update_text, 
        parse_mode="Markdown"
    )
          
@bot.message_handler(commands=['1бан'])
def ban_user(message):
    # Проверяем, является ли пользователь админом
    if message.from_user.id != ADMIN_ID:
        bot.send_message(message.chat.id, "❌ У вас нет прав на использование этой команды.")
        return

    # Проверяем, есть ли ответ на сообщение
    if not message.reply_to_message:
        bot.send_message(message.chat.id, "🔴 Используйте команду /бан в ответ на сообщение пользователя.")
        return

    banned_user_id = message.reply_to_message.from_user.id

    # Удаляем пользователя из всех таблиц
    cursor.execute("DELETE FROM users WHERE user_id = ?", (banned_user_id,))
    cursor.execute("DELETE FROM game_stats WHERE user_id = ?", (banned_user_id,))
    cursor.execute("DELETE FROM skins WHERE user_id = ?", (banned_user_id,))
    cursor.execute("DELETE FROM donations WHERE user_id = ?", (banned_user_id,))
    cursor.execute("DELETE FROM clan_members WHERE user_id = ?", (banned_user_id,))
    conn.commit()

    bot.send_message(
        message.chat.id, 
        f"🚨 Пользователь {message.reply_to_message.from_user.first_name} ({banned_user_id}) был полностью удалён из базы данных. "
        "Теперь он как будто никогда не существовал в боте. 👻"
    )

@bot.message_handler(commands=['всем'])
def give_all(message):
    if message.from_user.id != ADMIN_ID:
        bot.send_message(message.chat.id, "❌ У вас нет доступа к этой команде.")
        return

    # Разбираем сообщение, чтобы получить сумму
    args = message.text.split()
    if len(args) != 2 or not args[1].isdigit():
        bot.send_message(message.chat.id, "⚠️ Используйте команду так: `/всем 500`", parse_mode="Markdown")
        return

    amount = int(args[1])

    # Получаем список всех пользователей
    cursor.execute("SELECT user_id FROM users")
    users = cursor.fetchall()
    
    if not users:
        bot.send_message(message.chat.id, "⚠️ В базе нет пользователей для начисления GRAM.")
        return

    # Обновляем баланс всех пользователей
    cursor.execute("UPDATE users SET gram = gram + ?", (amount,))
    conn.commit()

    total_users = 0  # Счетчик получателей
    total_grams = 0  # Общая сумма начислений

    # Отправляем каждому пользователю уведомление
    for user in users:
        try:
            bot.send_message(user[0], f"Поздравляю с 8 Марта! 💝 Желаю всего наилучшего. {amount} GRAM ")
            total_users += 1
            total_grams += amount
        except Exception as e:
            print(f"Ошибка отправки пользователю {user[0]}: {e}")

    bot.send_message(
        message.chat.id,
        f"✅ Бонус успешно отправлен!\n👥 Получателей: {total_users}\n💰 Всего начислено: {total_grams} GRAM"
    )

@bot.message_handler(regexp="📢 Пригласить бота")
def invite_bot(message):
    user_id = message.from_user.id
    invite_link = f"https://t.me/{bot.get_me().username}?startgroup=true"

    bot.send_message(
        message.chat.id,
        f"➕ Добавьте бота в свой канал или чат по ссылке:\n\n{invite_link}\n\n"
        "💰 После добавления бота и назначения его администратором, вам начислится 5,000 GRAM!",
        reply_markup=types.InlineKeyboardMarkup().add(
            types.InlineKeyboardButton("➕ Добавить бота", url=invite_link)
        )
    )
    
@bot.my_chat_member_handler()
def track_bot_addition_removal(update):
    chat_id = update.chat.id
    user_id = update.from_user.id
    status = update.new_chat_member.status  

    if status in ["administrator", "member"]:  # Если бот добавлен
        cursor.execute("SELECT chat_id FROM bot_channels WHERE chat_id = ?", (chat_id,))
        existing_chat = cursor.fetchone()

        if not existing_chat:  # Если канал/чат новый
            cursor.execute("INSERT INTO bot_channels (chat_id, user_id) VALUES (?, ?)", (chat_id, user_id))
            conn.commit()
            
            cursor.execute("UPDATE users SET gram = gram + 5000 WHERE user_id = ?", (user_id,))
            conn.commit()
            
            bot.send_message(user_id, "✅ Вы добавили бота в чат/канал! Вам начислено 7,000 GRAM.")
        else:
            bot.send_message(user_id, "🤖 Бот уже добавлен в этот чат/канал.")
    
    elif status in ["kicked", "left"]:  # Если бот удален
        cursor.execute("SELECT user_id FROM bot_channels WHERE chat_id = ?", (chat_id,))
        owner = cursor.fetchone()

        if owner:
            owner_id = owner[0]
            cursor.execute("UPDATE users SET gram = gram - 5000 WHERE user_id = ?", (owner_id,))
            conn.commit()

            bot.send_message(owner_id, "❌ Вы удалили бота из чата/канала. С вашего баланса списано 5,000 GRAM.")
        
        cursor.execute("DELETE FROM bot_channels WHERE chat_id = ?", (chat_id,))
        conn.commit()
                                           
@bot.message_handler(commands=['admin'])
def admin_panel(message):
    if message.from_user.id == ADMIN_ID:
        bot.send_message(message.chat.id, "👑 Админ-панель", reply_markup=admin_menu())
    else:
        bot.send_message(message.chat.id, "❌ У вас нет доступа к этой команде.")

@bot.message_handler(func=lambda message: message.text == "📢 Реклама")
def ask_for_advertisement(message):
    if message.from_user.id == ADMIN_ID:
        msg = bot.send_message(message.chat.id, "✏️ Отправьте текст, изображение, видео или пересланное сообщение из канала:")
        bot.register_next_step_handler(msg, send_advertisement)
    else:
        bot.send_message(message.chat.id, "❌ У вас нет доступа к этой функции.")

def send_advertisement(message):
    if message.forward_from_chat and message.forward_from_chat.type == "channel":
        is_forwarded = True  # Если сообщение переслано из канала
    else:
        is_forwarded = False  # Если просто текст/фото/видео и т.д.

    bot.send_message(message.chat.id, "📤 Начинаю рассылку...")

    # Отправка пользователям
    cursor.execute("SELECT user_id FROM users")
    users = cursor.fetchall()
    sent_users = 0

    for user in users:
        user_id = user[0]
        try:
            if is_forwarded:
                bot.forward_message(user_id, message.chat.id, message.message_id)  # Пересылка от канала
            else:
                bot.copy_message(user_id, message.chat.id, message.message_id)  # Полная копия без пересылки
            sent_users += 1
        except Exception as e:
            print(f"Ошибка отправки пользователю {user_id}: {e}")

    # Отправка в чаты
    chat_list = [CHAT_ID_1, CHAT_ID_2]
    sent_chats = 0

    for chat_id in chat_list:
        try:
            if is_forwarded:
                bot.forward_message(chat_id, message.chat.id, message.message_id)
            else:
                bot.copy_message(chat_id, message.chat.id, message.message_id)
            sent_chats += 1
        except Exception as e:
            print(f"Ошибка отправки в чат {chat_id}: {e}")

    # Отчет админу
    bot.send_message(
        message.chat.id,
        f"✅ Рассылка завершена!\n"
        f"👤 Доставлено пользователям: {sent_users}\n"
        f"💬 Доставлено в чаты: {sent_chats}"
    )           

import datetime
import random

# Ожидание фото от админа
waiting_for_photo = False
bonus_photo = None

@bot.message_handler(commands=["дону"])
def request_bonus_photo(message):
    global waiting_for_photo
    if message.from_user.id == admin_id:
        waiting_for_photo = True
        bot.send_message(message.chat.id, "Отправьте фото для бонуса.")
    else:
        bot.send_message(message.chat.id, "У вас нет доступа к этой команде.")

@bot.message_handler(content_types=['photo'])
def receive_bonus_photo(message):
    global waiting_for_photo, bonus_photo
    if waiting_for_photo and message.from_user.id == admin_id:
        bonus_photo = message.photo[-1].file_id
        waiting_for_photo = False
        bot.send_message(message.chat.id, "Фото бонуса сохранено!")


# Создаем таблицу для хранения прогресса игр
cursor.execute('''
CREATE TABLE IF NOT EXISTS game_progress (
    user_id INTEGER PRIMARY KEY,
    games_played INTEGER DEFAULT 0,
    last_bonus_level INTEGER DEFAULT 0
)
''')
conn.commit()

@bot.message_handler(regexp="🎁 Бонус")
def daily_bonus(message):
    user_id = message.from_user.id
    today = datetime.datetime.now().strftime("%Y-%m-%d")
    
    cursor.execute("SELECT last_bonus FROM users WHERE user_id = ?", (user_id,))
    result = cursor.fetchone()
    
    if result and result[0] == today:
        bot.send_message(message.chat.id, "Вы уже получили бонус сегодня! Приходите завтра. ⏳")
        return
    
    bonus_amount = random.randint(100, 750)
    
    cursor.execute("UPDATE users SET gram = gram + ?, last_bonus = ? WHERE user_id = ?", 
                   (bonus_amount, today, user_id))
    conn.commit()
    
    if bonus_photo:
        bot.send_photo(message.chat.id, bonus_photo, caption=f"🎉 Вы получили {bonus_amount} GRAM! Возвращайтесь завтра за новым бонусом. 🎁")
    else:
        bot.send_message(message.chat.id, f"🎉 Вы получили {bonus_amount} GRAM! Возвращайтесь завтра за новым бонусом. 🎁")

# Команда /vp для отображения рейтинга по донатам
@bot.message_handler(commands=['vp'])
def view_donations(message):
    cursor.execute("SELECT u.username, d.donation_amount FROM donations d JOIN users u ON d.user_id = u.user_id WHERE d.donation_amount > 0 ORDER BY d.donation_amount DESC")
    results = cursor.fetchall()

    if not results:
        bot.send_message(message.chat.id, "Рейтинг пока пуст.")
        return

    # Создаем красивый яркий текст рейтинга
    rating = "🏆 <b>Топ донаторов:</b> 🏆\n\n"
    for i, (username, donation_amount) in enumerate(results, start=1):
        medal = "🥇" if i == 1 else "🥈" if i == 2 else "🥉" if i == 3 else "⭐"
        rating += f"{medal} <b>{username}</b> — <b>{donation_amount:.2f} GRAM</b>\n"

    # Отправляем сообщение с ярким дизайном
    bot.send_message(
        message.chat.id, 
        rating, 
        parse_mode='HTML',
        reply_markup=types.ReplyKeyboardRemove()
    )

cursor.execute('''
CREATE TABLE IF NOT EXISTS pets (
    user_id INTEGER PRIMARY KEY,
    pet_name TEXT,
    pet_image TEXT
)
''')
conn.commit()

@bot.message_handler(commands=['ref'])
def show_referrals_top(message):
    # Запрос для подсчета количества рефералов каждого пользователя
    cursor.execute("""
        SELECT u.username, COUNT(r.user_id) AS ref_count
        FROM users u
        LEFT JOIN users r ON u.user_id = r.referrer_id
        GROUP BY u.user_id
        ORDER BY ref_count DESC
        LIMIT 10
    """)
    referrals = cursor.fetchall()

    # Формируем сообщение с топом рефералов
    if referrals:
        top_text = "🏅 Топ рефералов:\n\n"
        for idx, (username, ref_count) in enumerate(referrals, 1):
            top_text += f"{idx}. {username} — {ref_count} рефералов\n"
    else:
        top_text = "🚫 В данный момент нет данных о рефералах."

    # Отправляем топ участникам
    bot.send_message(message.chat.id, top_text)

admin_id = 6392028972  

# Создаем таблицу пользователей, если её нет
with conn:
    cursor = conn.cursor()
    cursor.execute("""
        CREATE TABLE IF NOT EXISTS users (
            user_id INTEGER PRIMARY KEY,
            username TEXT,
            balance INTEGER DEFAULT 0
        )
    """)

# Обработчик кнопки "💰 Заработать GRAM"
@bot.message_handler(regexp="💰 Заработать")
def earn_gram_handler(message):
    keyboard = types.InlineKeyboardMarkup()
    ref_button = types.InlineKeyboardButton("👥 Рефералы", callback_data="referrals")
    reaction_button = types.InlineKeyboardButton("❤️ Реакции", callback_data="reactions")
    keyboard.add(ref_button, reaction_button)
    
    bot.send_message(message.chat.id, "Выберите способ заработка GRAM:", reply_markup=keyboard)

# Обработчик кнопки "Рефералы"
@bot.callback_query_handler(func=lambda call: call.data == "referrals")
def referrals_handler(call):
    user_id = call.from_user.id
    username = call.from_user.username or f"ID: {user_id}"  # Если username нет, показываем ID
    ref_link = f"https://t.me/{bot.get_me().username}?start={user_id}"
    
    # Создаем новый курсор для запроса
    with conn:
        cursor = conn.cursor()
        cursor.execute("SELECT COUNT(*) FROM users WHERE referrer_id = ?", (user_id,))
        total_referrals = cursor.fetchone()[0] or 0

    earnings_from_referrals = total_referrals * 1500

    ref_text = (
        f"👥 **Реферальная программа**\n\n"
        f"🔗 Ваша реферальная ссылка: [Нажмите, чтобы скопировать]({ref_link})\n"
        f"💰 1 реферал = 1500 GRAM\n"
        f"👤 Приглашено: {total_referrals}\n"
        f"💎 Заработано с рефералов: {earnings_from_referrals} GRAM\n\n"
        "Поделитесь ссылкой с друзьями, чтобы заработать больше GRAM!"
    )

    bot.send_message(call.message.chat.id, ref_text, parse_mode="Markdown", disable_web_page_preview=True)

# Словарь для хранения информации о пользователях, которые нажали кнопку "реакции"
reaction_users = {}

# Обработчик кнопки "Реакции"
@bot.callback_query_handler(func=lambda call: call.data == "reactions")
def reactions_handler(call):
    user_id = call.from_user.id
    reaction_users[user_id] = True  # Помечаем, что этот пользователь нажал кнопку

    bot.send_message(
        call.message.chat.id,
        "🔹 **Поставь реакцию и получи граммы !** \n\n"
        "❤️ **Обычные реакции** – **500 грамм**\n"
        "🔁 **3 реакции** = **1500 грамм**\n"
        "⭐ **Платные реакции** – **5000 грамм** за 1 шт.\n\n"
        "📸 **Как получить граммы?**\n"
        "1️⃣ Нажми [🔗 Нажать здесь](https://t.me/kartina111/266) и оставь реакцию.\n"
        "2️⃣ Сделай **скриншот** с твоей реакцией.\n"
        "3️⃣ Отправь его сюда.\n\n"
        "🎯 После подтверждения ты получишь свои **граммы!** 🚀",
        parse_mode="Markdown",
        disable_web_page_preview=True
    )

@bot.message_handler(content_types=["photo"])
def receive_screenshot(message):
    user_id = message.from_user.id
    
    # Проверяем, был ли этот пользователь в списке тех, кто нажал кнопку "реакции"
    if user_id not in reaction_users:
        return  # Просто игнорируем, если пользователь не в списке

    # Убираем пользователя из списка, чтобы не отправлять его скриншоты снова
    del reaction_users[user_id]

    # Отправляем админу сообщение с кнопкой подтверждения
    username = message.from_user.username or f"ID: {user_id}"  # Если нет username, используем ID
    keyboard = types.InlineKeyboardMarkup()
    approve_button = types.InlineKeyboardButton("✅ Подтвердить", callback_data=f"approve_reaction_{user_id}")
    keyboard.add(approve_button)

    bot.send_photo(admin_id, message.photo[-1].file_id, caption=f"📸 Проверка реакции от пользователя @{username}", reply_markup=keyboard)
    
    # Отправляем пользователю сообщение о том, что фото отправлено на проверку
    bot.send_message(message.chat.id, "✅ Ваш скриншот отправлен на проверку админу. Ожидайте!")

# Обработчик подтверждения администратора
@bot.callback_query_handler(func=lambda call: call.data.startswith("approve_reaction_"))
def approve_reaction_handler(call):
    user_id = int(call.data.split("_")[-1])

    # Создаем новый курсор для обновления баланса
    with conn:
        cursor = conn.cursor()
        cursor.execute("UPDATE users SET balance = balance + 5000 WHERE user_id = ?", (user_id,))
    
    # Получаем username пользователя
    with conn:
        cursor = conn.cursor()
        cursor.execute("SELECT username FROM users WHERE user_id = ?", (user_id,))
        username = cursor.fetchone()
        username = username[0] if username else f"ID: {user_id}"

    # Сообщаем пользователю, что задание подтверждено
    bot.send_message(user_id, "🎉 Ваша реакция подтверждена! Вы получили 5000 GRAM.")
    bot.send_message(call.message.chat.id, f"✅ Реакция от @{username} подтверждена. Начислено 5000 GRAM.")

cursor.execute('''
CREATE TABLE IF NOT EXISTS secret_message_readers (
       user_id INTEGER,
       secret_id INTEGER,
       PRIMARY KEY (user_id, secret_id),
       FOREIGN KEY (secret_id) REFERENCES secret_messages(secret_id)
   );
''')
conn.commit()

from telebot import TeleBot, types

# Данные для крафтов (по 10 на страницу)
crafts = [
    "🎄Christmas tree из +100🎁+100⏳ при 🪚",
  "🪑chair из +35🪵 при 🪚",
  "⚰️coffin из 100🪵+10🦴 при 🪚💔",    "🎨artist palette из 🟣+🟡+🟢+🔵+🔴+5🪵+5💦 при 🪚",
  "🔳white square button из +10🔲",
  "☑️check box with check из +10🔳",
  "♾️infinity из +10☑️",
  "🔢input numbers из +1️⃣+2️⃣3️⃣+4️⃣",
  "💯hundred points из +101️⃣ при🆗",
  "🚪door из +50🪵 при 🪚",
  "🪟window из +25🪵 при 🪚",        "🛏️bed из 75🪵 при 🪚",
    "🏠house из +🛏️+🪟+🚪+🪑+30🪵 при 🪚",
    "🏘️houses из +🏠+25🪵 при 🪚",
    "🏚️derelict house из +🏠+1000⏳",
    "⛰️snow-capped mountain из +1000🪨",
    "🌈rainbow из +💎+1000🔦+350💦 при ⛰️",
    "📄page facing up из +5⬜",
    "🫧bubbles из +100💦",
    "🆗OK Button из +1️⃣+2️⃣+3️⃣+4️⃣+5️⃣+6️⃣+7️⃣+8️⃣+9️⃣",
    "🏳‍🌈rainbow flag из +🟥+🟧+🟨+🟩+🟦+🟪 при ❤️",
    "💔broken heart из +❤️+💛+💚+💙+🤍+🤎+💙+💜+🖤+🧡",
    "💾floppy disk из +🔢+🆗",
    "🔟keycap: 10 из +101️⃣",
    "🏳white flag из +10⬜+5🧶 при ✂️",
    "🏴black flag из +10⬛+5🧶 при ✂️"
]

# Функция для отправки страницы крафтов
def send_crafts_page(chat_id, page=1):
    items_per_page = 10
    start = (page - 1) * items_per_page
    end = start + items_per_page
    page_crafts = crafts[start:end]
    text = "\n\n".join(page_crafts) if page_crafts else "Страница пуста."

    # Кнопки для навигации
    markup = types.InlineKeyboardMarkup(row_width=3)
    if page > 1:
        markup.add(types.InlineKeyboardButton("⬅️", callback_data=f"page_{page-1}"))
    markup.add(types.InlineKeyboardButton("⚙️ Крафт", callback_data="craft"))
    if end < len(crafts):
        markup.add(types.InlineKeyboardButton("➡️", callback_data=f"page_{page+1}"))
    markup.add(types.InlineKeyboardButton("❌ Закрыть", callback_data="close"))

    bot.send_message(chat_id, text, reply_markup=markup)

@bot.message_handler(func=lambda message: message.text == "Схемы")
def handle_scheme(message):
    send_crafts_page(message.chat.id, page=1)


@bot.callback_query_handler(func=lambda call: call.data.startswith("page_"))
def page_callback(call):
    page = int(call.data.split("_")[1])
    bot.edit_message_text(chat_id=call.message.chat.id, message_id=call.message.id, text="Загрузка...")
    send_crafts_page(call.message.chat.id, page=page)


@bot.callback_query_handler(func=lambda call: call.data == "craft")
def craft_callback(call):
    bot.answer_callback_query(call.id, "⚙️ Крафт")
    bot.send_message(call.message.chat.id, "⚙️ Крафт")


@bot.callback_query_handler(func=lambda call: call.data == "close")
def close_callback(call):
    bot.delete_message(chat_id=call.message.chat.id, message_id=call.message.id)

from telebot import types

# Создание и активация секретных сообщений
@bot.inline_handler(lambda query: query.query.startswith("сс"))
def inline_create_secret_message(query):
    try:
        args = query.query.split()
        if len(args) != 3:
            results = [
                types.InlineQueryResultArticle(
                    id="error",
                    title="Ошибка",
                    description="Используйте: сс <секретный текст> <количество активаций>",
                    input_message_content=types.InputTextMessageContent(
                        message_text="Неправильный формат команды. Используйте: сс <секретный текст> <количество активаций>."
                    )
                )
            ]
            bot.answer_inline_query(query.id, results)
            return

        _, secret_text, activation_limit = args
        activation_limit = int(activation_limit)

        if activation_limit <= 0:
            results = [
                types.InlineQueryResultArticle(
                    id="error",
                    title="Ошибка",
                    description="Укажите корректное количество активаций.",
                    input_message_content=types.InputTextMessageContent(
                        message_text="Укажите корректное количество активаций."
                    )
                )
            ]
            bot.answer_inline_query(query.id, results)
            return

        # Проверка баланса пользователя
        user_id = query.from_user.id
        cursor.execute("SELECT gram FROM users WHERE user_id = ?", (user_id,))
        result = cursor.fetchone()

        if not result or result[0] < 10:  # Например, активация сообщения стоит 10 GRAM
            results = [
                types.InlineQueryResultArticle(
                    id="error",
                    title="Ошибка",
                    description="У вас недостаточно GRAM для создания секретного сообщения.",
                    input_message_content=types.InputTextMessageContent(
                        message_text=f"У вас {result[0] if result else 0} GRAM, а нужно 10 GRAM для создания секретного сообщения."
                    )
                )
            ]
            bot.answer_inline_query(query.id, results)
            return

        # Списание GRAM и создание секретного сообщения
        cursor.execute("UPDATE users SET gram = gram - 10 WHERE user_id = ?", (user_id,))
        cursor.execute('''
            INSERT INTO secret_messages (creator_id, secret_text, activation_limit, activations, activations_left)
            VALUES (?, ?, ?, 0, ?)
        ''', (user_id, secret_text, activation_limit, activation_limit))
        conn.commit()

        secret_id = cursor.lastrowid

        # Формируем результат для инлайн-режима
        results = [
            types.InlineQueryResultArticle(
                id="secret_created",
                title=f"Секретное сообщение",
                description=f"Всего активаций: {activation_limit}",
                input_message_content=types.InputTextMessageContent(
                    message_text=f"Секретное сообщение создано! ID сообщения: {secret_id}\n🔄 Всего активаций: {activation_limit}"
                ),
                reply_markup=create_secret_message_markup(secret_id)
            )
        ]
        bot.answer_inline_query(query.id, results)

    except ValueError:
        results = [
            types.InlineQueryResultArticle(
                id="error",
                title="Ошибка",
                description="Неправильный формат команды.",
                input_message_content=types.InputTextMessageContent(
                    message_text="Неправильный формат команды. Используйте: сс <секретный текст> <количество активаций>."
                )
            )
        ]
        bot.answer_inline_query(query.id, results)

# Создание кнопки для прочтения секретного сообщения
def create_secret_message_markup(secret_id):
    markup = types.InlineKeyboardMarkup()
    activate_button = types.InlineKeyboardButton(
        text="Прочитать",
        callback_data=f"read_secret_{secret_id}"
    )
    markup.add(activate_button)
    return markup

# Обработка активации секретного сообщения (когда пользователь нажимает на кнопку "Прочитать")
@bot.callback_query_handler(func=lambda call: call.data.startswith("read_secret_"))
def read_secret(call):
       secret_id = int(call.data.split("_")[2])
       user_id = call.from_user.id

       # Проверка, прочитал ли текущий пользователь сообщение ранее
       cursor.execute("SELECT 1 FROM secret_message_readers WHERE user_id = ? AND secret_id = ?", (user_id, secret_id))
       if cursor.fetchone():
           bot.answer_callback_query(call.id, "Вы уже прочитали это сообщение.")
           return

       # Проверка наличия секретного сообщения и лимита активаций
       cursor.execute("SELECT secret_text, activations_left FROM secret_messages WHERE secret_id = ?", (secret_id,))
       secret = cursor.fetchone()

       if not secret:
           bot.answer_callback_query(call.id, "Секретное сообщение не найдено.")
           return

       secret_text, activations_left = secret

       if activations_left <= 0:
           bot.answer_callback_query(call.id, "Лимит активаций этого сообщения исчерпан.")
           return

       # Отправка сообщения пользователю в alert-режиме
       bot.answer_callback_query(call.id, show_alert=True, text=secret_text)

       # Уменьшаем количество оставшихся активаций
       cursor.execute("UPDATE secret_messages SET activations_left = activations_left - 1 WHERE secret_id = ?", (secret_id,))

       # Добавляем запись о том, что текущий пользователь прочитал сообщение
       cursor.execute("INSERT INTO secret_message_readers (user_id, secret_id) VALUES (?, ?)", (user_id, secret_id))

       conn.commit()

       # Проверка, если активации исчерпаны, убираем кнопку
       if activations_left - 1 == 0:
           markup = types.InlineKeyboardMarkup()
           closed_button = types.InlineKeyboardButton(
               text="Максимум активаций использовано",
               callback_data="closed"
           )
           markup.add(closed_button)
           bot.edit_message_reply_markup(call.message.chat.id, call.message.message_id, reply_markup=markup)

craft_recipes = {
    "craft_money": {"🏠 ": 1, "⏳": 1000, "result": "🏚️"},
    "craft_heart": {"🟡 ": 1, "🟢": 1, "result": "🔵"},
    "craft_glasses": {"💧": 1, "result": "💦"},
    "craft_chair": {"🪵": 45, "🪚": 1, "result": "🪑"},
    "craft_door": {"🪵": 55, "🪚": 1, "result": "🚪"},
    "craft_window": {"🪵": 35, "🪚": 1, "result": "🪟"},
    "craft_bed": {"🪵": 80,  "🪚": 1, "result": "🛏️"},  # Новый крафт
    "craft_house": {"🪑": 1, "🚪": 1, "🪟": 1, "🪵": 15, "🛏️": 1, "🪚": 1,"result": "🏠"},  # Обновленный крафт
    "craft_bubble": {"💦": 100, "result": "🫧"},
    "craft_mountain": {"🪨": 1000, "⛏️": 1, "result": "⛰️"},
    "craft_town": {"🏠": 3, "🪵": 25, "🪚": 1, "result": "🏘️"},
    "craft_scarf": {"🧶": 5, "🟥": 5, "🎗️": 5, "🪡": 1, "result": "🧣"},
    "craft_shirt": {"🧶": 10, "🟦": 10, "🎗️": 15, "🪡": 1, "result": "👕"},
    "craft_night_city": {"🏘️": 1, "🌚": 15, "🌟": 15, "result": "🌃"},
    "craft_broken_heart": {"❤️": 5, "💛": 1, "💚": 1, "💙": 1, "result": "💔"},
    "craft_numbers": {"1️⃣": 1, "2️⃣": 1, "3️⃣": 1, "4️⃣": 1, "result": "🔢"},
    "craft_101": {"1️⃣": 1, "1️⃣": 10, "result": "🔟"},
    "craft_100": {"1️⃣": 1, "2️⃣": 1, "3️⃣": 1, "4️⃣": 1, "🔢": 1, "10🔟": 1, "result": "💯"},
    "craft_coffin": {"🪵": 150, "🦴": 10, "result": "⚰️"},
    "craft_tree": {"🎁": 250, "⏳": 100, "result": "🎄"},
    "craft_square": {"🔲": 10, "result": "🔳"},  # Новый крафт
    "craft_big_square": {"🔳": 10, "result": "☑️"},  # Новый крафт
    "craft_infinity": {"☑️": 10, "result": "♾️"},  # Новый крафт

 # Новые рецепты
  "craft_mosque": {"🏠": 1, "🛐": 1, "☪️": 1, "result": "🕌"},
  "craft_church": {"🏠": 1, "🛐": 1, "☦️": 1, "result": "⛪"},
  "craft_synagogue": {"🏠": 1, "🛐": 1, "✡️": 1, "result": "🕍"},
  "craft_bank": {"🏠": 1, "💵": 1, "💷": 1, "💲": 10, "🛐": 1, "result": "🏦"},
  "craft_black": {"◼️": 5, "result": "⬛"},
  "craft_black_flag": {"⬛": 10, "🧶": 5, "result": "🏴"},
  "craft_white_flag": {"⬜": 10, "🧶": 5, "result": "🏳️"},
  "craft_white_large": {"◻️": 5, "result": "⬜"}
}


craft_list = list(craft_recipes.keys())  # Список всех крафтов
CRAFTS_PER_PAGE = 10  # Количество рецептов на одной странице

# Добавляем кнопку переплавки рядом с кнопками перелистывания
@bot.message_handler(func=lambda message: message.text == "⚙️ Крафт")
def craft_menu(message, page=0):
    keyboard = types.InlineKeyboardMarkup()

    start = page * CRAFTS_PER_PAGE
    end = start + CRAFTS_PER_PAGE
    for craft_key in craft_list[start:end]:
        craft_data = craft_recipes[craft_key]
        ingredients = " + ".join([f"{v} {k}" for k, v in craft_data.items() if k != "result"])
        button_text = f"{ingredients} = {craft_data['result']}"
        keyboard.add(types.InlineKeyboardButton(button_text, callback_data=f"{craft_key}_{page}"))

    # Кнопка переплавки
    keyboard.add(types.InlineKeyboardButton("🔥 Переплавка", callback_data=f"refine_page_{page}"))

    nav_buttons = []
    if page > 0:
        nav_buttons.append(types.InlineKeyboardButton("⬅️", callback_data=f"craft_page_{page-1}"))
    if end < len(craft_list):
        nav_buttons.append(types.InlineKeyboardButton("➡️", callback_data=f"craft_page_{page+1}"))

    if nav_buttons:
        keyboard.add(*nav_buttons)

    bot.send_message(message.chat.id, "Выберите предмет для крафта:", reply_markup=keyboard)

# Обработчик для переплавки
@bot.callback_query_handler(func=lambda call: call.data.startswith("refine_page_"))
def refine_page(call):
    user_id = call.from_user.id
    page = int(call.data.split("_")[-1])

    # Получаем инвентарь пользователя
    cursor.execute("SELECT skin, quantity FROM user_skins WHERE user_id = ?", (user_id,))
    user_skins = {row[0]: row[1] for row in cursor.fetchall()}

    # Формируем список скинов для переплавки
    available_skins = [skin for skin, quantity in user_skins.items() if quantity > 0]

    # Если нет доступных скинов
    if not available_skins:
        bot.answer_callback_query(call.id, "У вас нет скинов для переплавки!")
        return

    keyboard = types.InlineKeyboardMarkup()
    
    # Создаем кнопки для выбора скинов
    for i, skin in enumerate(available_skins[:15]):  # 3 x 5 = 15 скинов на страницу
        keyboard.add(types.InlineKeyboardButton(skin, callback_data=f"refine_{skin}_{page}"))

    # Навигационные кнопки для переплавки
    refine_nav_buttons = []
    if page > 0:
        refine_nav_buttons.append(types.InlineKeyboardButton("⬅️", callback_data=f"refine_page_{page-1}"))
    if len(available_skins) > (page + 1) * 15:
        refine_nav_buttons.append(types.InlineKeyboardButton("➡️", callback_data=f"refine_page_{page+1}"))

    if refine_nav_buttons:
        keyboard.add(*refine_nav_buttons)

    bot.send_message(call.message.chat.id, "Выберите скин для переплавки:", reply_markup=keyboard)

# Обработчик для переплавки скина в воск
@bot.callback_query_handler(func=lambda call: call.data.startswith("refine_"))
def refine_skin(call):
    user_id = call.from_user.id
    skin, page = call.data.split("_")[1], int(call.data.split("_")[-1])

    # Проверяем, есть ли у пользователя нужный скин
    cursor.execute("SELECT quantity FROM user_skins WHERE user_id = ? AND skin = ?", (user_id, skin))
    result = cursor.fetchone()

    if not result or result[0] < 1:
        bot.answer_callback_query(call.id, "У вас нет этого скина для переплавки!")
        return

    # Удаляем скин
    cursor.execute("UPDATE user_skins SET quantity = quantity - 1 WHERE user_id = ? AND skin = ?", (user_id, skin))
    
    # Добавляем воск
    cursor.execute('''
        INSERT INTO user_skins (user_id, skin, quantity)
        VALUES (?, ?, 1)
        ON CONFLICT(user_id, skin) DO UPDATE SET quantity = quantity + 1
    ''', (user_id, "🪔 "))

    conn.commit()

    bot.answer_callback_query(call.id, f"Вы успешно переплавили {skin} в воск!")
    bot.send_message(call.message.chat.id, f"🎉 Поздравляем! Вы получили воск за {skin}!")

    # Обновляем меню переплавки
    bot.delete_message(call.message.chat.id, call.message.message_id)
    refine_page(call)

@bot.callback_query_handler(func=lambda call: call.data.startswith("craft_page_"))
def change_page(call):
    page = int(call.data.split("_")[-1])
    bot.delete_message(call.message.chat.id, call.message.message_id)
    craft_menu(call.message, page)

@bot.callback_query_handler(func=lambda call: call.data.startswith("craft_") and not call.data.startswith("craft_page_"))
def craft_item(call):
    user_id = call.from_user.id
    craft_type, page = call.data.rsplit("_", 1)
    page = int(page)

    # Получаем инвентарь пользователя
    cursor.execute("SELECT skin, quantity FROM user_skins WHERE user_id = ?", (user_id,))
    user_skins = {row[0]: row[1] for row in cursor.fetchall()}

    if craft_type not in craft_recipes:
        return

    required_skins = craft_recipes[craft_type].copy()
    new_skin = required_skins.pop("result")

    # Проверяем наличие всех необходимых предметов
    for skin, needed in required_skins.items():
        if user_skins.get(skin, 0) < needed:
            bot.answer_callback_query(call.id, "У вас недостаточно ресурсов для крафта!")
            return

    # Убираем использованные предметы (кроме ⛏️ и 🪚)
    for skin, needed in required_skins.items():
        if skin not in ["⛏️", "🪚", "🪡"]:  # Эти предметы не тратятся
            cursor.execute("UPDATE user_skins SET quantity = quantity - ? WHERE user_id = ? AND skin = ?", (needed, user_id, skin))

    # Добавляем новый предмет
    cursor.execute('''
        INSERT INTO user_skins (user_id, skin, quantity)
        VALUES (?, ?, 1)
        ON CONFLICT(user_id, skin) DO UPDATE SET quantity = quantity + 1
    ''', (user_id, new_skin))
    
    conn.commit()

    bot.answer_callback_query(call.id, f"Вы успешно скрафтили {new_skin}!")
    bot.send_message(call.message.chat.id, f"🎉 Поздравляем! Вы создали {new_skin}!")

    # Обновляем меню крафта
    bot.delete_message(call.message.chat.id, call.message.message_id)
    craft_menu(call.message, page)

import random
# Обработчик кнопки "🎰 Казино"
@bot.message_handler(regexp="🎰 Казино")
def casino_handler(message):
    user_id = message.from_user.id
    
    # Проверка баланса пользователя
    cursor.execute("SELECT gram FROM users WHERE user_id = ?", (user_id,))
    user_data = cursor.fetchone()
    
    if not user_data or user_data[0] < 25:
        bot.send_message(
            message.chat.id, 
            "У вас недостаточно GRAM для игры. Пополните баланс или заработайте больше GRAM.",
            reply_markup=main_menu()
        )
        return
    
    # Списываем 25 GRAM за попытку
    cursor.execute("UPDATE users SET gram = gram - 25 WHERE user_id = ?", (user_id,))
    conn.commit()
    
    # Символы для слота
    symbols = ["7️⃣", "🍋", "🍇", "🍒", "🍉"]
    spin = [random.choice(symbols) for _ in range(3)]
    
    # Проверка комбинации
    result_text = "🎰 | " + " | ".join(spin) + " | 🎰\n\n"
    if spin == ["7️⃣", "7️⃣", "7️⃣"]:
        # Если выпало "777", награда 1000 GRAM
        cursor.execute("UPDATE users SET gram = gram + 1000 WHERE user_id = ?", (user_id,))
        conn.commit()
        result_text += "🎉 Поздравляем! Вам выпало 7️⃣7️⃣7️⃣! Вы выиграли 1000 GRAM! 🎉"
    elif spin[0] == spin[1] == spin[2]:
        # Если три одинаковых символа (не 7️⃣), награда 500 GRAM
        cursor.execute("UPDATE users SET gram = gram + 500 WHERE user_id = ?", (user_id,))
        conn.commit()
        result_text += f"🎉 Вам выпало три одинаковых символа {spin[0]}! Вы выиграли 500 GRAM! 🎉"
    else:
        # Если ничего не совпало
        result_text += "😔 К сожалению, вы ничего не выиграли -25 грамм. Попробуйте снова!"
    
    # Отправляем результат
    bot.send_message(
        message.chat.id, 
        result_text, 
        reply_markup=main_menu()
    )

duels = {}

@bot.message_handler(commands=['дуель'])
def duel_request(message):
    user_id = message.from_user.id

    # Проверяем, был ли вызов уже создан (проверка на активную дуэль)
    for opponent_id, duel in duels.items():
        if duel['challenger'] == user_id:
            bot.send_message(message.chat.id, "❌ Вы уже вызвали другого игрока на дуэль!")
            return

    # Сохраняем запрос на дуэль с текущим пользователем в качестве инициатора
    duels[user_id] = {'challenger': user_id}

    # Кнопка для принятия вызова
    keyboard = types.InlineKeyboardMarkup()
    accept_button = types.InlineKeyboardButton("⚔️ Принять вызов", callback_data=f"accept_duel_{user_id}")
    keyboard.add(accept_button)

    bot.send_message(
        message.chat.id,
        f"🎯 <b>{message.from_user.first_name}</b> вызывает кого-то на дуэль! Принять вызов?",
        parse_mode="HTML",
        reply_markup=keyboard
    )

@bot.callback_query_handler(func=lambda call: call.data.startswith("accept_duel_"))
def accept_duel(call):
    opponent_id = call.from_user.id
    duel_data = call.data.split("_")
    challenger_id = int(duel_data[2])

    # Проверяем, существует ли этот вызов и не является ли игрок инициатором
    if challenger_id not in duels or duels[challenger_id]['challenger'] == opponent_id:
        bot.answer_callback_query(call.id, "❌ Вы не можете принять свой собственный вызов", show_alert=True)
        return

    # Удаляем из списка активных дуэлей
    del duels[challenger_id]

    # Кнопки для игры (Камень, ножницы, бумага)
    keyboard = types.InlineKeyboardMarkup()
    options = ["✊", "✌️", "🤚"]
    for option in options:
        keyboard.add(types.InlineKeyboardButton(option, callback_data=f"rps_{challenger_id}_{opponent_id}_{option}"))

    bot.send_message(call.message.chat.id, f"🛡️ {call.from_user.first_name} принял дуэль!\nВыберите свой ход:", reply_markup=keyboard)

rps_choices = {}  # Словарь для хранения ходов участников

@bot.callback_query_handler(func=lambda call: call.data.startswith("rps_"))
def play_rps(call):
    data = call.data.split("_")
    challenger_id = int(data[1])
    opponent_id = int(data[2])
    player_choice = data[3]

    player_id = call.from_user.id

    # Проверяем, сделал ли ход первый игрок
    if (challenger_id, opponent_id) not in rps_choices:
        rps_choices[(challenger_id, opponent_id)] = {player_id: player_choice}
        bot.answer_callback_query(call.id, "Вы сделали выбор! Ожидаем соперника...")
        return

    # Получаем выбор обоих игроков
    rps_choices[(challenger_id, opponent_id)][player_id] = player_choice
    choices = rps_choices.pop((challenger_id, opponent_id))

    # Определяем победителя
    challenger_choice = choices[challenger_id]
    opponent_choice = choices[opponent_id]

    results = {
        ("✊", "✌️"): challenger_id, ("✌️", "🤚"): challenger_id, ("🤚", "✊"): challenger_id,  # Победа вызвавшего
        ("✌️", "✊"): opponent_id, ("🤚", "✌️"): opponent_id, ("✊", "🤚"): opponent_id  # Победа второго игрока
    }

    if challenger_choice == opponent_choice:
        winner_id = None
    else:
        winner_id = results.get((challenger_choice, opponent_choice))

    if winner_id:
        loser_id = challenger_id if winner_id == opponent_id else opponent_id

        # Победитель получит похвалу (или GRAM, если хотите)
        bot.send_message(call.message.chat.id,
            f"⚔️ Дуэль завершена!\n\n"
            f"🎭 {call.from_user.first_name} выбрал: {player_choice}\n"
            f"🏆 Победитель: {bot.get_chat_member(call.message.chat.id, winner_id).user.first_name}\n\n"
        )
    else:
        bot.send_message(call.message.chat.id, "⚔️ Дуэль завершилась ничьей!")     

@bot.message_handler(commands=['пж'])
def donate_invite(message):
    user_id = message.from_user.id
    username = message.from_user.username or f"Пользователь {user_id}"

    # Создаем инлайн-кнопку для пожертвования
    keyboard = types.InlineKeyboardMarkup()
    button = types.InlineKeyboardButton(
        text=f"Пожертвовать 100 GRAM {username}",
        callback_data=f"donate_{user_id}"
    )
    keyboard.add(button)

    # Отправляем сообщение с кнопкой в чат
    bot.send_message(
        message.chat.id,
        f"{username} просит пожертвовать ему 100 GRAM. Нажмите кнопку ниже, чтобы поддержать!",
        reply_markup=keyboard
    )

@bot.callback_query_handler(func=lambda call: call.data.startswith("donate_"))
def handle_donation(call):
    # Получаем ID получателя (того, кто создал кнопку)
    receiver_id = int(call.data.split("_")[1])
    sender_id = call.from_user.id
    sender_username = call.from_user.username or f"Пользователь {sender_id}"

    # Проверяем, есть ли у отправителя (нажавшего кнопку) 100 GRAM
    cursor.execute("SELECT gram FROM users WHERE user_id = ?", (sender_id,))
    result = cursor.fetchone()

    if not result or result[0] < 100:
        bot.answer_callback_query(call.id, "У вас недостаточно GRAM для пожертвования.")
        return

    # Выполняем перевод: списываем 100 GRAM с нажавшего кнопку и добавляем их получателю
    cursor.execute("UPDATE users SET gram = gram - 100 WHERE user_id = ?", (sender_id,))
    cursor.execute("INSERT OR IGNORE INTO users (user_id, gram) VALUES (?, 0)", (receiver_id,))
    cursor.execute("UPDATE users SET gram = gram + 100 WHERE user_id = ?", (receiver_id,))
    conn.commit()

    # Уведомляем обе стороны
    bot.answer_callback_query(call.id, "Вы успешно пожертвовали 100 GRAM.")
    bot.send_message(
        sender_id,
        f"Вы пожертвовали 100 GRAM пользователю {receiver_id}."
    )
    bot.send_message(
        receiver_id,
        f"{sender_username} пожертвовал(а) вам 100 GRAM!"
    )()

@bot.message_handler(commands=['мой_клан'])
def my_clan(message):
    user_id = message.from_user.id

    # Проверяем, есть ли у пользователя клан
    cursor.execute("SELECT clans.name FROM clans WHERE creator_id = ?", (user_id,))
    clan = cursor.fetchone()

    if clan:
        # Если клан есть, выводим его информацию
        clan_name = clan[0]
        cursor.execute("""
            SELECT u.username, cm.level FROM clan_members cm
            JOIN users u ON cm.user_id = u.user_id
            WHERE cm.clan_id = (SELECT id FROM clans WHERE name = ?)
        """, (clan_name,))
        members = cursor.fetchall()

        member_list = f"👑 <b>Клан:</b> {clan_name}\n\n"
        member_list += f"👑 <b>Создатель:</b> @{message.from_user.username or f'Пользователь {user_id}'}\n\n"
        member_list += "👥 <b>Участники:</b>\n"
        for i, (username, level) in enumerate(members, start=1):
            member_list += f"{i}. @{username} — Уровень: {level}\n"

        bot.send_message(
            message.chat.id,
            member_list,
            parse_mode="HTML",
            reply_markup=clan_management_menu(clan_name)
        )
    else:
        # Если клана нет, предлагаем создать
        cursor.execute("SELECT stars FROM users WHERE user_id = ?", (user_id,))
        user_stars = cursor.fetchone()[0]

        if user_stars < 150:
            bot.send_message(
                message.chat.id,
                "У вас недостаточно Stars для создания клана. Нужно 150 Stars."
            )
            return

        msg = bot.send_message(message.chat.id, "Введите название вашего клана:")
        bot.register_next_step_handler(msg, create_clan, user_id)


def create_clan(message, user_id):
    clan_name = message.text.strip()

    # Проверяем, есть ли уже такой клан
    cursor.execute("SELECT id FROM clans WHERE name = ?", (clan_name,))
    if cursor.fetchone():
        bot.send_message(message.chat.id, "Клан с таким названием уже существует.")
        return

    # Списываем Stars у пользователя
    cursor.execute("UPDATE users SET stars = stars - 150 WHERE user_id = ?", (user_id,))
    conn.commit()

    # Создаем клан
    cursor.execute("INSERT INTO clans (name, creator_id) VALUES (?, ?)", (clan_name, user_id))
    conn.commit()

    bot.send_message(message.chat.id, f"Клан '{clan_name}' успешно создан!")

    # Автоматически добавляем создателя в клан
    cursor.execute("INSERT INTO clan_members (user_id, clan_id) VALUES (?, (SELECT id FROM clans WHERE name = ?))", (user_id, clan_name))
    conn.commit()
    
    
def clan_management_menu(clan_name):
    keyboard = types.InlineKeyboardMarkup()
    join_button = types.InlineKeyboardButton(text="Вступить", callback_data=f"join_{clan_name}")
    leave_button = types.InlineKeyboardButton(text="Покинуть", callback_data=f"leave_{clan_name}")
    keyboard.add(join_button, leave_button)
    return keyboard
    
@bot.callback_query_handler(func=lambda call: call.data.startswith("join_"))
def join_clan(call):
    clan_name = call.data.split("_")[1]
    user_id = call.from_user.id

    # Проверяем, не состоит ли пользователь уже в клане
    cursor.execute("""
        SELECT cm.clan_id FROM clan_members cm
        JOIN clans c ON cm.clan_id = c.id
        WHERE cm.user_id = ?
    """, (user_id,))
    if cursor.fetchone():
        bot.answer_callback_query(call.id, "Вы уже состоите в клане.")
        return

    # Проверяем, существует ли клан
    cursor.execute("SELECT id FROM clans WHERE name = ?", (clan_name,))
    clan = cursor.fetchone()
    if not clan:
        bot.answer_callback_query(call.id, "Клан не найден.")
        return

    clan_id = clan[0]

    # Добавляем пользователя в клан с уровнем 1
    cursor.execute("INSERT INTO clan_members (user_id, clan_id, level) VALUES (?, ?, 1)", (user_id, clan_id))
    conn.commit()

    # Уведомляем создателя
    cursor.execute("SELECT creator_id FROM clans WHERE id = ?", (clan_id,))
    creator_id = cursor.fetchone()[0]
    bot.send_message(creator_id, f"@{call.from_user.username or f'Пользователь {user_id}'} вступил в ваш клан '{clan_name}'.")

    bot.answer_callback_query(call.id, f"Вы успешно вступили в клан '{clan_name}'.")

@bot.message_handler(commands=['klani'])
def clan_ranking(message):
    cursor.execute("""
        SELECT c.name, SUM(cm.level) AS total_levels
        FROM clans c
        LEFT JOIN clan_members cm ON c.id = cm.clan_id
        GROUP BY c.name
        ORDER BY total_levels DESC
    """)
    clans = cursor.fetchall()

    if clans:
        ranking = "🏆 <b>Рейтинг кланов по уровням:</b>\n\n"
        for i, (clan_name, total_levels) in enumerate(clans, start=1):
            ranking += f"{i}. {clan_name} — Суммарный уровень: {total_levels}\n"
        bot.send_message(message.chat.id, ranking, parse_mode="HTML")
    else:
        bot.send_message(message.chat.id, "Пока нет кланов.")
        
def increase_member_level(user_id, clan_id):
    cursor.execute("""
        UPDATE clan_members
        SET level = level + 1
        WHERE user_id = ? AND clan_id = ?
    """, (user_id, clan_id))
    conn.commit()        
        
# Функция обмена граммов на старсы
@bot.message_handler(func=lambda message: message.text == "💱 Обменять")
def exchange_grams(message):
    # Запрашиваем у пользователя количество граммов для обмена
    msg = bot.send_message(message.chat.id, "Введите количество граммов, которые хотите обменять на старсы 🌟 (1000, 2000, 3000,):")
    bot.register_next_step_handler(msg, process_exchange_grams)

# Обработка ввода количества граммов для обмена
def process_exchange_grams(message):
    user_id = message.from_user.id
    try:
        grams_amount = int(message.text)
        if grams_amount < 1000 or grams_amount % 1000 != 0:
            bot.send_message(user_id, "Количество граммов должно быть кратно 1000 и не менее 1000.")
            return
        
        # Получаем текущий баланс граммов
        cursor.execute("SELECT gram FROM users WHERE user_id = ?", (user_id,))
        result = cursor.fetchone()

        if not result:
            bot.send_message(user_id, "Ошибка: Вы не зарегистрированы в системе.")
            return

        current_grams = result[0]

        if current_grams < grams_amount:
            bot.send_message(user_id, f"У вас недостаточно граммов для обмена. Ваш текущий баланс: {current_grams} граммов.")
            return

        # Пересчитываем старсы (1 старс = 1000 грамм)
        stars_to_add = grams_amount // 1000

        # Обновляем баланс пользователя
        cursor.execute("UPDATE users SET gram = gram - ?, stars = stars + ? WHERE user_id = ?", (grams_amount, stars_to_add, user_id))
        conn.commit()

        # Подтверждаем обмен
        bot.send_message(user_id, f"Обмен завершен! Вы обменяли {grams_amount} граммов на {stars_to_add} старсов.")
    except ValueError:
        bot.send_message(user_id, "Пожалуйста, введите корректное количество граммов.")


# Таблица для хранения чеков
cursor.execute('''
CREATE TABLE IF NOT EXISTS checks (
    check_id INTEGER PRIMARY KEY AUTOINCREMENT,
    creator_id INTEGER NOT NULL,
    gram_amount REAL NOT NULL,
    activation_limit INTEGER NOT NULL,
    activations INTEGER DEFAULT 0
)
''')
conn.commit()

from telebot import types

@bot.inline_handler(lambda query: query.query.startswith("редк"))
def inline_check_rarity(query):
    try:
        args = query.query.split(maxsplit=1)

        if len(args) == 1:  
            # Если просто "редк" — отправляем весь список скинов  
            cursor.execute("SELECT skin, SUM(quantity) FROM user_skins GROUP BY skin")
            skins = cursor.fetchall()

            if not skins:
                text = "❌ В базе пока нет скинов."
            else:
                text = "📜 **Редкость скинов:**\n"
                for skin, total in skins:
                    text += f"🎭 {skin} — {total} раз(а)\n"

            results = [  
                types.InlineQueryResultArticle(  
                    id="all_skins",  
                    title="Список всех скинов",  
                    description="Посмотреть редкость всех скинов",  
                    input_message_content=types.InputTextMessageContent(  
                        message_text=text,  
                        parse_mode="Markdown"  
                    ),
                    reply_markup=types.InlineKeyboardMarkup().add(
                        types.InlineKeyboardButton(
                            text="@Valyta_stars_bot редк 🟡",
                            switch_inline_query_current_chat="Присоединяйся к этому боту!"
                        )
                    )
                )  
            ]  

        else:  
            # Проверка конкретного скина (эмодзи)
            skin = args[1]  
            cursor.execute("SELECT SUM(quantity) FROM user_skins WHERE skin = ?", (skin,))  
            result = cursor.fetchone()  

            total = result[0] if result and result[0] else 0  
            text = f"🎭 {skin} встречается {total} раз(а) среди всех пользователей."

            results = [  
                types.InlineQueryResultArticle(  
                    id=f"check_{skin}",  
                    title=f"{skin} — {total}",  
                    description="Редкость этого скина",  
                    input_message_content=types.InputTextMessageContent(  
                        message_text=text  
                    ),
                    reply_markup=types.InlineKeyboardMarkup().add(
                        types.InlineKeyboardButton(
                            text="Узнать редкость",
                            switch_inline_query_current_chat="редк 🟡"
                        )
                    )
                )  
            ]  

        bot.answer_inline_query(query.id, results, cache_time=5)

    except Exception as e:  
        print(f"Ошибка: {e}")  
        results = [  
            types.InlineQueryResultArticle(  
                id="error",  
                title="Ошибка",  
                description="Что-то пошло не так...",  
                input_message_content=types.InputTextMessageContent(  
                    message_text="❌ Произошла ошибка при обработке команды. Попробуйте снова."  
                )  
            )  
        ]  
        bot.answer_inline_query(query.id, results)

from telebot import types

@bot.inline_handler(lambda query: query.query.startswith("чк"))
def inline_create_check(query):
    try:
        args = query.query.split()
        if len(args) != 3:
            results = [
                types.InlineQueryResultArticle(
                    id="error",
                    title="Ошибка",
                    description="Используйте: чк <GRAM за активацию> <количество активаций>",
                    input_message_content=types.InputTextMessageContent(
                        message_text="Неправильный формат команды. Используйте: чк <GRAM за активацию> <количество активаций>."
                    )
                )
            ]
            bot.answer_inline_query(query.id, results)
            return

        _, gram_per_activation, activation_limit = args
        gram_per_activation = float(gram_per_activation)
        activation_limit = int(activation_limit)

        if gram_per_activation <= 0 or activation_limit <= 0:
            results = [
                types.InlineQueryResultArticle(
                    id="error",
                    title="Ошибка",
                    description="Укажите корректные значения.",
                    input_message_content=types.InputTextMessageContent(
                        message_text="Укажите корректное количество GRAM за активацию и количество активаций."
                    )
                )
            ]
            bot.answer_inline_query(query.id, results)
            return

        # Вычисляем итоговую сумму с учетом комиссии
        total_gram = gram_per_activation * activation_limit
        commission = total_gram * 0.08  # 5% комиссия
        total_with_commission = total_gram + commission

        # Проверяем баланс пользователя
        user_id = query.from_user.id
        cursor.execute("SELECT gram FROM users WHERE user_id = ?", (user_id,))
        result = cursor.fetchone()

        if not result or result[0] < total_with_commission:
            results = [
                types.InlineQueryResultArticle(
                    id="error",
                    title="Ошибка",
                    description="У вас недостаточно GRAM для создания чека.",
                    input_message_content=types.InputTextMessageContent(
                        message_text=f"У вас {result[0] if result else 0} GRAM, а нужно {total_with_commission:.2f} GRAM (с учетом комиссии 8%: {commission:.2f} GRAM)."
                    )
                )
            ]
            bot.answer_inline_query(query.id, results)
            return

        # Списание GRAM и создание чека
        cursor.execute("UPDATE users SET gram = gram - ? WHERE user_id = ?", (total_with_commission, user_id))
        cursor.execute('''
            INSERT INTO checks (creator_id, gram_amount, activation_limit, activations)
            VALUES (?, ?, ?, 0)
        ''', (user_id, gram_per_activation, activation_limit))
        conn.commit()

        check_id = cursor.lastrowid

        # Формируем результат для инлайн-режима
        results = [
            types.InlineQueryResultArticle(
                id="check_created",
                title=f"Чек на {gram_per_activation} GRAM за активацию",
                description=f"Всего активаций: {activation_limit}",
                input_message_content=types.InputTextMessageContent(
                    message_text=f"Чек создан! ID чека: {check_id}\n💰 Каждая активация: {gram_per_activation} GRAM\n🔄 Всего активаций: {activation_limit}\n📉 Общая сумма: {total_gram:.2f} GRAM\n🧾 Комиссия: {commission:.2f} GRAM"
                ),
                reply_markup=create_check_markup(check_id, gram_per_activation, activation_limit)
            )
        ]
        bot.answer_inline_query(query.id, results)

    except ValueError:
        results = [
            types.InlineQueryResultArticle(
                id="error",
                title="Ошибка",
                description="Неправильный формат команды.",
                input_message_content=types.InputTextMessageContent(
                    message_text="Неправильный формат команды. Используйте: чк <GRAM за активацию> <количество активаций>."
                )
            )
        ]
        bot.answer_inline_query(query.id, results)


# Создание кнопки для активации чека
def create_check_markup(check_id, gram_amount, activation_limit):
    markup = types.InlineKeyboardMarkup()
    activate_button = types.InlineKeyboardButton(
        text="Получить GRAM",
        callback_data=f"activate_check_{check_id}"
    )
    markup.add(activate_button)
    return markup
# Создание таблицы check_activations, если её нет
cursor.execute('''
CREATE TABLE IF NOT EXISTS check_activations (
    user_id INTEGER,
    check_id INTEGER,
    PRIMARY KEY (user_id, check_id)
)
''')
conn.commit()


# Обработка активации чека
@bot.callback_query_handler(func=lambda call: call.data.startswith("activate_check_"))
def activate_check(call):
    user_id = call.from_user.id
    check_id = int(call.data.split("_")[2])

    # Проверка наличия чека
    cursor.execute("SELECT creator_id, gram_amount, activation_limit, activations FROM checks WHERE check_id = ?", (check_id,))
    check = cursor.fetchone()
    
    if not check:
        bot.answer_callback_query(call.id, "Чек не найден.")
        return

    creator_id, gram_amount, activation_limit, activations = check

    # Проверка на превышение лимита активаций
    if activations >= activation_limit:
        bot.answer_callback_query(call.id, "Чек больше недоступен для активации.")
        return

    # Проверка, активировал ли пользователь этот чек
    cursor.execute("SELECT * FROM check_activations WHERE user_id = ? AND check_id = ?", (user_id, check_id))
    already_activated = cursor.fetchone()

    if already_activated:
        bot.answer_callback_query(call.id, "Вы уже активировали этот чек.")
        return

    # Добавление GRAM пользователю и обновление активаций
    cursor.execute("UPDATE users SET gram = gram + ? WHERE user_id = ?", (gram_amount, user_id))
    cursor.execute("UPDATE checks SET activations = activations + 1 WHERE check_id = ?", (check_id,))
    cursor.execute("INSERT INTO check_activations (user_id, check_id) VALUES (?, ?)", (user_id, check_id))
    conn.commit()

 
import random

@bot.message_handler(commands=['бой'])
def battle(message):
    # Проверяем, указана ли награда
    try:
        reward = int(message.text.split()[1])  # Извлекаем награду из команды
        if reward <= 0:
            raise ValueError  # Генерируем исключение для отрицательных и нулевых значений
    except (IndexError, ValueError):
        bot.reply_to(message, "Вы должны указать положительную награду после команды. Пример: /бой 100")
        return

    # Проверяем, есть ли ответ на сообщение
    if not message.reply_to_message:
        bot.reply_to(message, "Вы должны ответить на сообщение участника, чтобы вызвать его на бой.")
        return

    # Получаем участников
    attacker_id = message.from_user.id
    attacker_username = message.from_user.username or "Без имени"
    defender_id = message.reply_to_message.from_user.id
    defender_username = message.reply_to_message.from_user.username or "Без_имени"

    if attacker_id == defender_id:
        bot.reply_to(message, "Вы не можете атаковать сами себя!")
        return

    # Проверяем баланс атакующего
    cursor.execute("SELECT gram FROM users WHERE user_id = ?", (attacker_id,))
    attacker_balance = cursor.fetchone()
    if not attacker_balance or attacker_balance[0] < reward:
        bot.reply_to(message, "У вас недостаточно граммов для ставки!")
        return

    # Проверяем баланс защищающегося
    cursor.execute("SELECT gram FROM users WHERE user_id = ?", (defender_id,))
    defender_balance = cursor.fetchone()
    if not defender_balance or defender_balance[0] < reward:
        bot.reply_to(message, f"У {defender_username} недостаточно граммов для боя!")
        return

    # Создаем кнопки для принятия боя
    markup = types.InlineKeyboardMarkup()
    accept_button = types.InlineKeyboardButton("Принять вызов", callback_data=f"accept_battle_{attacker_id}_{defender_id}_{reward}")
    markup.add(accept_button)

    # Отправляем предложение на бой
    bot.send_message(
        message.chat.id,
        f"⚔️ @{attacker_username} вызвал @{defender_username} на бой с наградой {reward} GRAM!\n"
        f"{defender_username}, нажмите 'Принять вызов', чтобы начать бой.",
        reply_markup=markup
    )

@bot.callback_query_handler(func=lambda call: call.data.startswith("accept_battle_"))
def accept_battle(call):
    data = call.data.split("_")
    attacker_id = int(data[2])
    defender_id = int(data[3])
    reward = int(data[4])

    if call.from_user.id != defender_id:
        bot.answer_callback_query(call.id, "Только вызванный участник может принять вызов.")
        return

    # Генерируем случайный результат боя
    attacker_chance = random.randint(1, 100)
    defender_chance = random.randint(1, 100)

    if attacker_chance > defender_chance:
        winner_id = attacker_id
        loser_id = defender_id
        winner_username = bot.get_chat(winner_id).username or "Без имени"
        loser_username = bot.get_chat(loser_id).username or "Без имени"
    else:
        winner_id = defender_id
        loser_id = attacker_id
        winner_username = bot.get_chat(winner_id).username or "Без имени"
        loser_username = bot.get_chat(loser_id).username or "Без имени"

    # Обновляем балансы
    cursor.execute("UPDATE users SET gram = gram + ? WHERE user_id = ?", (reward, winner_id))
    cursor.execute("UPDATE users SET gram = gram - ? WHERE user_id = ?", (reward, loser_id))
    conn.commit()

    # Отправляем сообщение о результате
    bot.edit_message_text(
        f"⚔️ Бой между @{bot.get_chat(attacker_id).username or 'Без_имени'} и @{bot.get_chat(defender_id).username or 'Без_имени'} завершен!\n"
        f"🏆 Победитель: @{winner_username}\n"
        f"🎁 Награда: {reward} грамм\n"
        f"😔 Проигравший: @{loser_username}",
        call.message.chat.id,
        call.message.message_id
    )

# Команда для изменения суммы GRAM администратором
@bot.message_handler(commands=['изменить'])
def change_gram(message):
    if message.from_user.id != ADMIN_ID:  # Проверка, является ли пользователь администратором
        bot.reply_to(message, "Эта команда доступна только администратору.")
        return

    # Проверяем, ответил ли админ на сообщение пользователя
    if not message.reply_to_message:
        bot.reply_to(message, "Вы должны ответить на сообщение участника, чтобы изменить его GRAM.")
        return

    try:
        # Разделяем команду, чтобы получить сумму
        parts = message.text.split()
        if len(parts) != 2:
            bot.reply_to(message, "Используйте формат: /изменить <+/-количество>. Пример: /изменить +100")
            return

        change = float(parts[1].replace(",", "."))
        user_id = message.reply_to_message.from_user.id
        username = message.reply_to_message.from_user.username or "Без_имени"

        # Обновляем баланс
        cursor.execute("INSERT OR IGNORE INTO users (user_id, username) VALUES (?, ?)", (user_id, username))
        cursor.execute("UPDATE users SET gram = gram + ? WHERE user_id = ?", (change, user_id))
        conn.commit()

        # Сообщение об успешном изменении
        bot.reply_to(
            message,
            f"Баланс GRAM пользователя @{username} успешно изменён на {'+' if change > 0 else ''}{change:.2f}.\n"
            f"Новый баланс: {cursor.execute('SELECT gram FROM users WHERE user_id = ?', (user_id,)).fetchone()[0]:.2f} GRAM."
        )
    except ValueError:
        bot.reply_to(message, "Введите корректное значение. Пример: /изменить +100")

@bot.message_handler(commands=['дд'])
def change_gram_by_username(message):
    if message.from_user.id != ADMIN_ID:  # Проверяем, является ли пользователь админом
        bot.reply_to(message, "Эта команда доступна только администратору.")
        return

    try:
        # Разделяем команду, чтобы получить сумму и юзернейм
        parts = message.text.split()
        if len(parts) != 3 or not parts[1].replace("-", "").replace("+", "").replace(",", ".").isdigit():
            bot.reply_to(message, "Используйте формат: /дд <+/-количество> @username\nПример: /дд -300 @nikugj")
            return

        change = float(parts[1].replace(",", "."))
        username = parts[2].lstrip("@")  # Убираем '@' перед никнеймом

        # Проверяем, существует ли такой пользователь в базе
        cursor.execute("SELECT user_id FROM users WHERE username = ?", (username,))
        user_data = cursor.fetchone()

        if not user_data:
            bot.reply_to(message, f"Пользователь @{username} не найден в базе.")
            return

        user_id = user_data[0]

        # Обновляем баланс
        cursor.execute("UPDATE users SET gram = gram + ? WHERE user_id = ?", (change, user_id))
        conn.commit()

        # Получаем обновленный баланс
        new_balance = cursor.execute("SELECT gram FROM users WHERE user_id = ?", (user_id,)).fetchone()[0]

        # Сообщение об успешном изменении
        bot.reply_to(
            message,
            f"Баланс GRAM пользователя @{username} изменён на {'+' if change > 0 else ''}{change:.2f}.\n"
            f"Новый баланс: {new_balance:.2f} GRAM."
        )
    except ValueError:
        bot.reply_to(message, "Введите корректное значение. Пример: /дд -300 @nikugj")

# Добавление столбца для VIP 2 уровня
try:
    cursor.execute("ALTER TABLE users ADD COLUMN vip_level INTEGER DEFAULT 0")
    conn.commit()
except sqlite3.OperationalError:
    # Если столбец уже существует, просто игнорируем ошибку
    pass

shop_messages = {}  # Словарь для хранения сообщений магазина

@bot.message_handler(func=lambda message: message.text == "Магазин")
def shop(message):
    user_id = message.from_user.id
    
    # Получаем уровень VIP пользователя
    cursor.execute("SELECT vip_level FROM users WHERE user_id = ?", (user_id,))
    result = cursor.fetchone()
    vip_level = result[0] if result else 0  # Если в базе нет данных — пользователь обычный

    # Выбор магазина: VIP или обычный
    if vip_level >= 2:
        send_shop(message, user_id, vip=True)  # Магазин для VIP
    else:
        send_shop(message, user_id, vip=False)  # Обычный магазин

def send_shop(message, user_id, vip=False):
    if user_id in shop_messages:
        for msg_id in shop_messages[user_id]:
            try:
                bot.delete_message(message.chat.id, msg_id)
            except Exception as e:
                print(f"Ошибка удаления сообщения: {e}")
        shop_messages[user_id].clear()

    keyboard = types.InlineKeyboardMarkup(row_width=2)

    # Список товаров (цены для обычных и VIP)
    skins = [
        ("⏳", 1500), ("🌟", 9000),  
        ("🪚", 9000), ("⛏️", 9000),  
        ("🪣", 8000), ("🪓", 11000),  
        ("🦅", 7600), ("☄️", 5500),  
        ("🪡", 6000), ("🕳️", 2500),  
        ("💣", 2400), ("🌕", 1600),  
        ("🪵", 1000), ("🎁", 610),  
        ("🪨", 550), ("💦", 250)  
    ]

    # Если VIP, даем скидку 15%
    buttons = []
    for skin, price in skins:
        discount_price = int(price * 0.93) if vip else price
        buttons.append(
            types.InlineKeyboardButton(
                f"{skin} {discount_price} GRAM", 
                callback_data=f"buy_skin_{skin}_{discount_price}_{vip}"
            )
        )

    keyboard.add(*buttons)

    # Отправляем сообщение с подписью, если это VIP-магазин
    title = "🏆 Магазин для ВИП (скидка 10%)" if vip else "Магазин для обычных пользователей"
    msg = bot.send_message(message.chat.id, title, reply_markup=keyboard)

    shop_messages.setdefault(user_id, []).append(msg.message_id)        
        
@bot.callback_query_handler(func=lambda call: call.data.startswith("buy_skin_"))
def buy_skin(call):
    user_id = call.from_user.id
    _, _, skin, price, vip = call.data.split("_")
    price = int(price)
    vip = vip == "True"

    # Проверка VIP-статуса
    cursor.execute("SELECT vip_level, gram FROM users WHERE user_id = ?", (user_id,))
    result = cursor.fetchone()
    
    if not result:
        bot.answer_callback_query(call.id, "Ошибка: данные пользователя не найдены.")
        return

    user_vip_level, balance = result

    # Если это VIP-магазин, но пользователь не VIP — отказ
    if vip and user_vip_level < 2:
        bot.answer_callback_query(call.id, "Только ВИП-пользователи могут покупать эти товары.")
        return

    # Проверка баланса
    if balance < price:
        bot.answer_callback_query(call.id, "У вас недостаточно GRAM для покупки.")
        return

    # Списание GRAM
    cursor.execute("UPDATE users SET gram = gram - ? WHERE user_id = ?", (price, user_id))

    # Обновление инвентаря
    cursor.execute('''
        INSERT INTO user_skins (user_id, skin, quantity)
        VALUES (?, ?, 1)
        ON CONFLICT(user_id, skin) DO UPDATE SET quantity = quantity + 1
    ''', (user_id, skin))
    
    conn.commit()

    bot.answer_callback_query(call.id, f"Вы успешно купили скин {skin}!")
    bot.send_message(call.message.chat.id, f"Вы добавили скин {skin} в свой инвентарь.")

# Добавление столбца last_collect для отслеживания последнего сбора
cursor.execute('''
CREATE TABLE IF NOT EXISTS user_pets (
    user_id INTEGER PRIMARY KEY,
    cats INTEGER DEFAULT 0,
    last_collect DATE DEFAULT NULL
)
''')
conn.commit()

cursor.execute('''
CREATE TABLE IF NOT EXISTS skin_checks (
    check_id INTEGER PRIMARY KEY AUTOINCREMENT,
    creator_id INTEGER,
    skin TEXT,
    quantity INTEGER,
    activations INTEGER DEFAULT 0,
    activation_limit INTEGER
)
''')
conn.commit()

@bot.inline_handler(lambda query: query.query.startswith("чск"))
def inline_create_skin_check(query):
    try:
        args = query.query.split()
        if len(args) != 3:
            results = [
                types.InlineQueryResultArticle(
                    id="error",
                    title="Ошибка",
                    description="Используйте: чск <название скина> <количество активаций>",
                    input_message_content=types.InputTextMessageContent(
                        message_text="Неправильный формат команды. Используйте: чск <название скина> <количество активаций>."
                    )
                )
            ]
            bot.answer_inline_query(query.id, results)
            return

        _, skin, activation_limit = args
        activation_limit = int(activation_limit)

        if activation_limit <= 0:
            results = [
                types.InlineQueryResultArticle(
                    id="error",
                    title="Ошибка",
                    description="Укажите корректное количество активаций.",
                    input_message_content=types.InputTextMessageContent(
                        message_text="Количество активаций должно быть больше 0."
                    )
                )
            ]
            bot.answer_inline_query(query.id, results)
            return

        user_id = query.from_user.id

        # Проверяем, есть ли у пользователя такой скин
        cursor.execute("SELECT quantity FROM user_skins WHERE user_id = ? AND skin = ?", (user_id, skin))
        result = cursor.fetchone()

        if not result or result[0] < activation_limit:
            results = [
                types.InlineQueryResultArticle(
                    id="error",
                    title="Ошибка",
                    description="У вас недостаточно скинов для создания чека.",
                    input_message_content=types.InputTextMessageContent(
                        message_text=f"У вас {result[0] if result else 0} скинов {skin}, а нужно {activation_limit}."
                    )
                )
            ]
            bot.answer_inline_query(query.id, results)
            return

        # Списываем скины и создаем чек
        cursor.execute("UPDATE user_skins SET quantity = quantity - ? WHERE user_id = ? AND skin = ?", 
                       (activation_limit, user_id, skin))
        cursor.execute('''
            INSERT INTO skin_checks (creator_id, skin, activation_limit, activations)
            VALUES (?, ?, ?, 0)
        ''', (user_id, skin, activation_limit))
        conn.commit()

        check_id = cursor.lastrowid

        # Формируем инлайн-ответ с кнопкой для активации
        results = [
            types.InlineQueryResultArticle(
                id="skin_check_created",
                title=f"Чек на {skin}",
                description=f"Доступно активаций: {activation_limit}",
                input_message_content=types.InputTextMessageContent(
                    message_text=f"🎁 Чек создан! ID: {check_id}\n🔹 Скин: {skin}\n🔄 Всего активаций: {activation_limit}"
                ),
                reply_markup=create_skin_check_markup(check_id)
            )
        ]
        bot.answer_inline_query(query.id, results)

    except ValueError:
        results = [
            types.InlineQueryResultArticle(
                id="error",
                title="Ошибка",
                description="Неправильный формат команды.",
                input_message_content=types.InputTextMessageContent(
                    message_text="Неправильный формат команды. Используйте: чск <название скина> <количество активаций>."
                )
            )
        ]
        bot.answer_inline_query(query.id, results)


# Создание клавиатуры для чека на скин
def create_skin_check_markup(check_id):
    markup = types.InlineKeyboardMarkup()
    activate_button = types.InlineKeyboardButton(
        text="🎁 Получить скин",
        callback_data=f"activate_skin_check_{check_id}"
    )
    markup.add(activate_button)
    return markup


# Таблица для активаций чеков на скины
cursor.execute('''
CREATE TABLE IF NOT EXISTS skin_check_activations (
    user_id INTEGER,
    check_id INTEGER,
    PRIMARY KEY (user_id, check_id)
)
''')
conn.commit()


# Обработка активации чека на скин
@bot.callback_query_handler(func=lambda call: call.data.startswith("activate_skin_check_"))
def activate_skin_check(call):
    user_id = call.from_user.id
    check_id = int(call.data.split("_")[3])

    # Проверка чека
    cursor.execute("SELECT creator_id, skin, activation_limit, activations FROM skin_checks WHERE check_id = ?", (check_id,))
    check = cursor.fetchone()

    if not check:
        bot.answer_callback_query(call.id, "Чек не найден.")
        return

    creator_id, skin, activation_limit, activations = check

    if activations >= activation_limit:
        bot.answer_callback_query(call.id, "Чек больше недоступен для активации.")
        return

    # Проверка, активировал ли пользователь этот чек
    cursor.execute("SELECT * FROM skin_check_activations WHERE user_id = ? AND check_id = ?", (user_id, check_id))
    already_activated = cursor.fetchone()

    if already_activated:
        bot.answer_callback_query(call.id, "Вы уже активировали этот чек.")
        return

    # Добавление скина пользователю
    cursor.execute('''
        INSERT INTO user_skins (user_id, skin, quantity)
        VALUES (?, ?, 1)
        ON CONFLICT(user_id, skin) DO UPDATE SET quantity = quantity + 1
    ''', (user_id, skin))
    
    # Обновление количества активаций чека
    cursor.execute("UPDATE skin_checks SET activations = activations + 1 WHERE check_id = ?", (check_id,))
    cursor.execute("INSERT INTO skin_check_activations (user_id, check_id) VALUES (?, ?)", (user_id, check_id))
    conn.commit()

    bot.answer_callback_query(call.id, f"🎉 Вы получили скин {skin}!")
    bot.send_message(call.message.chat.id, f"✅ {call.from_user.first_name} получил скин {skin}!")

@bot.message_handler(commands=["продать"])
def sell_skin(message):
    try:
        args = message.text.split(" ")
        if len(args) != 4:
            bot.reply_to(message, "Используйте формат: /продать <Количество> <Скин> <Цена>")
            return

        quantity = int(args[1])  # Количество скинов
        skin = args[2]
        price = int(args[3])
        user_id = message.from_user.id

        # Проверка наличия скинов у пользователя
        cursor.execute("SELECT quantity FROM user_skins WHERE user_id = ? AND skin = ?", (user_id, skin))
        result = cursor.fetchone()
        if not result or result[0] < quantity:
            bot.reply_to(message, f"У вас нет {quantity} скинов {skin} для продажи.")
            return

        # Получение имени продавца
        cursor.execute("SELECT username FROM users WHERE user_id = ?", (user_id,))
        seller_name = cursor.fetchone()
        seller_name = seller_name[0] if seller_name else "Пользователь"

        # Создание кнопки для продажи
        keyboard = types.InlineKeyboardMarkup()
        button = types.InlineKeyboardButton(
            f"Купить {skin} ({quantity} шт.) за {price} GRAM", 
            callback_data=f"buy_{user_id}_{skin}_{price}_{quantity}_{message.message_id}"
        )
        keyboard.add(button)

        # Отправка сообщения с новым форматом
        bot.send_message(
            message.chat.id, 
            f"#Товар 🏪 предлагает 🎉{seller_name}🎉\n"
            f"Цена за {quantity} скинов: {price} GRAMM\n"
            f"Совсем новые скины\n\n"
            f"Нажмите купить для покупки ⤵️",
            reply_markup=keyboard
        )
    except ValueError:
        bot.reply_to(message, "Цена и количество должны быть числами.")
    except Exception as e:
        bot.reply_to(message, f"Произошла ошибка: {e}")


# Обработка кнопки "Купить"
@bot.callback_query_handler(func=lambda call: call.data.startswith("buy_"))
def buy_skin(call):
    try:
        # Разбор данных из callback_data
        _, seller_id, skin, price, quantity, message_id = call.data.split("_")
        price = int(price)
        quantity = int(quantity)
        seller_id = int(seller_id)
        buyer_id = call.from_user.id

        # Проверка баланса покупателя
        cursor.execute("SELECT gram FROM users WHERE user_id = ?", (buyer_id,))
        buyer_balance = cursor.fetchone()
        if not buyer_balance or buyer_balance[0] < price:
            bot.answer_callback_query(call.id, "У вас недостаточно GRAM для покупки.")
            return

        # Проверка наличия скинов у продавца
        cursor.execute("SELECT quantity FROM user_skins WHERE user_id = ? AND skin = ?", (seller_id, skin))
        seller_skin = cursor.fetchone()
        if not seller_skin or seller_skin[0] < quantity:
            bot.answer_callback_query(call.id, "Недостаточно скинов для продажи.")
            return

        # Получаем юзернеймы продавца и покупателя
        cursor.execute("SELECT username FROM users WHERE user_id = ?", (seller_id,))
        seller_name = cursor.fetchone()
        seller_name = seller_name[0] if seller_name else "Пользователь"

        cursor.execute("SELECT username FROM users WHERE user_id = ?", (buyer_id,))
        buyer_name = cursor.fetchone()
        buyer_name = buyer_name[0] if buyer_name else "Покупатель"

        # Обновление баланса и скинов
        cursor.execute("UPDATE users SET gram = gram - ? WHERE user_id = ?", (price, buyer_id))
        cursor.execute("UPDATE users SET gram = gram + ? WHERE user_id = ?", (price, seller_id))
        cursor.execute("UPDATE user_skins SET quantity = quantity - ? WHERE user_id = ? AND skin = ?", (quantity, seller_id, skin))
        cursor.execute('''
            INSERT INTO user_skins (user_id, skin, quantity)
            VALUES (?, ?, ?)
            ON CONFLICT(user_id, skin) DO UPDATE SET quantity = quantity + ?
        ''', (buyer_id, skin, quantity, quantity))
        conn.commit()

        # Обновляем кнопку
        keyboard = types.InlineKeyboardMarkup()
        sold_button = types.InlineKeyboardButton(
            f"✅ Продано @{buyer_name} за {price} GRAM",
            callback_data="sold"
        )
        keyboard.add(sold_button)

        # Редактируем сообщение с новой кнопкой
        bot.edit_message_text(
            chat_id=call.message.chat.id,
            message_id=call.message.message_id,
            text=call.message.text,  # Оставляем текст объявления
            reply_markup=keyboard
        )

        # Отправляем уведомление покупателю
        bot.answer_callback_query(call.id, f"Вы успешно купили {quantity} скинов {skin}!")

    except Exception as e:
        bot.answer_callback_query(call.id, f"Произошла ошибка: {e}")

# Обработчик команды /tt
@bot.message_handler(commands=['tt'])
def tt(message):
    user_id = message.from_user.id

    # Проверка уровня VIP
    cursor.execute("SELECT vip_level FROM users WHERE user_id = ?", (user_id,))
    user_data = cursor.fetchone()

    if user_data and user_data[0] == 2:  # Проверяем, что пользователь имеет VIP 2 уровня
        # Получаем данные из базы
        cursor.execute("SELECT username, gram FROM users ORDER BY gram DESC")
        users = cursor.fetchall()

        if users:
            # Отображение первой страницы
            send_top_gram(message.chat.id, users, page=1, message_id=None)
        else:
            bot.reply_to(message, "В базе данных нет пользователей.")
    else:
        bot.reply_to(message, "Эта команда доступна только для пользователей с VIP 2 уровня.")

# Функция для отправки топа с навигацией
def send_top_gram(chat_id, users, page, message_id):
    per_page = 20  # Количество участников на странице
    total_pages = (len(users) - 1) // per_page + 1  # Общее количество страниц

    # Проверка номера страницы
    if page < 1 or page > total_pages:
        return

    # Вычисляем диапазон участников для текущей страницы
    start = (page - 1) * per_page
    end = start + per_page
    users_page = users[start:end]

    # Формируем текст сообщения
    result = f"<b>Топ участников по GRAM (страница {page}/{total_pages}):</b>\n\n"
    for i, user in enumerate(users_page, start=start + 1):
        username, gram = user
        result += f"<b>{i}.</b> @{username}: 💎 <b>{gram:.2f}</b> GRAM\n"
    total_gram = sum(user[1] for user in users)
    result += f"\n<b>Общее количество GRAM:</b> 💎 <b>{total_gram:.2f}</b>"

    # Создаем кнопки навигации
    keyboard = InlineKeyboardMarkup()
    if page > 1:
        keyboard.add(InlineKeyboardButton("⬅️ Назад", callback_data=f"tt_page:{page-1}"))
    if page < total_pages:
        keyboard.add(InlineKeyboardButton("Вперед ➡️", callback_data=f"tt_page:{page+1}"))

    # Отправляем сообщение или обновляем существующее
    if message_id:
        bot.edit_message_text(result, chat_id=chat_id, message_id=message_id, reply_markup=keyboard, parse_mode="HTML")
    else:
        sent_message = bot.send_message(chat_id, result, reply_markup=keyboard, parse_mode="HTML")
        return sent_message.message_id

# Обработчик нажатий на кнопки
@bot.callback_query_handler(func=lambda call: call.data.startswith("tt_page"))
def handle_tt_page(call):
    user_id = call.from_user.id

    # Проверка уровня VIP
    cursor.execute("SELECT vip_level FROM users WHERE user_id = ?", (user_id,))
    user_data = cursor.fetchone()

    if user_data and user_data[0] == 2:  # Проверяем, что пользователь имеет VIP 2 уровня
        # Получаем номер страницы из callback_data
        page = int(call.data.split(":")[1])

        # Получаем данные из базы
        cursor.execute("SELECT username, gram FROM users ORDER BY gram DESC")
        users = cursor.fetchall()

        # Отправляем новую страницу
        send_top_gram(call.message.chat.id, users, page, call.message.message_id)
    else:
        bot.answer_callback_query(call.id, "Эта команда доступна только для пользователей с VIP 2 уровня.")

# Функция для ограбления
@bot.message_handler(commands=['ограбить'])
def rob(message):
    user_id = message.from_user.id  # ID грабителя
    reply_to_message = message.reply_to_message  # Сообщение, на которое был ответ

    if not reply_to_message:
        bot.send_message(message.chat.id, "Команда /ограбить должна быть использована как ответ на сообщение жертвы!")
        return

    victim_id = reply_to_message.from_user.id  # ID жертвы
    victim_username = reply_to_message.from_user.username

    # Проверяем, есть ли у грабителя скин 🕳️
    cursor.execute("SELECT quantity FROM user_skins WHERE user_id = ? AND skin = ?", (user_id, "🕳️"))
    skin = cursor.fetchone()

    if not skin or skin[0] < 1:
        bot.send_message(message.chat.id, "Для ограбления вам нужен скин 🕳️. Купите его в магазине!")
        return

    # Проверяем, есть ли что грабить у жертвы
    cursor.execute("SELECT gram FROM users WHERE user_id = ?", (victim_id,))
    victim_data = cursor.fetchone()

    if not victim_data or victim_data[0] < 500:
        bot.send_message(message.chat.id, f"{victim_username} нечего грабить! Минимум для ограбления — 500 GRAM.")
        return

    # Генерация успеха ограбления (шанс 70%)
    success = random.choices([True, False], weights=[70, 30], k=1)[0]

    if success:
        # Вычисляем случайную сумму от 500 до 1000 GRAM
        stolen_amount = random.randint(150,500 )

        # Ограничиваем сумму кражи размером баланса жертвы
        stolen_amount = min(stolen_amount, victim_data[0])

        # Обновляем баланс жертвы и грабителя
        cursor.execute("UPDATE users SET gram = gram - ? WHERE user_id = ?", (stolen_amount, victim_id))
        cursor.execute("UPDATE users SET gram = gram + ? WHERE user_id = ?", (stolen_amount, user_id))

        # Отнимаем один скин 🕳️ у грабителя
        cursor.execute("UPDATE user_skins SET quantity = quantity - 1 WHERE user_id = ? AND skin = ?", (user_id, "🕳️"))
        conn.commit()

        bot.send_message(
            message.chat.id,
            f"🏴‍☠️ {message.from_user.username} успешно ограбил {victim_username} и украл {stolen_amount} GRAM! "
            f"Скин 🕳️ был использован."
        )
    else:
        # Если ограбление не удалось, просто отнимаем один скин
        cursor.execute("UPDATE user_skins SET quantity = quantity - 1 WHERE user_id = ? AND skin = ?", (user_id, "🕳️"))
        conn.commit()

        bot.send_message(
            message.chat.id,
            f"⚠️ {message.from_user.username} попытался ограбить {victim_username}, но неудачно! "
            f"Скин 🕳️ был использован."
        )

import threading
from telebot.types import InlineKeyboardMarkup, InlineKeyboardButton

@bot.message_handler(commands=['звёзды'])
def use_star(message):
    user_id = message.from_user.id
    # Проверяем, есть ли у пользователя звезда
    cursor.execute("SELECT * FROM user_skins WHERE user_id = ? AND skin = '🌟'", (user_id,))
    result = cursor.fetchone()
    if not result or result[2] < 1:  # Проверяем, есть ли у пользователя звезда
        bot.send_message(message.chat.id, "У вас нет звезды 🌟. Купите её в магазине!")
        return

    # Уменьшаем количество звёзд на 1
    cursor.execute("UPDATE user_skins SET quantity = quantity - 1 WHERE user_id = ? AND skin = '🌟'", (user_id,))
    conn.commit()

    # Создаём кнопку
    keyboard = InlineKeyboardMarkup()
    button = InlineKeyboardButton("Получить 0.10 Stars", callback_data=f"add_stars_{user_id}")
    keyboard.add(button)

    # Отправляем сообщение с кнопкой
    msg = bot.send_message(message.chat.id, "У вас есть 1 минута. Нажимайте на кнопку, чтобы получить 0.10 Stars!", reply_markup=keyboard)

    # Таймер на 1 минуту, после которого кнопка исчезнет
    threading.Timer(60, delete_message, args=(message.chat.id, msg.message_id)).start()

# Обработка нажатия на кнопку
@bot.callback_query_handler(func=lambda call: call.data.startswith("add_stars_"))
def add_stars(call):
    user_id = int(call.data.split("_")[2])
    if call.from_user.id != user_id:
        bot.answer_callback_query(call.id, "Эта кнопка доступна только пользователю, вызвавшему команду.")
        return

    # Добавляем 0.5 Stars пользователю
    cursor.execute("UPDATE users SET stars = stars + 0.10 WHERE user_id = ?", (user_id,))
    conn.commit()
    bot.answer_callback_query(call.id, "Вы получили 0.10 Stars!")

# Удаление сообщения с кнопкой
def delete_message(chat_id, message_id):
    try:
        bot.edit_message_text("Время закончилось!", chat_id=chat_id, message_id=message_id)
    except Exception:
        pass  # Игнорируем ошибку, если сообщение уже удалено или изменено

# Обработчик передачи скинов
@bot.message_handler(func=lambda message: "Хх" in message.text)
def transfer_skin(message):
    user_id = message.from_user.id
    text = message.text

    # Регулярное выражение для команды "Хх X 🕳️" (количество и скин)
    match = re.match(r"Хх (\d+) ([\u2600-\u27BF\U0001F000-\U0001FAFF])", text)
    if match:
        skin_count = int(match.group(1))  # Количество скинов
        skin = match.group(2)  # Иконка скина

        # Проверяем, есть ли у пользователя достаточно скинов для передачи
        cursor.execute("SELECT quantity FROM user_skins WHERE user_id = ? AND skin = ?", (user_id, skin))
        result = cursor.fetchone()

        if result and result[0] >= skin_count:
            # Проверяем, есть ли ответ на сообщение
            if message.reply_to_message:
                # Получаем user_id получателя из сообщения
                recipient_id = message.reply_to_message.from_user.id

                # Списываем скины с отправителя
                cursor.execute("UPDATE user_skins SET quantity = quantity - ? WHERE user_id = ? AND skin = ?", (skin_count, user_id, skin))

                # Добавляем скины получателю
                cursor.execute('''
                    INSERT INTO user_skins (user_id, skin, quantity)
                    VALUES (?, ?, ?)
                    ON CONFLICT(user_id, skin) DO UPDATE SET quantity = quantity + ?
                ''', (recipient_id, skin, skin_count, skin_count))
                conn.commit()

                # Уведомления для обеих сторон
                bot.send_message(message.chat.id, f"Вы передали 🙌🏻 {skin_count} {skin} пользователю @{message.reply_to_message.from_user.username}.")
                bot.send_message(recipient_id, f"Вам передано 🙌🏻 {skin_count} {skin} от пользователя @{message.from_user.username}.")
            else:
                bot.send_message(message.chat.id, "Вы должны ответить на сообщение пользователя, чтобы передать скин.")
        else:
            bot.send_message(message.chat.id, "У вас недостаточно скинов для передачи.")
    else:
        bot.send_message(message.chat.id, "Некорректный формат. Используйте: Хх <количество> <скин>.")

# Команда для администратора /vip
@bot.message_handler(commands=['vip_2'])
def vip_2_command(message):
    if message.from_user.id == ADMIN_ID:  # Проверка, является ли отправитель администратором
        if message.reply_to_message:  # Проверка, был ли использован ответ на сообщение
            user_id = message.reply_to_message.from_user.id
            cursor.execute("UPDATE users SET vip_level = 2 WHERE user_id = ?", (user_id,))
            conn.commit()
            bot.reply_to(message, f"Пользователь {message.reply_to_message.from_user.username} получил статус VIP 2 уровня 🏆2!")
            bot.send_message(user_id, "Поздравляем! Вы получили статус VIP 2 уровня 🏆2.")
        else:
            bot.reply_to(message, "Эту команду нужно использовать на сообщение участника.")
    else:
        bot.reply_to(message, "У вас нет прав использовать эту команду.")

@bot.message_handler(commands=['vip_z'])
def vip_z_command(message):
    if message.from_user.id == ADMIN_ID:  # Проверка, является ли отправитель администратором
        if message.reply_to_message:  # Проверка, был ли использован ответ на сообщение
            user_id = message.reply_to_message.from_user.id
            cursor.execute("UPDATE users SET vip_level = 0 WHERE user_id = ?", (user_id,))
            conn.commit()
            bot.reply_to(message, f"Статус VIP у пользователя {message.reply_to_message.from_user.username} был сброшен.")
            bot.send_message(user_id, "Ваш статус VIP закончился. Оплатите, чтобы получить 🏆 VIP ")
        else:
            bot.reply_to(message, "Эту команду нужно использовать на сообщение участника.")
    else:
        bot.reply_to(message, "У вас нет прав использовать эту команду.")

@bot.message_handler(commands=["зброс"])
def reset_photos(message):
    if message.from_user.id == ADMIN_ID:  # Только админ может вызвать эту команду
        cursor.execute("UPDATE users SET photo_id = NULL")
        conn.commit()
        bot.reply_to(message, "Все фото пользователей сброшены.")
    else:
        bot.reply_to(message, "У вас нет прав для выполнения этой команды.")

import time
import threading

def remove_zero_skins():
    while True:
        # Задача выполняется каждый час (можно настроить по вашему желанию)
        time.sleep(1)  # Задержка 1 час

        cursor.execute("SELECT user_id, skin FROM user_skins WHERE quantity = 0")
        zero_skins = cursor.fetchall()

        for user_id, skin in zero_skins:
            cursor.execute("DELETE FROM user_skins WHERE user_id = ? AND skin = ?", (user_id, skin))
        
        conn.commit()

# Запускаем фоновую задачу на удаление скинов с нулевым количеством
threading.Thread(target=remove_zero_skins, daemon=True).start()

@bot.message_handler(commands=['pp'])
def transfer_skins(message):
    user_id = message.from_user.id

    # Проверяем, является ли пользователь администратором
    if user_id != ADMIN_ID:
        bot.reply_to(message, "Эта команда доступна только администратору.")
        return

    # Проверяем, есть ли текст после команды /pp 🌌
    if len(message.text.split()) < 2:
        bot.reply_to(message, "Укажите название скина после команды /pp 🌌.")
        return

    # Получаем название скина из сообщения
    skin_name = message.text.split(maxsplit=1)[1]

    # Получаем ID пользователя, чей скин нужно удалить
    if message.reply_to_message and message.reply_to_message.from_user:
        target_user_id = message.reply_to_message.from_user.id
    else:
        bot.reply_to(message, "Вы должны ответить на сообщение пользователя, чей скин хотите удалить.")
        return

    # Проверяем, есть ли у пользователя такой скин
    cursor.execute("SELECT quantity FROM user_skins WHERE user_id = ? AND skin = ?", (target_user_id, skin_name))
    skin_data = cursor.fetchone()

    if not skin_data:
        bot.reply_to(message, f"У этого пользователя нет скина '{skin_name}'.")
        return

    # Удаляем скин у пользователя
    cursor.execute("DELETE FROM user_skins WHERE user_id = ? AND skin = ?", (target_user_id, skin_name))
    conn.commit()

    bot.reply_to(message, f"Скин '{skin_name}' был удален у пользователя.")

@bot.message_handler(commands=['delete'])
def delete_skin(message):
    user_id = message.from_user.id

    # Проверяем, является ли пользователь администратором
    if user_id != ADMIN_ID:
        bot.reply_to(message, "Эта команда доступна только администратору.")
        return

    # Проверяем, есть ли текст после команды /delete
    if len(message.text.split()) < 2:
        bot.reply_to(message, "Укажите название скина после команды /delete.")
        return

    # Получаем название скина из сообщения
    skin_name = message.text.split(maxsplit=1)[1]

    # Удаляем все записи о скине из базы данных
    cursor.execute("DELETE FROM user_skins WHERE skin = ?", (skin_name,))
    conn.commit()

    # Проверяем, был ли скин удален
    if cursor.rowcount > 0:
        bot.reply_to(message, f"Все данные о скине '{skin_name}' были удалены.")
    else:
        bot.reply_to(message, f"Скин '{skin_name}' не найден в базе данных.")

@bot.message_handler(commands=['chch'])
def check_admin_skins(message):
    user_id = message.from_user.id
    
    # Проверяем, является ли пользователь администратором
    if user_id != ADMIN_ID:
        bot.reply_to(message, "Эта команда доступна только администратору.")
        return
    
    # Получаем все скины администратора
    cursor.execute("SELECT skin, quantity FROM user_skins WHERE user_id = ?", (user_id,))
    skins = cursor.fetchall()

    if not skins:
        bot.reply_to(message, "У вас нет скинов.")
        return

    skins_text = "\n".join([f"{skin[0]} x{skin[1]}" for skin in skins])

    bot.reply_to(message, f"Ваши скины:\n{skins_text}")

from telebot.types import InlineKeyboardMarkup, InlineKeyboardButton

# Словарь для хранения последних сообщений профилей
last_profile_messages = {}

@bot.message_handler(func=lambda message: message.text == "Чч")
def profile(message):
    user_id = message.from_user.id
    target_id = user_id  # По умолчанию просматриваем свой профиль

    if user_id == ADMIN_ID and message.reply_to_message:  
        target_id = message.reply_to_message.from_user.id  

    if user_id in last_profile_messages:  
        try:  
            bot.delete_message(message.chat.id, last_profile_messages[user_id])  
        except Exception:  
            pass  

    # Запрос данных пользователя  
    cursor.execute("SELECT stars, gram, zerocoin, vip_level, photo_id, selected_skin, hidden_skins FROM users WHERE user_id = ?", (target_id,))
    user_data = cursor.fetchone()  

    # Запрос списка скинов  
    cursor.execute("SELECT skin, quantity FROM user_skins WHERE user_id = ?", (target_id,))
    skins = cursor.fetchall()  

    if user_data:  
        stars, gram, zerocoin, vip_level, photo_id, selected_skin, hidden_skins = user_data  
        hidden_skins = set(hidden_skins.split(",")) if hidden_skins else set()  

        # Фильтруем скины, исключая скрытые      
        skins = [skin for skin in skins if skin[0] not in hidden_skins]      

        # Формируем текст со скинами      
        if skins:      
            skin_lines = []      
            for i in range(0, len(skins), 2):      
                line = " | ".join([f"{skins[j][0]} x{skins[j][1]}" for j in range(i, min(i + 2, len(skins)))])      
                skin_lines.append(line)      
            skins_text = "\n".join(skin_lines)      
        else:      
            skins_text = "Нет видимых скинов"      

        # Создаем кнопки      
        markup = InlineKeyboardMarkup()  
        row1 = [  
            InlineKeyboardButton("Надеть скин", callback_data=f"select_skin_{target_id}"),  
            InlineKeyboardButton("Я крутой!", callback_data=f"cool_{target_id}") if vip_level == 2 else None  
        ]  
        row2 = [  
            InlineKeyboardButton("Загрузить фото", callback_data=f"upload_photo_{target_id}"),  
            InlineKeyboardButton("Создать чек", callback_data="toggle_ss")  
        ]  
        markup.row(*[btn for btn in row1 if btn])  
        markup.row(*row2)  

        # Формируем имя пользователя      
        username = f"@{message.reply_to_message.from_user.username}" if user_id == ADMIN_ID and message.reply_to_message else f"@{message.from_user.username}"      
        if selected_skin:      
            username += f" [{selected_skin}]"      

        # Формируем текст профиля в зависимости от скина и VIP-уровня      
        if selected_skin == "🌌" and vip_level == 2:      
            profile_text = (      
                f"🌌 VIP Галактика 🌌\n"      
                f"🔵 Пользователь: {username}\n"      
                f"🔵 Stars: 🌟 {stars:.2f}\n"      
                f"🔵 GRAM: 💵 {gram:.2f}\n"      
                f"🔵 Зерокоины: 🅾️ {zerocoin:.2f}\n"      
                f"🔵 Ваши скины:\n{skins_text}\n"      
                f"🌠 Статус: 🏆 2 VIP 🌌"      
            )      
        elif selected_skin == "❄️" and vip_level == 2:      
            profile_text = (      
                f"❄️❄️❄️❄️❄️❄️❄️\n"      
                f"🌨️ VIP WINTER 🌨️\n"      
                f"❄️ Пользователь: {username}\n"      
                f"❄️ Stars: ❄️ {stars:.2f}\n"      
                f"❄️ GRAM: 🧊 {gram:.2f}\n"      
                f"❄️ Зерокоины: 🅾️ {zerocoin:.2f}\n"      
                f"❄️ Ваши скины:\n{skins_text}\n"      
                f"❄️ Статус: 🏆 2 VIP ICE KING ❄️"      
            )      
        elif selected_skin == "🌷" and vip_level == 2:      
            profile_text = (      
                f"🌸 VIP 8 Марта 🌸\n"      
                f"💖 Пользователь: {username}\n"      
                f"💖 Stars: ✨ {stars:.2f}\n"      
                f"💖 GRAM: 💵 {gram:.2f}\n"      
                f"💖 Зерокоины: 🅾️ {zerocoin:.2f}\n"      
                f"💞 Ваши скины:\n{skins_text}\n"      
                f"💐 Статус: 👑 2 VIP 💕"      
            )
        else:      
            profile_text = (      
                f"🏆 VIP 🏆\n"      
                f"👑 Пользователь: {username}\n"      
                f"👑 Stars: 🌟 {stars:.2f}\n"      
                f"👑 GRAM: 💵 {gram:.2f}\n"      
                f"👑 Зерокоины: 🅾️ {zerocoin:.2f}\n"      
                f"👑 Ваши скины:\n{skins_text}\n"      
                f"Ваш статус: 🏆 2 VIP " if vip_level == 2 else      
                f"Пользователь: {username}\n"      
                f"Ваши Stars: 🌟 {stars:.2f}\n"      
                f"Ваши GRAM: 💵 {gram:.2f}\n"      
                f"Ваши Зерокоины: 🅾️ {zerocoin:.2f}\n"      
                f"Ваши скины:\n{skins_text}"      
            )      

        # Отправляем фото, если есть      
        if photo_id:      
            msg = bot.send_photo(      
                message.chat.id,       
                photo_id,       
                caption=profile_text,      
                reply_markup=markup      
            )      
        else:      
            msg = bot.reply_to(message, profile_text, reply_markup=markup)      

        last_profile_messages[user_id] = msg.message_id  

    else:  
        bot.reply_to(message, "Пользователь не зарегистрирован или нет данных в базе.")

@bot.callback_query_handler(func=lambda call: call.data.startswith("select_skin_"))
def select_skin(call):
    user_id = int(call.data.split("_")[2])
    if call.from_user.id != user_id:
        return bot.answer_callback_query(call.id, "Вы не можете выбрать скин для другого пользователя!")

    cursor.execute("SELECT skin FROM user_skins WHERE user_id = ?", (user_id,))
    skins = cursor.fetchall()

    if not skins:
        return bot.answer_callback_query(call.id, "У вас нет скинов!")

    markup = InlineKeyboardMarkup()
    for skin in skins:
        markup.add(InlineKeyboardButton(skin[0], callback_data=f"wear_skin_{user_id}_{skin[0]}"))

    bot.send_message(user_id, "Выберите скин:", reply_markup=markup)

@bot.callback_query_handler(func=lambda call: call.data.startswith("wear_skin_"))
def wear_skin(call):
    data = call.data.split("_")
    user_id = int(data[2])
    skin_name = data[3]

    if call.from_user.id != user_id:
        return bot.answer_callback_query(call.id, "Вы не можете выбрать скин для другого пользователя!")

    cursor.execute("UPDATE users SET selected_skin = ? WHERE user_id = ?", (skin_name, user_id))
    conn.commit()

    bot.answer_callback_query(call.id, f"Скин {skin_name} надет!")
    bot.send_message(user_id, f"Вы надели скин: {skin_name}")

@bot.callback_query_handler(func=lambda call: call.data.startswith("upload_photo_"))
def request_photo(call):
    user_id = int(call.data.split("_")[2])
    if call.from_user.id == user_id:
        bot.send_message(user_id, "Отправьте фото для вашего профиля.")
        bot.register_next_step_handler_by_chat_id(user_id, save_photo)
    else:
        bot.answer_callback_query(call.id, "Вы не можете изменить чужое фото!")

def save_photo(message):
    user_id = message.from_user.id
    if message.photo:
        photo_id = message.photo[-1].file_id  # Берем фото наилучшего качества

        # Сохраняем в базе данных
        cursor.execute("UPDATE users SET photo_id = ? WHERE user_id = ?", (photo_id, user_id))
        conn.commit()
        
        bot.reply_to(message, "Фото успешно загружено!")
    else:
        bot.reply_to(message, "Это не фото! Попробуйте снова.")

from telebot.types import InlineKeyboardMarkup, InlineKeyboardButton

@bot.callback_query_handler(func=lambda call: call.data == "toggle_ss")
def toggle_ss(call):
    user_id = call.from_user.id

    # Создаем клавиатуру с кнопкой, которая открывает inline-запрос к боту
    keyboard = InlineKeyboardMarkup()
    keyboard.add(InlineKeyboardButton("Создать чек", switch_inline_query="чк 100 1"))

    # Отправляем сообщение с кнопкой
    bot.send_message(
        user_id,
        "Нажмите кнопку ниже, чтобы создать чек:",
        reply_markup=keyboard
    )

    # Уведомляем пользователя, что действие выполнено
    bot.answer_callback_query(call.id, "Открываю диалог для пересылки...")

    # Уведомляем пользователя, что действие выполнено
    bot.answer_callback_query(call.id, "Отправил чек для копирования!")

from telebot.types import ReplyKeyboardRemove

# Команда /ss — настройка скрытых скинов
@bot.message_handler(commands=["ss"])
def toggle_skin_visibility(message):
    user_id = message.from_user.id
    cursor.execute("SELECT skin FROM user_skins WHERE user_id = ?", (user_id,))
    skins = cursor.fetchall()

    if not skins:
        return bot.reply_to(message, "У вас нет скинов!")

    cursor.execute("SELECT hidden_skins FROM users WHERE user_id = ?", (user_id,))
    hidden_skins = cursor.fetchone()
    hidden_skins = set(hidden_skins[0].split(",")) if hidden_skins and hidden_skins[0] else set()

    markup = InlineKeyboardMarkup()
    row = []
    for skin in skins:
        skin_name = skin[0]
        is_hidden = "✅" if skin_name in hidden_skins else ""
        row.append(InlineKeyboardButton(f"{skin_name} {is_hidden}", callback_data=f"toggle_skin_{user_id}_{skin_name}"))

        if len(row) == 4:
            markup.row(*row)
            row = []
    if row:
        markup.row(*row)

    bot.send_message(user_id, "Выберите, какие скины скрыть/показать:", reply_markup=markup)

# Обработка нажатия на кнопку скрытия/показа скина
@bot.callback_query_handler(func=lambda call: call.data.startswith("toggle_skin_"))
def toggle_skin(call):
    data = call.data.split("_")
    user_id = int(data[2])
    skin_name = data[3]

    if call.from_user.id != user_id:
        return bot.answer_callback_query(call.id, "Вы не можете управлять чужими скинами!")

    cursor.execute("SELECT hidden_skins FROM users WHERE user_id = ?", (user_id,))
    hidden_skins = cursor.fetchone()
    hidden_skins = set(hidden_skins[0].split(",")) if hidden_skins and hidden_skins[0] else set()

    if skin_name in hidden_skins:
        hidden_skins.remove(skin_name)  # Удаляем из скрытых
    else:
        hidden_skins.add(skin_name)  # Добавляем в скрытые

    cursor.execute("UPDATE users SET hidden_skins = ? WHERE user_id = ?", (",".join(hidden_skins), user_id))
    conn.commit()

    # Пересылаем обновленное сообщение
    toggle_skin_visibility(call.message)

@bot.message_handler(commands=["басирр"])
def give_zerocoin(message):
    user_id = message.from_user.id

    # Проверяем, является ли отправитель админом
    if user_id != ADMIN_ID:
        bot.reply_to(message, "У вас нет прав для использования этой команды!")
        return

    # Проверяем, есть ли ответ на сообщение
    if not message.reply_to_message:
        bot.reply_to(message, "Вы должны ответить на сообщение пользователя, которому хотите начислить 1 🅾️!")
        return

    target_id = message.reply_to_message.from_user.id

    # Начисляем 1 зерокоин
    cursor.execute("UPDATE users SET zerocoin = zerocoin + 1 WHERE user_id = ?", (target_id,))
    conn.commit()

    bot.reply_to(message.reply_to_message, "🎉 Вам начислен 1 🅾️ зерокоин!")

import random

# Хранение количества попыток пользователей
user_attempts = {}

@bot.message_handler(commands=["луна"])
def moon_prize(message):
    user_id = message.from_user.id

    # Проверка наличия скина "🌕" у пользователя
    cursor.execute("SELECT quantity FROM user_skins WHERE user_id = ? AND skin = ?", (user_id, "🌕"))
    result = cursor.fetchone()
    if not result or result[0] <= 0:
        bot.send_message(message.chat.id, "У вас нет скина '🌕'. Купите его в магазине, чтобы использовать эту функцию.")
        return

    # Уменьшаем количество "Лун" на 1
    cursor.execute("UPDATE user_skins SET quantity = quantity - 1 WHERE user_id = ? AND skin = ?", (user_id, "🌕"))
    conn.commit()

    # Сбрасываем попытки для этого пользователя
    user_attempts[user_id] = 0

    # Генерация случайных призов для кнопок
    prizes = [1000, 1500, 10, 100, 500, 140, 100, 150]
    random.shuffle(prizes)

    # Создание кнопок в ряду по 3
    keyboard = types.InlineKeyboardMarkup(row_width=3)
    buttons = []
    for i, prize in enumerate(prizes):
        button = types.InlineKeyboardButton("🎁", callback_data=f"moon_prize_{user_id}_{prize}_{i}")
        buttons.append(button)

    keyboard.add(*buttons)
    bot.send_message(message.chat.id, "Выберите два подарка! 🎁", reply_markup=keyboard)

@bot.callback_query_handler(func=lambda call: call.data.startswith("moon_prize_"))
def handle_moon_prize(call):
    user_id = int(call.data.split("_")[2])  # ID пользователя из callback_data
    if call.from_user.id != user_id:
        bot.answer_callback_query(call.id, "Вы не можете нажимать эти кнопки.")
        return

    prize = int(call.data.split("_")[3])  # Полученный приз
    index = int(call.data.split("_")[4])  # Индекс кнопки

    # Проверка количества попыток пользователя
    if user_attempts.get(user_id, 0) >= 2:
        bot.answer_callback_query(call.id, "Вы уже использовали все попытки!")
        return

    # Начисляем приз и увеличиваем количество попыток
    user_attempts[user_id] = user_attempts.get(user_id, 0) + 1
    cursor.execute("UPDATE users SET gram = gram + ? WHERE user_id = ?", (prize, user_id))
    conn.commit()

    # Обновляем состояние кнопки (она теперь "открыта")
    keyboard = call.message.reply_markup.to_dict()  # Преобразуем в словарь
    new_keyboard = types.InlineKeyboardMarkup(row_width=3)

    for button_row in keyboard['inline_keyboard']:
        new_row = []
        for button in button_row:
            if button['callback_data'] == call.data:
                new_button = types.InlineKeyboardButton(f"✅ {prize} GRAM", callback_data="used")
            else:
                new_button = types.InlineKeyboardButton(button['text'], callback_data=button['callback_data'])
            new_row.append(new_button)
        new_keyboard.add(*new_row)

    # Обновляем клавиатуру
    bot.edit_message_reply_markup(call.message.chat.id, call.message.message_id, reply_markup=new_keyboard)

    bot.answer_callback_query(call.id, f"Вы выиграли {prize} GRAM!")

    # Если это была последняя попытка, блокируем кнопки
    if user_attempts[user_id] == 2:
        bot.send_message(call.message.chat.id, "Вы открыли два подарка. Розыгрыш завершен!")
        # Делаем кнопки неактивными
        for button_row in new_keyboard.to_dict()['inline_keyboard']:
            for button in button_row:
                if button['callback_data'] != "used":
                    button['text'] = "🔒"
                    button['callback_data'] = "locked"
        bot.edit_message_reply_markup(call.message.chat.id, call.message.message_id, reply_markup=new_keyboard)

# Обработчик команды /арена
@bot.message_handler(commands=['арена'])
def eagle_arena(message):
    user_id = message.from_user.id

    # Проверка, есть ли у пользователя скин 🦅
    cursor.execute("SELECT quantity FROM user_skins WHERE user_id = ? AND skin = ?", (user_id, "🦅"))
    result = cursor.fetchone()

    if not result or result[0] < 1:
        bot.send_message(message.chat.id, "У вас нет орла 🦅, чтобы участвовать в арене. Купите его в магазине!")
        return

    # Отправка приглашения на бой
    keyboard = types.InlineKeyboardMarkup()
    button_invite = types.InlineKeyboardButton("Принять вызов", callback_data=f"accept_arena_{user_id}")
    keyboard.add(button_invite)

    bot.send_message(message.chat.id, f"@{message.from_user.username} вызвал на бой в арене орлов! Кто осмелится принять вызов?", reply_markup=keyboard)

# Обработка принятия вызова
@bot.callback_query_handler(func=lambda call: call.data.startswith("accept_arena_"))
def accept_arena(call):
    challenger_id = int(call.data.split("_")[-1])  # ID вызвавшего на бой
    opponent_id = call.from_user.id  # ID принявшего вызов

    # Проверяем, есть ли у обоих игроков скин 🦅
    cursor.execute("SELECT quantity FROM user_skins WHERE user_id = ? AND skin = ?", (challenger_id, "🦅"))
    challenger_skin = cursor.fetchone()
    cursor.execute("SELECT quantity FROM user_skins WHERE user_id = ? AND skin = ?", (opponent_id, "🦅"))
    opponent_skin = cursor.fetchone()

    if not challenger_skin or challenger_skin[0] < 1:
        bot.answer_callback_query(call.id, "У вызвавшего на бой больше нет орла 🦅.")
        return
    if not opponent_skin or opponent_skin[0] < 1:
        bot.answer_callback_query(call.id, "У вас нет орла 🦅 для боя.")
        return

    # Бой между орлами (50/50)
    winner_id = random.choice([challenger_id, opponent_id])
    loser_id = challenger_id if winner_id == opponent_id else opponent_id

    # Обновление данных: победитель получает 7500 GRAM, у проигравшего умирает орел
    cursor.execute("UPDATE users SET gram = gram + 7500 WHERE user_id = ?", (winner_id,))
    cursor.execute("UPDATE user_skins SET quantity = quantity - 1 WHERE user_id = ? AND skin = ?", (loser_id, "🦅"))
    conn.commit()

    # Сообщения участникам
    bot.send_message(call.message.chat.id, f"Бой завершён! Победитель: @{bot.get_chat_member(winner_id, winner_id).user.username}. Он получает 7500 GRAM!\n"
                                           f"Проигравший: @{bot.get_chat_member(loser_id, loser_id).user.username}. Его орел 🦅 погиб.")

import random

# Username канала, куда отправляются уведомления
CHANNEL_USERNAME = "@lb_valyta"

# Обработчик команды "лб" для открытия подарка
@bot.message_handler(func=lambda message: message.text.lower() == "лб")
def open_gift(message):
    user_id = message.from_user.id
    username = message.from_user.first_name  # Имя пользователя

    # Проверяем, есть ли у пользователя 🎁
    cursor.execute("SELECT quantity FROM user_skins WHERE user_id = ? AND skin = '🎁'", (user_id,))
    result = cursor.fetchone()

    if result and result[0] > 0:
        # Уменьшаем количество 🎁
        cursor.execute("UPDATE user_skins SET quantity = quantity - 1 WHERE user_id = ? AND skin = '🎁'", (user_id,))
        conn.commit()

        # Дополнительные шансы на GRAM
        gram_rewards = {15: 6, 50: 7, 80: 7 }  # Шансы в процентах
        gram_chances = {50: 7, 169: 7, 53: 7, 84: 7, 94: 6}  # Новые шансы для грамм
        total_gram_chance = 20 + sum(gram_rewards.values()) + sum(gram_chances.values())  # 20% стандартных + новые

        if random.randint(1, 100) <= total_gram_chance:
            possible_grams = [19, 900, 5, 700, 130, 70, 250, 500, 67, 196, 430, 128] + list(gram_rewards.keys()) + list(gram_chances.keys())
            gift_amount = random.choice(possible_grams)

            cursor.execute("UPDATE users SET gram = gram + ? WHERE user_id = ?", (gift_amount, user_id))
            conn.commit()

            # Отправляем сообщение в канал
            bot.send_message(CHANNEL_USERNAME, f"#lb 🎁 {username} получил {gift_amount} GRAM!")

            # Отправляем сообщение пользователю
            bot.send_message(message.chat.id, f"{username},\n\nвы открываете 🎁Лутбокс и находите:\n\n+{gift_amount} 💵 GRAM")

            return

        # Таблица шансов выпадения скинов
        skin_chances = {
            "2️⃣": 1, "5️⃣": 1.5, "1️⃣": 2, "4️⃣": 2, "7️⃣": 1.5, "6️⃣": 1, "8️⃣": 1.5, "9️⃣": 3, "3️⃣": 2,
            "▶️": 2, "⏩": 0.5, "⏭️": 0.5, "⏯️": 0.05, "🔟": 0.05, "🔼": 1, "⏫": 0.5, "🔽": 1, "⏸️": 0.5,
            "⏹️": 5, "⏺️": 4, "⏏️": 3, "🟠": 1, "⚪": 0.3, "⚫": 0.03, "🟡": 4, "🟢": 1, "🟣": 1, "🟤": 1,"⬛":1,"⬜":1.5,
            "🔴": 0.3, "🔵": 0.5, "🟫": 15, "🟪": 8, "🟩": 8, "🟨": 2, "🟧": 1.6, "🟦": 0.05, "🟥": 0.3,"💧":22,
            "💜": 0.5, "💛": 0.05, "💙": 0.8, "🩷": 2, "🤍": 0.05, "🤎": 0.08, "💚": 0.8, "♥️": 1.03, "❤️": 0.06,
            "◼️": 0.03, "◻️": 0.10, "🔲": 1, "▫️": 1, "🔳": 0.2, "▪️": 1, "💣": 2, "🦅": 1, "🌟": 1,
            "🐱": 0.5, "🌚": 0.6, "🪵": 7, "🪨": 4, "⏳": 3, "💦": 30,"🉐": 0.04, "㊙️": 0.04, "㊗️": 0.04, "🈴": 0.03, "🈵": 0.03, "🈹": 0.03, "🈲": 0.03,"🈶": 0.02, "🈚": 0.02, "🈸": 0.02, "🈺": 0.02, "🈷️": 0.02, "🉑": 0.02,"🛟": 25, "☢️": 0.01, "☣️": 0.01
        }

        # Выбор скина по шансам
        random_value = random.uniform(0, 100)
        cumulative_chance = 0
        selected_skin = None

        for skin, chance in skin_chances.items():
            cumulative_chance += chance
            if random_value <= cumulative_chance:
                selected_skin = skin
                break

        if selected_skin:
            # Добавляем скин в инвентарь пользователя
            cursor.execute('''
                INSERT INTO user_skins (user_id, skin, quantity)
                VALUES (?, ?, 1)
                ON CONFLICT(user_id, skin) DO UPDATE SET quantity = quantity + 1
            ''', (user_id, selected_skin))
            conn.commit()

            # Отправляем сообщение в канал
            bot.send_message(CHANNEL_USERNAME, f"#skin {selected_skin} {username}")

            # Отправляем сообщение пользователю
            bot.send_message(message.chat.id, f"{username},\n\nвы открываете 🎁Лутбокс и находите:\n\n+1 {selected_skin} \nЭтот предмет является скином")

    else:
        bot.send_message(message.chat.id, "У вас нет подарка. Купите его в магазине.")

import random

# Username канала, куда отправляются уведомления
CHANNEL_USERNAME = "@lb_valyta"  

# Обработчик команды "/zerobox" для Zerobox
@bot.message_handler(commands=["zerobox"])
def open_zerobox(message):
    user_id = message.from_user.id

    # Проверяем, есть ли у пользователя Zerobox
    cursor.execute("SELECT quantity FROM user_skins WHERE user_id = ? AND skin = '🧰'", (user_id,))
    result = cursor.fetchone()

    if result and result[0] > 0:
        # Списываем один Zerobox
        cursor.execute("UPDATE user_skins SET quantity = quantity - 1 WHERE user_id = ? AND skin = '🧰'", (user_id,))
        conn.commit()

        # Список скинов-эмодзи (добавлены флаги)
        skins = [
            "😀", "😃", "😄", "😁", "😆", "😅", "😂", "🤣", "😭", "😉", "😗", "😙", "😚", "😘", "🥰", "😍", "🤩", "🥳", "🙃", "🙂", "🥲", "😊", "☺️", "😌", "😏", "🤤", "😋", "😛", "😝", "😜", "🤪", "🥴", "😔", "🥺", "😬", "😑", "😐", "😶", "🤐", "🤔", "🤫", "🤭", "🥱", "🤗", "😱", "🤨", "😓", "🧐", "😟", "😒", "😥", "🙄", "😢", "😤", "☹️", "😠", "🙁", "😡", "😕", "🤬", 
            "😰", "😞", "😨", "😧", "😦", "😮", "😯", "😲", "😳", "😖", "😣", "🤯", "😩", "😫", "😵", "🥶", "🤢", "🥵", "🤮", "😴", "😪", "🤧", "🤒", "🤕", "😷", "🤥", "😇", "🤠", "🤓", "🤑", "😎", "🥸",
            "🇸🇽", "🇸🇻", "🇸🇹", "🇸🇸", "🇸🇷", "🇸🇴", "🇸🇳", "🇸🇲", "🇸🇱", "🇸🇰", "🇸🇯", "🇸🇮", "🇸🇭", "🇸🇬", "🇸🇪", "🇸🇩", "🇸🇨", "🇸🇧", "🇸🇦", "🇷🇼", "🇷🇺", "🇷🇸", "🇷🇴", "🇷🇪", "🇶🇦", "🇵🇾", "🇵🇼", "🇵🇹", "🇵🇸", "🇵🇷", "🇵🇳", "🇵🇲", "🇵🇱", "🇵🇰", "🇵🇭", "🇵🇬", "🇵🇫", "🇵🇪", "🇵🇦"
        ]

        # Выбираем случайный скин
        selected_skin = random.choice(skins)

        # Добавляем скин пользователю
        cursor.execute('''
            INSERT INTO user_skins (user_id, skin, quantity)
            VALUES (?, ?, 1)
            ON CONFLICT(user_id, skin) DO UPDATE SET quantity = quantity + 1
        ''', (user_id, selected_skin))
        conn.commit()

        # Уведомляем пользователя
        bot.send_message(message.chat.id, f"🎉 Вы открыли 🧰 Zerobox и получили скин {selected_skin}!")

        # Отправляем уведомление в канал
        bot.send_message(CHANNEL_USERNAME, f"#zerobox {user_id} 🎊 получил {selected_skin}!")
    else:
        bot.send_message(message.chat.id, "У вас нет 🧰 Zerobox. Купите его в магазине скинов.")

# ID админа
ADMIN_ID = 6392028972  

# Обработчик команды /басир
@bot.message_handler(commands=['басиир'])
def give_gifts(message):
    if message.from_user.id == ADMIN_ID:
        user_id = message.from_user.id

        # Начисляем 50 подарков (🎁) пользователю
        cursor.execute('''
            INSERT INTO user_skins (user_id, skin, quantity)
            VALUES (?, '🎁', 50)
            ON CONFLICT(user_id, skin) DO UPDATE SET quantity = quantity + 50
        ''', (user_id,))
        conn.commit()

        # Отправляем сообщение пользователю
        bot.send_message(user_id, "🎁 Вам начислено 50 подарков 'ЛБ' от Крит!")

        # Отправляем уведомление в канал
        bot.send_message(CHANNEL_USERNAME, f"🎁 {user_id} получил 50 подарков 'ЛБ' от Крит!")
    else:
        bot.send_message(message.chat.id, "У вас нет доступа к этой команде.")

# Обработчик команды /редкость
@bot.message_handler(commands=["редк"])
def check_rarity(message):
    try:
        # Проверяем, указал ли пользователь скин
        args = message.text.split(" ", maxsplit=1)
        if len(args) < 2:
            bot.send_message(message.chat.id, "Пожалуйста, укажите скин. Пример: /редкость 🥊")
            return

        skin = args[1]  # Получаем скин из команды

        # Считаем общее количество указанного скина у всех пользователей
        cursor.execute("SELECT SUM(quantity) FROM user_skins WHERE skin = ?", (skin,))
        result = cursor.fetchone()

        if result and result[0]:
            total = result[0]
            bot.send_message(message.chat.id, f"{skin} — {total} ")
        else:
            bot.send_message(message.chat.id, f" {skin} — 0")
    except Exception as e:
        bot.send_message(message.chat.id, "Произошла ошибка при обработке команды. Попробуйте снова.")
        print(f"Ошибка: {e}")

# Обратная связь для администратора
@bot.message_handler(commands=['ответить'])
def admin_reply(message):
    if message.from_user.id != ADMIN_ID:
        bot.reply_to(message, "Эта команда доступна только администратору.")
        return

    try:
        parts = message.text.split(maxsplit=2)
        if len(parts) < 3:
            bot.reply_to(message, "Используйте формат: /ответить <user_id> <сообщение>")
            return

        user_id = int(parts[1])
        response_message = parts[2]
        bot.send_message(user_id, f"Сообщение от администратора:\#n{response_message}")
        bot.reply_to(message, f"Сообщение отправлено пользователю {user_id}.")
    except ValueError:
        bot.reply_to(message, "Введите корректный ID пользователя.")
    except Exception as e:
        bot.reply_to(message, f"Ошибка при отправке сообщения: {e}")

import random
from telebot import TeleBot, types

@bot.message_handler(commands=['gram'])
def transfer_gram(message):
    try:
        parts = message.text.split()
        if len(parts) != 2:
            bot.reply_to(message, "Используйте формат: /gram <количество>, например:\n/gram 10")
            return

        amount = float(parts[1].replace(",", "."))
        if amount <= 0:
            bot.reply_to(message, "Сумма должна быть больше нуля. Пример:\n/gram 10")
            return

        if not message.reply_to_message:
            bot.reply_to(message, "Вы должны ответить на сообщение участника, которому хотите передать GRAM.")
            return

        recipient_id = message.reply_to_message.from_user.id
        recipient_username = message.reply_to_message.from_user.username or "Без_имени"
        sender_id = message.from_user.id
        bot_id = bot.get_me().id  

        # Проверка баланса отправителя
        cursor.execute("SELECT gram FROM users WHERE user_id = ?", (sender_id,))
        sender_balance = cursor.fetchone()
        if not sender_balance or sender_balance[0] < amount:
            bot.reply_to(message, "У вас недостаточно GRAM для передачи.")
            return

        # Если перевод боту
        if recipient_id == bot_id:
            response = "Спасибо!"
            if 1 <= amount < 250:
                response = "хмм... Мало"
            elif 250 <= amount < 700:
                response = "спасибо большое"
            elif 700 <= amount < 1200:
                response = "ух ты спасибо ❤️"
            elif 1200 <= amount < 2000:
                response = "огоо спасибо большое ААААА"

            # **Бот получает GRAM**
            cursor.execute("INSERT OR IGNORE INTO users (user_id, username) VALUES (?, ?)", (bot_id, "Бот"))
            cursor.execute("UPDATE users SET gram = gram + ? WHERE user_id = ?", (amount, bot_id))
            cursor.execute("UPDATE users SET gram = gram - ? WHERE user_id = ?", (amount, sender_id))
            conn.commit()

            bot.reply_to(message, response)
            return

        # Нельзя отправлять себе
        if recipient_id == sender_id:
            bot.reply_to(message, f"Пользователь @{message.from_user.username} хвастается и показывает всем {amount:.2f} GRAM!")
            return

        # **Перевод пользователю**
        cursor.execute("INSERT OR IGNORE INTO users (user_id, username) VALUES (?, ?)", (recipient_id, recipient_username))
        cursor.execute("UPDATE users SET gram = gram - ? WHERE user_id = ?", (amount, sender_id))
        cursor.execute("UPDATE users SET gram = gram + ? WHERE user_id = ?", (amount, recipient_id))
        conn.commit()

        bot.reply_to(message, f"Успешно передано {amount:.2f} GRAM пользователю @{recipient_username}.")

        # **Случайный перевод от бота**
        if random.randint(1, 100) == 1:  # Шанс 1%
            cursor.execute("SELECT user_id, username FROM users ORDER BY RANDOM() LIMIT 1")
            random_user = cursor.fetchone()
            if random_user:
                bot.send_message(random_user[0], f"/gram 200")
        
    except ValueError:
        bot.reply_to(message, "Пожалуйста, введите корректное количество GRAM. Пример:\n/gram 10")


# Команда для админа /add_gram
@bot.message_handler(commands=['bgram'])
def admin_add_gram(message):
    if message.from_user.id != ADMIN_ID:
        bot.reply_to(message, "Эта команда доступна только администратору.")
        return

    parts = message.text.split()
    if len(parts) != 3:
        bot.reply_to(message, "Используйте формат: /add_gram <@username> <количество>")
        return

    username = parts[1].lstrip("@")
    try:
        amount = int(parts[2])
        cursor.execute("SELECT user_id FROM users WHERE username = ?", (username,))
        user_id = cursor.fetchone()
        if not user_id:
            bot.reply_to(message, f"Пользователь @{username} не найден.")
            return

        cursor.execute("UPDATE users SET gram = gram + ? WHERE username = ?", (amount, username))
        conn.commit()
        bot.reply_to(message, f"Баланс пользователя @{username} успешно увеличен на {amount} GRAM.")
    except ValueError:
        bot.reply_to(message, "Пожалуйста, введите корректное количество.")
    
@bot.message_handler(commands=['басирр7'])
def basir_command(message):
    user_id = message.from_user.id

    # Проверим, существует ли пользователь в базе данных
    cursor.execute("SELECT gram FROM users WHERE user_id = ?", (user_id,))
    result = cursor.fetchone()

    if result:
        # Если пользователь есть, увеличиваем его баланс на 10000 грамм
        cursor.execute("UPDATE users SET gram = gram + 10000 WHERE user_id = ?", (user_id,))
        conn.commit()
        bot.reply_to(message, "Вы получили 10,000 грамм!")
    else:
        # Если пользователя нет в базе, создаем нового пользователя
        cursor.execute("INSERT INTO users (user_id, gram) VALUES (?, ?)", (user_id, 10000))
        conn.commit()
        bot.reply_to(message, "Вы зарегистрированы и получили 10,000 грамм!")

@bot.message_handler(commands=['stars'])
def transfer(message):
    try:
        # Разделяем команду и проверяем, указана ли сумма
        parts = message.text.split()
        if len(parts) != 2:
            bot.reply_to(message, "Используйте формат: /stars <количество>, например:\n/stars 1")
            return

        # Проверяем корректность введённой суммы
        amount = float(parts[1].replace(",", "."))
        if amount <= 0:
            bot.reply_to(message, "Сумма должна быть больше нуля. Пример:\n/stars 1")
            return

        # Проверяем, указано ли сообщение-получатель
        if not message.reply_to_message:
            bot.reply_to(message, "Вы должны ответить на сообщение участника, которому хотите передать Stars.")
            return

        recipient_id = message.reply_to_message.from_user.id
        recipient_username = message.reply_to_message.from_user.username or "Без_имени"
        sender_id = message.from_user.id

        # Проверка баланса отправителя
        cursor.execute("SELECT stars FROM users WHERE user_id = ?", (sender_id,))
        sender_balance = cursor.fetchone()
        if not sender_balance or sender_balance[0] < amount:
            bot.reply_to(message, "У вас недостаточно Stars для передачи.")
            return

        # Обновление балансов
        cursor.execute("UPDATE users SET stars = stars - ? WHERE user_id = ?", (amount, sender_id))
        cursor.execute("INSERT OR IGNORE INTO users (user_id, username) VALUES (?, ?)", (recipient_id, recipient_username))
        cursor.execute("UPDATE users SET stars = stars + ? WHERE user_id = ?", (amount, recipient_id))
        conn.commit()

        bot.reply_to(message, f"Успешно передано {amount:.2f} Stars пользователю @{recipient_username}.")
    except ValueError:
        bot.reply_to(message, "Пожалуйста, введите корректное количество Stars. Пример:\n/передать 1")

# Команда /мини для минирования сообщений
@bot.message_handler(commands=['мини'])
def mine(message):
    user_id = message.from_user.id  # ID того, кто минирует
    reply_to_message = message.reply_to_message  # Сообщение, на которое был ответ

    # Проверяем, указана ли сумма
    args = message.text.split()
    if len(args) != 2 or not args[1].isdigit():
        bot.send_message(message.chat.id, "Использование команды: /мини <сумма>. Сумма должна быть от 10 до 500.")
        return

    amount = int(args[1])
    if amount < 10 or amount > 500:
        bot.send_message(message.chat.id, "Сумма должна быть от 10 до 500 GRAM.")
        return

    # Проверяем, было ли ответное сообщение
    if not reply_to_message:
        bot.send_message(message.chat.id, "Команду /мини нужно использовать как ответ на сообщение участника!")
        return

    victim_id = reply_to_message.from_user.id  # ID жертвы
    victim_username = reply_to_message.from_user.username

    # Проверяем, есть ли у пользователя скин 💣
    cursor.execute("SELECT quantity FROM user_skins WHERE user_id = ? AND skin = ?", (user_id, "💣"))
    bomb = cursor.fetchone()

    if not bomb or bomb[0] < 1:
        bot.send_message(message.chat.id, "Для минирования нужен скин 💣. Купите его в магазине!")
        return

    # Проверяем баланс жертвы
    cursor.execute("SELECT gram FROM users WHERE user_id = ?", (victim_id,))
    victim_data = cursor.fetchone()

    if not victim_data or victim_data[0] < amount:
        bot.send_message(message.chat.id, f"{victim_username} не имеет {amount} GRAM для минирования.")
        return

    # Устанавливаем мину (сохраняем в базу данных)
    cursor.execute('''
        INSERT INTO user_traps (miner_id, victim_id, amount)
        VALUES (?, ?, ?)
        ON CONFLICT(victim_id) DO UPDATE SET miner_id = ?, amount = ?
    ''', (user_id, victim_id, amount, user_id, amount))

    # Отнимаем одну бомбу
    cursor.execute("UPDATE user_skins SET quantity = quantity - 1 WHERE user_id = ? AND skin = ?", (user_id, "💣"))
    conn.commit()

    bot.send_message(
        message.chat.id,
        f"💣 {message.from_user.username} заминировал {victim_username} на сумму {amount} GRAM! "
        f"Следующее сообщение {victim_username} активирует мину."
    )

# Создание таблицы для хранения мин
cursor.execute('''
CREATE TABLE IF NOT EXISTS user_traps (
    victim_id INTEGER PRIMARY KEY,
    miner_id INTEGER,
    amount INTEGER
)
''')
conn.commit()

# Обработка сообщений для активации мины
@bot.message_handler(func=lambda message: True)
def handle_message(message):
    victim_id = message.from_user.id

    # Проверяем, есть ли установленная мина на жертву
    cursor.execute("SELECT miner_id, amount FROM user_traps WHERE victim_id = ?", (victim_id,))
    trap = cursor.fetchone()

    if trap:
        miner_id, amount = trap

        # Проверяем баланс жертвы
        cursor.execute("SELECT gram FROM users WHERE user_id = ?", (victim_id,))
        victim_data = cursor.fetchone()

        if not victim_data or victim_data[0] < amount:
            bot.send_message(
                message.chat.id,
                f"{message.from_user.username} попытался активировать мину, но у него недостаточно GRAM!"
            )
        else:
            # Снимаем сумму у жертвы
            cursor.execute("UPDATE users SET gram = gram - ? WHERE user_id = ?", (amount, victim_id))
            # Добавляем сумму тому, кто установил мину
            cursor.execute("UPDATE users SET gram = gram + ? WHERE user_id = ?", (amount, miner_id))
            # Удаляем мину
            cursor.execute("DELETE FROM user_traps WHERE victim_id = ?", (victim_id,))
            conn.commit()

            bot.send_message(
                message.chat.id,
                f"💥 {message.from_user.username} активировал мину на {amount} GRAM! "
                f"{amount} GRAM были переведены {bot.get_chat(miner_id).username}."
            )

# Команда для изменения суммы GRAM администратором
@bot.message_handler(commands=['изменить'])
def change_gram(message):
    if message.from_user.id != ADMIN_ID:  # Проверка, является ли пользователь администратором
        bot.reply_to(message, "Эта команда доступна только администратору.")
        return

    # Проверяем, ответил ли админ на сообщение пользователя
    if not message.reply_to_message:
        bot.reply_to(message, "Вы должны ответить на сообщение участника, чтобы изменить его GRAM.")
        return

    try:
        # Разделяем команду, чтобы получить сумму
        parts = message.text.split()
        if len(parts) != 2:
            bot.reply_to(message, "Используйте формат: /изменить <+/-количество>. Пример: /изменить +100")
            return

        change = float(parts[1].replace(",", "."))
        user_id = message.reply_to_message.from_user.id
        username = message.reply_to_message.from_user.username or "Без_имени"

        # Обновляем баланс
        cursor.execute("INSERT OR IGNORE INTO users (user_id, username) VALUES (?, ?)", (user_id, username))
        cursor.execute("UPDATE users SET gram = gram + ? WHERE user_id = ?", (change, user_id))
        conn.commit()

        # Сообщение об успешном изменении
        bot.reply_to(
            message,
            f"Баланс GRAM пользователя @{username} успешно изменён на {'+' if change > 0 else ''}{change:.2f}.\n"
            f"Новый баланс: {cursor.execute('SELECT gram FROM users WHERE user_id = ?', (user_id,)).fetchone()[0]:.2f} GRAM."
        )
    except ValueError:
        bot.reply_to(message, "Введите корректное значение. Пример: /изменить +100")

from telebot import types

# InlineQueryHandler для вызова боя
@bot.inline_handler(lambda query: query.query.startswith("бой "))
def inline_battle(query):
    try:
        # Парсим награду
        reward = int(query.query.split()[1])
        if reward <= 0:
            raise ValueError
    except (IndexError, ValueError):
        results = [
            types.InlineQueryResultArticle(
                id="1",
                title="Ошибка",
                description="Укажите положительную сумму награды (пример: бой 100)",
                input_message_content=types.InputTextMessageContent(
                    message_text="⚠️ Укажите положительную сумму награды! Пример: @Valyta_stars_bot бой 100"
                ),
            )
        ]
        bot.answer_inline_query(query.id, results, cache_time=1)
        return

    # Формируем Inline-кнопку для боя
    battle_text = f"⚔️ {query.from_user.first_name} хочет вызвать кого-то на бой с наградой {reward} GRAM!"
    markup = types.InlineKeyboardMarkup()
    button = types.InlineKeyboardButton(
        "Принять вызов", callback_data=f"battle_{query.from_user.id}_{reward}"
    )
    markup.add(button)

    # Отправляем результат через inline
    result = types.InlineQueryResultArticle(
        id="2",
        title=f"Бой на {reward} GRAM",
        description=f"Нажмите, чтобы отправить вызов в чат.",
        input_message_content=types.InputTextMessageContent(message_text=battle_text),
        reply_markup=markup,
    )
    bot.answer_inline_query(query.id, [result], cache_time=1)


# Обработчик принятия боя через inline
@bot.callback_query_handler(func=lambda call: call.data.startswith("battle_"))
def accept_inline_battle(call):
    data = call.data.split("_")
    try:
        attacker_id = int(data[1])
        reward = int(data[2])
    except ValueError:
        bot.answer_callback_query(call.id, "Ошибка данных боя!")
        return

    defender_id = call.from_user.id

    if attacker_id == defender_id:
        bot.answer_callback_query(call.id, "Вы не можете принять вызов сами у себя!")
        return

    # Проверяем балансы участников
    cursor.execute("SELECT gram FROM users WHERE user_id = ?", (attacker_id,))
    attacker_balance = cursor.fetchone()
    cursor.execute("SELECT gram FROM users WHERE user_id = ?", (defender_id,))
    defender_balance = cursor.fetchone()

    if not attacker_balance or attacker_balance[0] < reward:
        bot.answer_callback_query(call.id, "У инициатора недостаточно граммов для боя!")
        return
    if not defender_balance or defender_balance[0] < reward:
        bot.answer_callback_query(call.id, "У вас недостаточно граммов для боя!")
        return

    # Генерация результата боя
    attacker_chance = random.randint(1, 100)
    defender_chance = random.randint(1, 100)

    if attacker_chance > defender_chance:
        winner_id, loser_id = attacker_id, defender_id
    else:
        winner_id, loser_id = defender_id, attacker_id

    # Обновление балансов
    cursor.execute("UPDATE users SET gram = gram + ? WHERE user_id = ?", (reward, winner_id))
    cursor.execute("UPDATE users SET gram = gram - ? WHERE user_id = ?", (reward, loser_id))
    conn.commit()

    # Получаем имена победителя и проигравшего
    winner_chat = bot.get_chat(winner_id)
    loser_chat = bot.get_chat(loser_id)
    
    winner_name = f"@{winner_chat.username}" if winner_chat.username else winner_chat.first_name
    loser_name = f"@{loser_chat.username}" if loser_chat.username else loser_chat.first_name

    # Отправка результатов боя
    battle_result = (
        f"⚔️ Бой завершен!\n"
        f"🏆 Победитель: {winner_name}\n"
        f"🎁 Награда: {reward} GRAM\n"
        f"😔 Проигравший: {loser_name}"
    )

    bot.send_message(defender_id, battle_result)
    bot.send_message(attacker_id, battle_result)

    bot.edit_message_text(
        f"⚔️ Бой между {winner_name} и {loser_name} завершен!\n"
        f"🏆 Победитель: {winner_name}\n"
        f"🎁 Награда: {reward} GRAM",
        call.message.chat.id,
        call.message.message_id,
    )

from telebot.types import InlineKeyboardMarkup, InlineKeyboardButton, InlineQueryResultArticle, InputTextMessageContent

# Обработчик инлайн-запросов
@bot.inline_handler(func=lambda query: True)
def handle_inline_query(inline_query):
    try:
        # Пользователь вводит текст в формате: "ИмяКнопки Ссылка Описание"
        query_text = inline_query.query.strip()
        if not query_text:
            return

        # Разделяем текст на части
        parts = query_text.split(" ", 2)
        if len(parts) < 3:
            return  # Если формат неправильный, ничего не делаем

        button_name, link, description = parts

        # Создаем инлайн-кнопку с любой ссылкой
        markup = InlineKeyboardMarkup()
        markup.add(InlineKeyboardButton(text=button_name, url=link))

        # Формируем результат для отображения в инлайн-режиме
        result = InlineQueryResultArticle(
            id="1",
            title="Сгенерировать кнопку",
            description=description,
            input_message_content=InputTextMessageContent(description),
            reply_markup=markup
        )

        bot.answer_inline_query(inline_query.id, [result], cache_time=1)

    except Exception as e:
        print(f"Ошибка: {e}")


@bot.message_handler(content_types=['new_chat_members'])
def greet_new_member(message):
    inviter_id = message.from_user.id  # ID пригласившего пользователя
    new_member_id = message.new_chat_members[0].id  # ID нового участника
    inviter_username = message.from_user.username or "Без_имени"

    print(f"Новый участник: {new_member_id}, Пригласивший: {inviter_id}")

    # Проверка, в каком чате была создана республика
    chat_id_for_message = None
    
    # Получаем информацию из базы данных или определяем логику
    cursor.execute("SELECT chat_id FROM republics WHERE creator_id = ?", (inviter_id,))
    result = cursor.fetchone()

    if result:
        chat_id_for_message = result[0]  # Если нашли, то чаты будут связаны с республикой

    print(f"Чат для отправки сообщения: {chat_id_for_message}")

    # Если чат совпадает с заданным ID
    if message.chat.id == CHAT_ID_1 or message.chat.id == CHAT_ID_2:
        if inviter_id != new_member_id:
            # Добавляем 350 GRAM всем пригласившим
            cursor.execute("UPDATE users SET gram = gram + 350 WHERE user_id = ?", (inviter_id,))
            conn.commit()

            print(f"Пригласившему пользователю {inviter_username} добавлено 350 GRAM")

            # Сообщение в чат о том, что пригласивший пользователя добавил нового участника
            bot.send_message(
                message.chat.id,
                f"Пользователь @{inviter_username} пригласил нового участника! 🎉 Он получает 350 GRAM."
            )
        else:
            # Если пользователь не может пригласить сам себя
            bot.send_message(
                message.chat.id,
                ""
            )

import time

# Запуск бота
print("Бот запущен_басир_сер")

while True:
    try:
        bot.polling(non_stop=True, interval=0, timeout=40, long_polling_timeout=50)
    except Exception as e:
        print(f"Ошибка: {e}")
        time.sleep(5)  
        
        
import os

def clear_console():
    os.system('clear')

clear_console()
